# 第六章图形修订

本目录承接用户《第6章.docx》及两张示意图方案，并按已确认的待审稿绘制：

- `q1_workflow`：从统一模型、问题一物性与输入到温度和含水率两条独立求解支路。输出框的 0–1800 s 表示求解时域；正式 `result1.xlsx` 的逐秒输出仍为 1–1800 s。
- `q1_boundary`：中截面与径向区间、轴心对称条件和表面 Robin 条件。热量传入与水分迁出分别标注；`b(t)` 是有效边界值。
- `q1_profiles`：从原 `q1_unrounded.npz` 读取已有采样值，提高字号与文字对比度、居中子图标题。保留坐标范围、色标范围、着色方式及末时数值，不插值或平滑数据。

中文使用宋体，英文和数字使用 Times New Roman。图形以 PDF、SVG、PNG 保存；论文使用 PDF。

完整仓库重绘入口：`python paper/q1234_draft_v1/build_ch6_assets.py`。仅后处理已保存数据，不运行任何 PDE 或参数情景。独立源稿包使用已生成的图片直接编译，无须重新生成数值结果。

实际读取基线为 `0b402a4b372c8f46cebc74abe0b63186a805d520`。原图及历史绘图证据不覆盖；来源、数组一致性与输出哈希见[本轮图形记录](../../evidence/ch6_revision_20260913/figure_sources.json)，文字与编译核验见[本轮修订记录](../../evidence/ch6_revision_20260913/第六章文字与图示修订记录.md)。
