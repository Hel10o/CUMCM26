# 四问论文初稿 v1 实际验收

日期：2026-09-12。实际读取的输入提交：`ad9c6766d6db2f21ee61d6d5a1a7a9c54c28f377`。本轮完成四问初稿与可编辑源码交付；未改写现行四份正式答案，也未重跑全部历史 PDE。

## 内容及用户修改

完整稿已含摘要、共同假设/方程、Q1–Q4、六张规定表、五幅图、二维检验、环境情景、适用范围、结论、AI声明、7条参考文献和完整采用代码。根据用户追加要求，摘要中的第一、第二、第三、第四问分别独立成段，仍完整落在第1页。

Q2采用新完整逐秒工作簿与统一轨迹；Q3/Q4条件执行值分别为57.4741 h、51.0921 h。Q4粗二维正式点未达标、配对校正与精细直接补证的区别明确保留。物理边界和潜热诊断没有写成真实实验验证或严格数学保证。

## 编译与格式

- 实际工具：Windows TeX Live 2026、XeLaTeX、latexmk；调用与结果保存于 `build_full.log`。编译脚本为 `build.ps1`。
- 最终论文：**146页**。摘要第1页；主体、AI声明与参考文献第2–19页，共**18页**；完整代码附录从第20页起，共127页。附录页数单列，不将其并入30页正文限制。
- `AI工具使用详情.pdf`共**4页**。两份PDF均远小于20 MB；实际字节数、SHA256和嵌入字体报告见 `final_artifacts.json`。
- A4，四边设置3 cm，摘要首页、无目录、居中连续阿拉伯页码；电子稿不含承诺书和编号页。论文和AI详情作者元数据为空。
- 最终 LaTeX 日志没有Overfull、缺字或未定义引用。个别正常Underfull只是两端对齐疏密提示。完整代码使用Consolas，并为摄氏符号使用中文字体；路径可自动换行，源码标题前保留最小后续空间。
- 两份PDF全部使用字体均嵌入。没有把字体字号或ctexart声称为官方指定；当届规范实际获取证据见 `official_sources.json`。

## 数据与程序检查

`validate_paper.py`在最终PDF实际返回0，**37项通过、0项失败**，另2项为明确边界信息；见 `pdf_validation.json`。

六张题表共**279个正文单元格**与预期逐格一致：216个温湿数、45个时间、18个Q4域外空白。预期表还经独立代理对正式CSV逐行核对。本项证明正式数据到论文的转录一致性，不等于对所有模型状态重做独立数值求解。机器同时检查A4、页码、实际文字框、已知身份及绝对路径、引用唯一性、AI声明顺序和运行过程中输入哈希稳定性。

机器验收还真实测试了缺文件、缺附录、改错单格数值三个拒绝路径，没有将输入异常当作成功。文字框采用25 mm范围加2 pt字体bbox容差，页脚只豁免实际识别的居中页码。

代码附录包含42个采用源文件及1个新增启动脚本，共43段全文；不是节选。匿名快照中64文件哈希检查通过，42个Python文件AST、25个安全`--help`、1个JavaScript语法检查通过，见 `support/smoke_result.json`。这些检查未执行PDE或大工作簿导出；后者的数据及环境依赖明确列于 `support/README.md`。

## 版面复核与复现边界

正文、附录和AI详情已按实际PDF渲染检查；具体快照SHA、全页联系图覆盖、放大样本及附录标题的发现和修复复查，见 `independent_visual_review.md`。主代理另外检查了最终摘要及代表性正文、参考文献和附录页。联系图覆盖不等于逐字重读全部代码；不得据此宣称人工完成核心建模审核。

源稿包由 `package_source.py`生成并逐项读回核对，清单和字节哈希见 `source_package.json`。它服务于编辑编译，**不是已验收的最终竞赛支撑包**；大数组、正式Excel及原始冻结证据保留在仓库原目录。PDF初稿版式验收不替代队员人工复核、完整AI使用记录与最终支撑材料20 MB检查。

本轮Git差异应仅为根README与新建 `paper/q1234_draft_v1/`；旧论文、四问交付和既有复审包保持原字节。提交后的远程一致性由交付时另行实际核对，不在提交前填造远程提交号。
