# 摘要综合稿确认与替换记录

日期：2026-09-13。用户确认“好的，就这一版，修改”，本轮将已确认的摘要综合稿写入论文。

## 实际基线与修改范围

- 开工检查并获取远程状态；本地 `main` 与 `origin/main` 同为 `9623b9fdb24621f15323df163b7108be8ed324e3`，远程为 `https://github.com/Hel10o/libai.git`。
- 开工时已跟踪文件无待提交变化；原有未跟踪 `main.pdf` 和仓库根 `template/` 保留，本轮不纳入提交。
- 仅替换 `main.tex` 摘要正文。摘要以外的主文件字节、各章节、表格、图及支撑材料不变；不改关键词、章节数量、全局格式或数值。
- 原稿与确认稿完整对照见 [abstract_comparison.md](abstract_comparison.md)。本轮以用户最后确认稿为准，早期“不改摘要”的阶段约束已由本轮明确批准替换。

## 实际文字修改

首段直接说明预测问题、题给输入、递进模型体系与输出，并保留有效湿度边界、不显式计入潜热及后段环境条件。第一问补明常热物性与含水率相关扩散率；第二问结论采用确认稿“温度场趋于均匀时，含水率仍存在明显的径向梯度”。第三问保留径向模型中先说明轴心最湿依据、再给出时间结论的顺序。第四问保留同物性条件对照及60.65%的模型临界时间缩短结果。

末段采用确认的综合分析，概括温湿响应不同步、径向模型干燥终点与收缩的几何尺度和干基边界标度共同作用。不追加独立的2.39天、2.13天换算句。不计关键词标签，原摘要22处加粗变为23处，新增仅为用户批准的“温湿响应不同步”，其余加粗依照确认稿保留。

## 实际核验

| 核验 | 实际结果与证据 |
| --- | --- |
| 确认稿与源文 | 已按确认文本转换为TeX；19/19项独立静态检查通过（含关键词标签共24处加粗），逐字与加粗核对见 [abstract_audit.md](abstract_audit.md) / [JSON](abstract_audit.json) |
| 数字与修改边界 | 摘要原始数字标记序列完全一致（含缩进参数及附件编号）；摘要之外主文件字节相同，正文、图表、计算程序与冻结数据无改动，见 [source_integrity.json](source_integrity.json) |
| 编译 | 摘要/正文预览和完整XeLaTeX编译均成功，见两份 `*_compile_console.txt` |
| 页数 | 完整PDF158页：摘要1页、正文30页、附录127页，与基线相同 |
| 版面参数 | 摘要11.5pt字号、18pt行距、5pt段间距及首行2em缩进不变，无排版参数调整 |
| PDF检查 | 实际运行完整验证器：37项通过、2项信息提示，六张题设表279/279显示单元格一致，见 [pdf_validation.json](pdf_validation.json) |
| 页间不变性 | 全158页以108dpi含页脚像素比较，仅第1页变化，其余157页完全一致，见 [page_integrity.json](page_integrity.json) |
| 目视检查 | Poppler渲染最终摘要页，实际查看标题、四问、综合段、关键词及页脚：摘要全部容纳，无裁切、重叠或缺字；其余页采用基线像素比对，未宣称逐页重新人工审阅 |
| 打包 | README与本轮证据随源稿更新；包内外清单、源文件哈希与ZIP校验的独立实测结果见打包后生成的 [source_package.json](source_package.json) |

实际执行：

```powershell
latexmk -xelatex -interaction=nonstopmode -halt-on-error -file-line-error -outdir=build/abstract_synthesis body_preview.tex
latexmk -xelatex -interaction=nonstopmode -halt-on-error -file-line-error -outdir=build/abstract_synthesis main.tex
python -X utf8 validate_paper.py --pdf build/abstract_synthesis/main.pdf --aux build/abstract_synthesis/main.aux --output evidence/abstract_synthesis_20260913/pdf_validation.json
pdftoppm -f 1 -l 1 -scale-to 1700 -png build/abstract_synthesis/main.pdf build/abstract_synthesis/final
```

正式PDF已由本次编译产物替换，SHA256：`5ee5ab3f0e5064c2cdf92eb0ec47840ea0da097f15cfaa613275c9864e5b54ce`。

## 未做与范围说明

未运行PDE、参数情景或作图，未变更正文论证与历史证据。自动检查和本轮摘要目视核验仅证明本轮文字替换、排版及数据保持情况，不作为数值模型物理有效性或最终参赛提交合规性的重新认证。源稿ZIP是可编辑论文包，不是正式竞赛支撑材料提交包。
