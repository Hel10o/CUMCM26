# 问题四流程图前导语补充记录

- 实际读取基线：`394039d11118f5ce8db38f4059d95e4d0ff162ef`；开始时工作区干净，本地 `main` 与抓取后的 `origin/main` 一致。
- 用户范围：仿照问题一、二、三，在问题四流程图之前补一段总述。
- 实际修改：仅在 `sections/q4.tex` 的章节标签之后、原流程图之前新增以下一段；原流程图、9.1 及以后全部原文和公式均保留。

> 问题四在第五章统一模型基础上，结合附件2的观测半径，在恒长度、均匀径向收缩假设下，用材料坐标与干物质守恒处理几何变化。采用附录4物性，从共同初态联立求解温湿场，沿用问题三的径向最大含水率判据确定所设环境条件下的模型干燥时长，并通过同物性恒半径对照分析收缩作用。求解流程见图9。

实际源稿中的“第五章”和“图9”分别通过 `\ref{sec:common-model}` 与 `\ref{fig:q4-workflow}` 生成，没有写死编号。

## 排版处理

仅加入导语的两次预览试编译（较长初稿和精简稿）均造成正文31页。因此在问题四内部新增局部分组，公式前后留白设为8 pt，短公式前后留白设为6 pt，伸缩量沿用其他问题章节的局部样式。字号、正文行距、页边距、图形尺寸、图注与全局样式不变，分组在本章结束时恢复。最终导语为一个自然段。

首次预览启动因工作目录导致日志输出路径不存在，未执行编译；修正为论文目录后，预览与完整 XeLaTeX/latexmk 编译均成功。本轮未运行任何 PDE 求解、参数情景或新的模型验证。

## 实际核验

1. 删除本轮新增段落及局部排版命令后，问题四源文件与基线逐字节相同；原220个数字项及全部保护性表述不变。
2. 打包前核对另外796个既有论文及支撑包文件，字节均未变；本轮随后有意更新正式PDF、README、源稿ZIP及清单。
3. 论文专项自动检查25项通过、1项信息提示；本次没有调用独立AI说明文件的检查流程，不能记为37项完整检查。
4. 六张题设结果表全部通过，逐格比对279项一致。
5. 正式PDF共158页：摘要1页、正文30页（含参考文献及AI声明）、附录127页。
6. 最终编译日志未发现未定义引用、重复定义标签、Overfull或缺字警告；新增两处交叉引用解析为第五章和图9。
7. 以72 dpi 对正式PDF全部158页与基线逐页像素比对，仅物理第20—24页变化；摘要及其余页面相同。
8. 完整PDF前31页与最终正文预览逐页像素一致；人工查看预览第20—25页及第31页，新增段落、流程图、公式和跨页接续无重叠或裁切。
9. 支撑材料ZIP不包含本次改动的论文TeX或论文PDF，其成员未受影响，保留原包；本轮更新可编辑LaTeX源稿ZIP并核验CRC、成员哈希及本轮提交源文件一致性，包后核对记录见本目录 `source_package.json`。

数据证据：`source_integrity.json`、`paper_checks.json`、`render_checks.json`。源稿ZIP核验是文件一致性检查，不代表重新求解或物理验证。

## 并行工作隔离

完成本轮核验后，发现工作区 `sections/validation_limits.tex` 另有删除“改进方向与数据需求”小节的未提交变化，该变化不属于本轮导语修改。本轮保留其他任务的工作区变更，不暂存、不覆盖；同时发现共享PDF及通用检查文件也被并行任务更新，故PDF按本轮已核验构建产物暂存，源稿包全部成员从本轮暂存版本构造隔离快照，其中限制章节采用读取基线 `394039d11118f5ce8db38f4059d95e4d0ff162ef` 的字节，与本轮已编译PDF一致。第一次直接从工作区打出的源稿包包含该未提交变化，未交付，已由隔离快照包替换。

为避免并行任务继续更新通用PDF影响本轮审核，另保留已核验的同字节交付副本 `四问论文_问题四导语修订.pdf`；本轮Git提交中的通用PDF与该副本一致。
