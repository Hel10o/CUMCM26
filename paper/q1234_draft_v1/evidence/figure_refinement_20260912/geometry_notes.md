# Q3/Q4 现有定量图细修记录

基线提交：`e1ae2bc23917cbbbd357cb14ca795896f4a39f25`。日期：2026-09-12。

本轮新增 `visual_refinement/geometry.py` 与两组 `figures/visual_refinement/q3_q4_drying`、`q4_shrinkage`（PDF、SVG、600 dpi PNG）。旧 `visual_revision` 脚本及其图形均不覆盖。源脚本从上一版几何绘图脚本复制后最小修改，只读取冻结轨迹、事件记录和半径观测；没有导入求解器、执行 PDE、继续时间积分、重新找根或运行恒半径情景。

## 实际吸取的方案

### Q3/Q4 事件组合图

外框保持 16.4 × 8.0 cm，不拆图。顶部新增“物性不同，非收缩效应对照”，图例移至其下；保留两个问题标题的附录物性与几何区别。原轴心、当前表面、阈值、执行点、两条离散根—执行时间标尺及执行最大值全部保留。

主过程数据继续来自保存数组；Q3 使用每 60 s 的保存值加精确执行输出，Q4 使用全部已存输出。下方标尺只画原 JSON 的根与执行点，分别显示 `+0.3701 s`、`+0.3483 s`，不画未经保存的秒级局部含水率曲线。

本次增加比较边界标注，没有改变执行标签 `57.4741 h`、`51.0921 h`、两份执行最大值或一维条件结论的强度。图注可继续沿用原一维有效模型与两问不同物性的说明；无需为新图修改任何事件数值。

### Q4 收缩材料域与受控对照

外框保持 16.4 × 9.2 cm，仍为三面板：

1. 上方面板将完整观测半径历程并入原材料域色图。横轴为 0–72 h，全部 145 个观测点用小空心圆表示，相邻观测点以分段线性输入连线；含水率色场只使用已保存至执行时刻 `51.0921 h` 的材料场。该时刻设竖虚线并明确标为“执行”。其后区域仅画观测半径，文字写“仅观测半径”；没有继续生成含水率场，没有域外或执行后填零。原对数色标与数值范围保持不变。
2. 左下面板继续保留 6 h、12 h、24 h、51.0921 h 的四条当前径向含水率剖面，末端为当时材料表面的实心点。原阈值线旁补 `0.15` 直接标注，坐标仍为当前物理半径，未改为材料坐标或外推到固定半径。
3. 右下面板以共同零起点的两条时间条代替原重复的半径输入小图；完整半径输入已移入上方面板。新增图中信息全部是现有正文已经报告的同物性条件对照。标题明确附录 4、同物性、同环境，横轴明确模型临界时间；图下限定差异同时包含扩散尺度与干基边界交换标度变化。

## 临界时间与执行时间的精确口径

恒半径临界根取 `q4_complete_delivery/v1/validation/fixed80.json` 的 `event.critical_s`，规定收缩临界根取 `q4_complete_delivery/v1/output/end_event.json` 的 `critical_s`，两者均仅除以 3600 换算为小时，并通过严格相等断言与各自保存的 `critical_h` 核对一致：

| 项目 | 冻结值或对冻结值直接相减/相除 | 图内显示 |
| --- | --- | --- |
| 同物性恒半径临界根 | 129.84836083945225 h | 129.8484 h |
| 规定收缩临界根 | 51.09200325430254 h | 51.0920 h |
| 临界时间差 | 78.75635758514971 h | 78.7564 h |
| 模型临界时间缩短 | 60.65256201618597% | 60.65% |
| 规定收缩正式执行 | 51.0921 h | 上图及剖面保持 51.0921 h，明确为执行 |

差值为两份原临界根之差，百分比以恒半径临界时间为分母。没有将执行时刻替换为临界根，也没有用问题三作恒半径基准。输入 `main.json` 与 `fixed80.json` 记录的模型源 SHA 和后段环境一致；前者为正式 120 阶、后者为既有 80 阶对照。本轮复用原正文已采用的两个根，不宣称同网格配对或新增收敛验证。完整未舍入值、来源路径和哈希均见 `geometry_sources.json`。

## 图注及邻接读图段建议

Q3/Q4 组合图保留原图注即可，顶部新增限制与原图注“物性不同，差异不能全部归因于收缩”一致。

Q4 图注需说明原半径小图已并入上图，右下现在展示同物性临界时间对照。建议内容：

> 规定收缩下的径向有效模型场、观测输入与同物性条件对照。(a) 当前实际半径坐标中的含水率色图仅显示至正式执行时刻，采用对数色标；全部观测点及分段线性半径输入保留至72 h，执行线之后只展示半径。(b) 各时刻含水率剖面止于当时材料表面。(c) 附录4同物性、同环境下的恒半径与规定收缩临界根对照。临界根与执行时刻分别标示；时间差同时包含扩散尺度和干基边界交换标度变化。

原图后读图段中“右下小图保留完整半径观测输入”须改为“上图保留完整半径观测输入，右下对照原恒半径与规定收缩的临界时间”；紧接的原定量段及全部数字不必变动。本子任务不编辑 TeX。

## 实际检查与限制

- 已运行新绘图脚本，读取 7 份冻结输入并生成 6 个输出，运行末核对输入哈希均未改变；原事件执行最大值、根—执行时间差、四个对照显示值和半径插值一致性都有脚本断言。
- 已通过 `view_image` 查看新 Q3/Q4 PNG。首版发现 Q4 右下横轴标题与差值文字重叠、上图执行竖线穿过图例，随后调整右下轴的高度及图例位置并重新查看；修订后未见这些遮挡。Q3 顶部限制、图例和面板标题清楚分离。
- 600 dpi PNG 用于输出；工具显示时将大图缩至适合查看的宽度。本子任务未以此代替最终整页 PDF 的字级检查，也未实际灰度打印。
- 主论文总页数、摘要/正文/附录分别页数以及最终图注、交叉引用和插入后的裁切，由主代理重新编译核验；这里不提前记为通过。
- 所有图形均为冻结结果的确定性绘制；没有新增有限圆柱连续严格界、精细二维终点、实际工况保证或实验验证。

实际输出尺寸补记：PyMuPDF 读取两份新 PDF 均为单页；Q3/Q4 为约 16.4000003 × 8.0000000 cm，Q4 为约 16.4000003 × 9.2000005 cm（浮点换算误差）。PNG 分别 3874 × 1889、3874 × 2173 像素，DPI 元数据约 599.9988。两份 PDF 字体均为 TrueType，组合图新增限制用微软雅黑粗体。最终 7 个输入、6 个输出及脚本的 SHA 与来源记录一致；这属于图形文件核查，不是模型数值验证。


## 最终整页目视后的局部修正

300 dpi的第23页裁切发现，左下剖面的6h曲线与12h图例样线靠近。主代理将这一面板纵轴上端由1.85改为2.2，仅增加上方留白；下端、原0/0.5/1/1.5刻度、全部曲线与16.4×9.2 cm外框保持。随后再次实际运行geometry.py，7个输入哈希保持、6个输出哈希与来源JSON一致，重新打开新PNG确认图例和曲线分离。最终论文编译页另作复核。
