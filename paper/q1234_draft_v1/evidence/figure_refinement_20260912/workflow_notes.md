# 总流程图的来源、语义与图形检查

本文件只记录本轮新增的模型总流程图。科学叙述核对基线为 `e1ae2bc23917cbbbd357cb14ca795896f4a39f25`。图的性质是四问的模型、输入与任务输出关系示意，并非历史工作簿的文件构建依赖图。

## 文件与复现

- 绘图脚本：`paper/q1234_draft_v1/visual_refinement/workflow.py`。
- 共享绘图设置：`paper/q1234_draft_v1/visual_refinement/common.py`，本子任务只读。
- 正式图：`paper/q1234_draft_v1/figures/visual_refinement/model_workflow.pdf`、`.svg`、`.png`。
- 来源和逐箭头记录：同目录证据文件 `workflow_sources.json`。其中保存五份实际读取的代码文件 SHA256、六份基线正文的 Git 提交及文本 SHA256、对应行号摘录、脚本及共享设置哈希和三种输出哈希。
- 从项目 Git 检出目录运行：`python -X utf8 paper/q1234_draft_v1/visual_refinement/workflow.py`。脚本仅读取源码文本和指定提交的论文文本，不导入求解器，不读取或修改数值数组，不执行 PDE、参数情景或工作簿导出。脚本使用基线正文，因此后续正文中插入图及图注不改变本图的科学审阅基准。

图宽 15 cm、高 6.4 cm，复用 Microsoft YaHei 字体、现有青色/灰色/橙色设置；最小字号 7.4 pt。PDF 与 SVG 为矢量图，PNG 按 600 dpi 导出。

## 逐箭头及关系自查

| 编号 | 图中关系 | 核对依据及边界 |
|---|---|---|
| A1 | 共同输入到 Q1 | `q1_solver.py` 第 21–35、82、167–198 行，及基线 `problem_model.tex` 第 2、4、102 行附近：Q1 从 28 °C、2.55 kg/kg 独立起算，仅取 1800 s 内的环境记录。上方橙色文字明确后段假设只适用于 Q2/Q3/Q4。 |
| A2 | 共同输入到 Q2/Q3 | `solve_unified_q23.py` 第 1–6、46–60 行：附录3温湿联立从共同初态开始，前 4 h 对观测作线性插值，后续采用末小时均值平台假设。 |
| A3 | 共同输入到 Q4 | `run_q4.py` 第 1–5、20–26 行，及基线 `q4.tex` 第 6–14 行：附录4、附件2规定半径、共同初态独立起算。图中没有 Q3 到 Q4 的箭头。 |
| A4 | Q1 模型到 Q1 输出 | `q1_solver.py` 第 140、167–198、240 行：仅热物性为给定常值，扩散率通过 `D0*exp(-a/u)` 随含水率变化；输出时域到 1800 s。图中文字为“常热物性”，没有将 Q1 概括为所有物性恒定。 |
| A5 | Q2/Q3 共享轨迹分支到 Q2 输出 | `solve_unified_q23.py` 第 46–81 行，`package_workbooks.py` 第 234–240、288 行，基线 `q2.tex` 第 112–115、189–197 行：题目规定前 3 h 表格和全过程工作簿分别取相应范围的温湿联合轨迹。 |
| A6 | Q2/Q3 共享轨迹分支到 Q3 事件及输出 | `solve_unified_q23.py` 第 58–60、72–81 行，`audit_unified_q23.py` 第 59–68 行，基线 `q3.tex` 第 8–9、47、95 行附近：空间极值、等号根、留余量的执行点分开，不能由四位表值判定严格低于阈值。图并未画 Q2 舍入表格到 Q3 的箭头。 |
| A7 | Q4 模型到 Q4 事件及输出 | `run_q4.py` 第 29–54、92–94 行，`package_workbooks.py` 第 218–230 行：在一维材料域主轨迹上重构空间极值并处理事件。没有把节点证据或一维根提升为连续二维保证。 |
| D1 | Q4 与同物性恒半径对照 | `run_q4.py` 第 100–102 行提供半径类型入口，基线 `q4.tex` 第 256 行起明确固定附录4及其余条件的对照。橙色虚线直接从 Q4 模型框关联对照框，无箭头头部；不来自 Q4 执行末态，也不来自 Q3。 |
| D2–D4 | 三路模型与并行校核 | 基线 `problem_model.tex` 第 4、21 行及 `validation_limits.tex` 的既有校核段落。灰色虚线无箭头头部，只编码校核关系；二维中截面、全域节点和独立数值离散用于可信度与适用范围。正式工作簿的来源仍为主轨迹，图中没有校核框流入工作簿框的箭头。 |

历史的 `result3.xlsx` 导出与后来的统一 Q2/Q3 轨迹不是同一个文件构建脚本；本图表示现稿采用的模型及任务输出关系，没有宣称本轮重新生成过这些工作簿，亦没有将历史打包顺序画成状态依赖。`audit_unified_q23.py` 在本轮只读，未重新执行其比较程序；图不报告任何新增的数值一致性结论。

“空间极值→等号根→加余量→执行点”均位于一维主模型分支。二维检查被放置在独立虚线关系框中，不表示所有连续位置已充分验证，也不把既有同网格配对方向写成一般定理。图没有新增参数、结果数值或实际工况保证。

## 实际图形检查

1. 实际运行过上述绘图脚本，生成 PDF、SVG、600 dpi PNG；五份代码输入在运行前后 SHA256 相同。第一次生成后通过 `tools.view_image` 看 PNG，发现小型对照框与输出框的行距可改善；调整了框高及正文行距，没有修改科学内容。
2. 实际通过 `tools.view_image` 查看最终 600 dpi PNG。随后运行 `pdftoppm -singlefile -r 180 -png` 渲染正式图 PDF，并通过 `tools.view_image` 查看渲染图。最终图的全部文字、框线、分支和箭头均进入画布；未见乱码、重叠或裁切。三路模型、Q2/Q3 分叉以及 Q4 的虚线条件对照可清楚区分，底部校核框与主输出之间没有误接箭头。
3. 实际运行 `pdfinfo`，图 PDF 为 1 页，页面为 `425.197 × 181.417 pt`，与 15 × 6.4 cm 一致。脚本另检查了所有文本的外接框位于画布内部；这项程序检查只检查画布边界，不替代图形目视。
4. 本子任务只审阅图本身，论文整页的浮动体位置、最终页数和上下文排版由主任务编译后另行检查。此处没有声称论文整页已通过审阅，更没有将图形检查称为模型验证。

实际看图对应哈希如下：

| 文件 | SHA256 |
|---|---|
| 正式 `model_workflow.pdf` | `dce22435dd7447f8ed6bfcf08d6799f8fda227374abcceb0d1d54ccb2cc2d100` |
| 正式 `model_workflow.png` | `0d97ff6775f26fcc0776a00ca61af2a149e0b07e6eb0a3761b0324005276fee3` |
| 正式 `model_workflow.svg` | `02c8d6f14e97d6501cf24fadbfabf024ae677d9fa09233964d8cc66caab6e099` |
| `build/figure_refinement/workflow_review/model_workflow.png`，上述 PDF 的 180 dpi 渲染 | `012db7213a8796f0d89f867a824ec02028ccc72a0a0a02a17f953469b5d830f1` |

## 建议图注与正文连接

建议图注：

> 四问模型、任务输出与并行校核的关系。实线箭头表示主流程，虚线表示校核或条件对照关系；图中表1–6均指题目规定表。问题二和三共享从共同初态出发的未舍入轨迹，问题四使用附录4及规定半径独立起算。4 h 后的环境平台为延拓假设；二维校核用于说明适用范围，不生成正式工作簿。

输入框本身采用实线边框，观测与假设由分行文字及橙色“长时延拓假设”区分，因此图注不应声称该环境假设采用虚框。若正文已有四问关系段落，可仅在段末加入图引用；无需复制图内各节点，也无需新增科学断言。本子任务未修改任何 TeX、旧图、历史证据或正式数值文件。
