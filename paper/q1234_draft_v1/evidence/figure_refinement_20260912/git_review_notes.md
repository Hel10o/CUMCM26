# 暂存内容复核

本轮仅暂存根README和paper/q1234_draft_v1内交付，原template目录不纳入。所有受保护旧证据和正式数值由独立检查哈希验证。

实际运行非生成文本的git diff --cached --check，退出0。五份新SVG是Matplotlib原生导出，路径数据行保留生成器尾空格；完整check对这些SVG给出空白提示，因此没有声称全文件空白检查为零。为保持已核对的图形/来源哈希，未为消除此类不影响SVG语义的生成格式而改写图形；检查非生成文本时按精确图形目录排除SVG，未放开源码、TeX或说明文档。
