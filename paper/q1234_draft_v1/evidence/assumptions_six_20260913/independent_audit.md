# 六条批准假设：独立静态审查

基线提交：`d5787b1c21856049f6e234588cad389587b7f1eb`。受检 `sections/problem_model.tex` SHA256：`0052a0fbde54b891959d18febca03dceaca3861154f7635fd8583f1e8177b253`。

批准依据为父代理转发的六条完整 Markdown，用户随后确认“好的，就按你修改的来”；配套说明允许将具体约定移至方程或输入处，并在第六条后保留61点说明及原平台等式。仅规范化排版空白、单位宏及 `3--4` 与 `3—4` 的等价写法后，六条文字和加粗位置均逐字一致。未自行增改批准文字。

实际完成 **30/30 项静态核对**，详细检查与哈希见配套 JSON。

- 六条所在源码行：32, 34, 35, 36, 38, 39。每条一个批准的加粗块。
- `eq:future` 原公式、原标签及环境数值字节不变，仍在第六条内部，标签位于第 43 行；61个采样点说明保留。
- `ass:midsection` 仍唯一绑定第一条，问题分析末段改为回指空间近似，统一方程另回指第一条；源码层面全部 ref/eqref 目标均唯一存在。未冒充 AUX 编号核验。
- 8 个显示方程块按顺序与基线原字节一致；符号表、分问物性表及后续方法/事件/输出原文保持。
- 主文件、其余 7 份章节、75 份图资源和 6 份表资源逐成员 Git blob 均与基线一致。摘要、结论、11.3/11.4 及反例数值未动。

旧细节迁移：

- 题表空间解释与径向变量：当前 `problem_model.tex:85`。
- 内部只用相对材料扩散：当前 `problem_model.tex:99`。
- 空气量恒等映射：当前 `problem_model.tex:129`。
- hT单系数及原给定交换参数：当前 `problem_model.tex:129`，相关数值/定义位于第 134 行。
- 环境和半径分段线性输入：当前 `problem_model.tex:130`。

初始干密度均匀已在第一条明确，并在第 116 行作为约去干密度的前提，与既有 Q4 材料运动推导一致。原相变、有效容量与独立干质量范围由批准第四条、统一方程及未改11.3/11.4共同保持。

去掉 Needspace 宏后，整份源码阿拉伯数字词元为 124 → 122，新增 {"4": 1}，删除 {"2": 2, "0": 1}；这是文中约定/引用重复次数的统计，不能与科学结果变更混同。新增的 4 来自统一输入段重复说明 4 h；减少的两个 2 来自原假设的附录2/附件2重复编号，0 来自原假设中 L_0 下标。原显示方程与结果表字节核对另行通过。

最终局部排版仅另在5.1前加入 Needspace{8baselineskip}；题表解释位于第五章引言，恒等映射/换热系数解释/插值输入位于5.1末尾。上述细节均处于统一模型章节，未丢失。

本审核只写本 Markdown 与 JSON。未编译、核 PDF 页数/视觉/AUX、重跑求解或情景、检查 ZIP 或同步；这些事项不记为已通过。本记录仅绑定上列源码快照，后续局部排版若改变源码须刷新哈希。
