# 模型假设六条版修订记录

日期：2026-09-13。用户确认“好的，就按你修改的来”，采用上一回复展示的六条假设及配套迁移说明。

## 实际读取与边界

开工读取 `AGENTS.md`、论文主文件、现行八条假设与统一模型；获取远程后，本地 `main` 与 `origin/main` 同为 `d5787b1c21856049f6e234588cad389587b7f1eb`。原跟踪树无待提交变化。已有未跟踪 `paper/q1234_draft_v1/main.pdf` 与根 `template/` 保留、不纳入本轮提交。

正文源文件只改 `sections/problem_model.tex`，不改变数值结果，不运行PDE、参数情景或作图。摘要、结论、其余章节、原图表及历史审核证据不改。一级章节仍为12个。

## 实际修改及原约定落位

原八条改为用户批准的六条，每条保留批准的加粗位置；第一条明确初始干密度空间均匀，第四条采用“不显式计入”表述。新旧全文见 [assumptions_comparison.md](assumptions_comparison.md)。

| 原内容 | 本轮对应位置 |
| --- | --- |
| 轴对称、初始均匀、中截面径向近似 | 新假设1；补明初始干密度均匀，统一方程约去干密度前同步援引此条件 |
| 题表“到中心距离”的解释、径向状态变量 | 第五章引言；问题分析末段保留对假设1空间近似的回指 |
| 线性Robin、恒定交换系数 | 新假设3；题给系数数值仍在5.2 |
| hT单系数、不另分对流与辐射 | 5.1末的边界说明 |
| 空气量有效化及恒等映射g | 新假设2及5.1末，显式映射原式保持 |
| 内部仅扩散、不计压力驱动或相对干骨架对流 | 5.1通量定义前 |
| 干质量守恒、经验密度仅用于有效热容量及忽略项 | 新假设4、统一方程与原11.3/11.4，未新增潜热修正方向 |
| 前三问固定尺寸、第四问恒长均匀径向收缩 | 新假设5，第四问原材料映射与观测来源仍完整保留 |
| 环境后段平台、3—4h的61点算术均值 | 新假设6及其后原平台公式；`eq:future`仍在假设章、仍为式(1) |
| 环境和半径的分段线性插值 | 5.1末的输入说明 |

## 九项实际核验

| 项目 | 实际结果 |
| --- | --- |
| 1. 用户批准文字与加粗 | 六条逐字及加粗核对通过；仅单位宏、空白与区间破折号作TeX等价转换，独立静态审核30/30项通过 |
| 2. 修改范围 | 除上述源稿及配套记录/交付包外，665份受保护文件逐字节与基线一致；摘要、结论、限制章整份源文件不变 |
| 3. 数值与公式 | 原8个显示方程及符号表原字节不变，六张规定结果表279/279显示单元格与冻结清单一致 |
| 4. 平台公式与交叉引用 | `eq:future`唯一且原字节保留在第六条后，编译AUX为式(1)、物理页3；`ass:midsection`唯一且编号1，两处回指有效；最终日志无undefined或multiply defined |
| 5. 原假设必要约定 | 上表全部落位可查；11.3量化反例及11.4原说明保持，未靠删免责提高主张强度 |
| 6. 编译与页数 | 完整XeLaTeX编译通过；PDF158页=摘要1页+正文30页+附录127页，仍为12个正文一级章节 |
| 7. PDF自动检查 | 实际运行完整验证器，37项通过、2项信息提示；最终日志无Overfull或缺字提示 |
| 8. 版面与不变性 | 物理页3—6经Poppler渲染逐页目视通过；全158页按108dpi含页脚比较，仅3、4、5、6页变化，其余154页完全一致 |
| 9. 打包 | README及本轮证据与源稿包同步；包内外清单、源文件字节和哈希实测见打包后生成的独立 [source_package.json](source_package.json) |

第一次预览为32页（摘要1+正文31）；为避免迁移说明挤动后续章节，将输入及边界解释集中至5.1末，题表解释置于第五章引言，并在5.1前增加局部 `\Needspace{8\baselineskip}`。第二次预览恢复31页；其后完整最终编译为158页。全局字号、行距、段距及其他章节格式未改。各阶段编译输出分别保存，初次预览不记作页数通过。

源码阿拉伯数字词元124→122，少的是重复附录/附件编号和`L_0`下标等约定标记，不是结果数字；具体差异见独立审核。数值不变的判断以原方程、结果表、冻结文件字节及PDF表格核对为依据。

## 命令、证据与产物

```powershell
latexmk -xelatex -interaction=nonstopmode -halt-on-error -file-line-error -outdir=build/assumptions_six body_preview.tex
latexmk -xelatex -interaction=nonstopmode -halt-on-error -file-line-error -outdir=build/assumptions_six main.tex
python -X utf8 validate_paper.py --pdf build/assumptions_six/main.pdf --aux build/assumptions_six/main.aux --output evidence/assumptions_six_20260913/pdf_validation.json
pdftoppm -f 3 -l 6 -scale-to 1700 -png build/assumptions_six/main.pdf build/assumptions_six/final
```

- [独立静态审核](independent_audit.md) / [JSON](independent_audit.json)
- [源文件与公式不变性](source_integrity.json)
- [PDF自动检查](pdf_validation.json)
- [页数、逐页像素与目视核验](page_integrity.json)
- 最终 `problem_model.tex` SHA256：`0052a0fbde54b891959d18febca03dceaca3861154f7635fd8583f1e8177b253`。
- 最终PDF SHA256：`36e04d6c2e94e3ef6063cef4f0d409b8531186df667f5b76cd3015117073664a`。

本轮未重做数值求解、参数情景、物理标定或提交合规认证。既有模型限制仍有效。可编辑源稿ZIP不作为正式竞赛支撑材料提交包。
