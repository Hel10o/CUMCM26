# 第三问摘要新旧对照

用户确认仅展示一维结果，以“最终干燥时长由轴心含水率决定。”收尾。模型条件与关键结果保留；二维证据仅移出摘要，正文不变。

## 本轮开始时

```tex
\textbf{针对问题三}，在第二问一维径向有效模型的全程轨迹上，以全径向最大含水率严格低于0.15\unit{kg/kg}为停止条件，建立干燥终点判定模型。通过\textbf{重构极值搜索}定位等号临界时刻，结合网格加密和独立时间积分检验设置数值裕量，区分临界根与执行时刻。在4\unit{h}后环境保持末小时均值的假设下，所需干燥时间为\textbf{57.4741\unit{h}}。\textbf{最湿轴心控制终止，表面先达标不足以判断整根干燥完成。}正式时刻的二维离散节点场最大含水率为0.149998606290\unit{kg/kg}，全部节点低于阈值；80$\times$256同网格配对中，端部交换使二维根比一维提前约7.4740\unit{s}，支持所取时长的工程保守方向。
```

## 本轮修改后

```tex
\textbf{针对问题三}，在第二问一维径向有效模型的基础上，以全径向最大含水率严格低于0.15\unit{kg/kg}为停止条件，建立干燥终点判定模型。采用\textbf{重构极值搜索}定位等号临界时刻，结合网格加密和独立时间积分检验设置数值裕量，确定执行时刻。在4\unit{h}后环境保持末小时均值的假设下，计算得到所需模型干燥时长为\textbf{57.4741\unit{h}}。\textbf{最终干燥时长由轴心含水率决定。}
```
