# 图5时空双热图替换记录

## 实际读取

- 本轮开始时工作区干净，本地 main 与实际获取的 origin/main 同为 `1b84c827ae72353b37549715ff4da5513cc61ea1`。
- 读取当前 `AGENTS.md`、论文问题二源码、图表来源清单、原图及已保存结果数组。对照用户《第7章 .docx》及《第七章图片.zip》中的图7-2；本轮核对文件哈希与已提取图片，确认参考图与 ZIP 内 `第七章图片/7-2.PNG` 字节一致。输入路径及哈希见 [baseline_inputs.json](baseline_inputs.json)。本轮没有重新渲染 Word 页面，也没有修改 Word 文件。
- 原正式 PDF 的 SHA256 为 `e88edc0185f152e7c18747ba2efd1adb7831289f1b01d07db2f033f3c5ae85bc`，原版可由上述 Git 提交恢复。

## 实际修改

按用户确认，将原图5“温湿状态路径＋径向含水率剖面”一对一替换为“径向温度场＋径向含水率场”时空双热图。采用图7-2的表达结构，并补充独立色标、少量带数值的等值线、轴心/表面标记及3 h末时差值。中文宋体，英文和数字 Times New Roman；标题居中，图形原始尺寸仍为16.4×8.1 cm，正文插入宽度仍为0.94倍版心宽度。

问题二源码只改三处：图文件路径、图注和一个读图段。保留 `fig:q2-profiles` 标签；7.1—7.3、7.5机制分析、7.6、所有表格和科学数值均不改；未改任何排版指令。逐段新旧对照见 [chapter_comparison.md](chapter_comparison.md)。新读图段为：

> 图中左图显示温度总体随时间升高，并逐渐趋于径向均匀；右图显示含水率先在表层下降，低含水区域随后向内部扩展，径向差异仍较明显。

同步更新图表来源清单、当前图形入口、论文与根 README；新增独立绘图脚本及数据来源记录，刷新可编辑源稿 ZIP 和提交支撑包。原图及历史审核文件保留，不覆盖原模型结果。

## 数据与显示方法

唯一数值来源为 `q4_complete_delivery/v1/q123_closeout/output/q23_unified.npz`，SHA256为 `80bd0a8ee77ac04189646a89e2dd78c2843344d9538923e402bef993b980c6f4`。使用 `time_s`、`radius_cm`、`profile_TC` 的前3 h全部10801个逐秒时刻、21个径向节点。仅为显示，在每个已保存时刻沿径向线性插值为101个显示节点，原21节点是显示网格的精确子集，节点值差为0；未作时间插值或平滑。

等值线在原始采样网格内插值得到，只辅助辨识时空分布，不构成连续域阈值的严格边界。完整数组签名、字体与边界检查见 [figure_provenance.json](figure_provenance.json) 和 [新图说明](../../figures/q2_heatmap/README.md)。

| 3 h量 | 轴心 | 表面 | 径向差值 |
|---|---:|---:|---:|
| 温度 / °C | 49.8495 | 49.9664 | 0.1169 K |
| 干基含水率 / (kg/kg) | 1.7662 | 1.0081 | 0.7581 kg/kg |

以上标注由保存数组直接计算并按原稿精度显示，与表3、表4及正文一致，未产生新模型结果。

## 实际核验

| 核验项目 | 实际结果与证据 |
|---|---|
| 修改范围 | 问题二除图路径、图注、读图段外逐字节一致；7.1—7.3共5982字节保持。见 `source_integrity.json`。 |
| 数值与原稿 | 排除图文件路径及插图选项后的228个数字标记序列保持；受保护的108个原科学源码、表格与图件字节保持。 |
| 数据与图形 | 输入数组及原图哈希保持；原节点值完全保持；末时四端点及两差值与原结果一致。 |
| 字体与排版 | 图内宋体、Times New Roman字体嵌入；文本在画布内；六个等值线标记均在轴内且互不重叠；标题居中。正文缩放后最小标注约8.77 pt。 |
| 编译与引用 | 实际运行 XeLaTeX/latexmk，预览与完整稿编译成功；日志未见未定义引用、重复标签、缺字或Overfull问题。所有AUX标签名称、编号与页码保持。 |
| 页数 | 最终PDF158页：摘要1页、正文30页、附录127页，与基线相同。正文仍12个一级章节、12图，44个显示方程环境、43个编号方程标签保持。 |
| 全页像素比较 | 72 dpi逐页比较仅物理第13、14页改变，其余156页相同；摘要及127页附录全部保持。见 `page_integrity.json`。 |
| 论文专项与表格 | 论文专项25项通过、1项信息、0失败；六张规定表共279格逐格匹配。见 `paper_validation.json`。 |
| 目视与独立复核 | 主任务实际查看新图及最终第13、14页；独立审核者实际查看新图及最终第13—15页，未发现裁切、重叠或图文矛盾，并独立打开正式PDF确认158页与哈希。独立审核者未重跑279格或全页像素检查。 |

正式PDF SHA256：`f8801469d3ee7f9a2b75633a54473138f2cc131e32fc68b023626710bf332e96`，与编译产物完全一致。新图PDF SHA256：`47fd500235089fddc27d421ead469e8ca59c268ad14d499f8b6dde9a051b4a0e`。

支撑包实际完成94成员CRC、成员集合及字节读回；12张正文图与现稿一致，其余11图和5个数值文件与前一版字节保持。支撑包SHA256为 `b127ea501b815e0032e396e6757b69668e27e98b0e7106b862c43f3aacd8a5bc`，详见 [支撑包刷新记录](../../../../q1234_submission_support_20260913/evidence/q2_heatmap_refresh_20260913/刷新记录.md)。可编辑源稿包的哈希与最终成员读回结果单独记录在本目录 `source_package.json`，避免包清单自引用。

## 未做或受限

- 未运行PDE、参数情景、独立数值积分、结果工作簿导出或物理实验核验；本轮是图形表达和论文编辑，不构成新的模型验证。
- 本轮执行论文专项检查，没有执行包含独立AI使用说明PDF的整套联合检查，也未处理该文件此前已记录的边距问题。`paper_validation.json` 的完整提交验收状态仍为false。
- 目视检查集中在新图及受影响页面；其他页面以像素比较核对。没有将全158页逐页目视写为已完成。
- 既有竞赛格式和包体积检查沿用项目验收工具，本轮未重新检索竞赛规则或声称已完成最终参赛提交验收。
