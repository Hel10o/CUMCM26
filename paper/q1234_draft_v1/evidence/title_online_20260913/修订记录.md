# 论文标题排到一行记录

日期：2026-09-13。用户要求把论文标题排到一行。

## 实际修改

- `main.tex` 摘要页标题去掉原第二行前的强制换行：`{\LARGE\bfseries 考虑变物性与径向收缩的\par 圆柱药材干燥模型\par}` 改为 `{\LARGE\bfseries 考虑变物性与径向收缩的圆柱药材干燥模型\par}`。
- 标题现为一行 22 pt 粗体居中，实测行宽 418 pt，版心宽 453.5 pt，左右各余约 18 pt，编译日志无该行的 Overfull \hbox。标题下仍居中排“摘　要”，其后摘要段落与关键词不变。
- 未改动摘要文字、正文各节、数值、图表、文献表与 PDF 文档属性（`pdftitle` 原即为完整标题）。

## 实际核验

- 完整 XeLaTeX 编译成功；PDF 157 页（摘要 1、正文 29、附录 127 页），与改动前一致（本轮改动前的基线即并发轮次产出的 157 页版本）。
- `validate_paper.py`：37 项通过、2 项信息提示；六张题设表 279/279 显示单元格一致；摘要仍单独占第 1 页。
- 页间像素比对（108 dpi 含页脚）：全部 157 页中仅第 1 页变化，其余 156 页与改动前逐像素一致。
- 目视检查：渲染第 1 页并实际查看，标题单行居中、未越出版心，与“摘　要”标题间距正常，摘要段落与关键词排版未受影响，见 `abstract_page.png`。

## 与并发轮次的关系

本轮工作期间，另一并发会话正在做“第四问 9.4 全域数值检验精简”一轮（正文由 30 页变为 29 页、摘要中 51.0921 h 表述细化），其改动尚未提交。本轮的页间比对基线即含该轮成果的编译产物，故比对结果只反映标题一行化这一步；本轮提交只包含标题改动，不包含该并发轮次的内容。

## 未做或受限

未重跑求解、未改数值与图形、未重做正文页的目视复核；标题字体与字号沿用原设置，未因单行化而缩小，如后续标题字数增加需重新检查是否越出版心。
