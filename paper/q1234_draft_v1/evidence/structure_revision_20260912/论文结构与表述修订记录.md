# 四问论文结构与表述修订记录

日期：2026-09-12。本轮仅作论文编辑、编译、静态一致性及页面检查，没有导入或执行求解器，没有生成新的数值结果。

## 实际读取

先 `git fetch origin main`，确认本地 HEAD 与远程 main 同为 **`b51ca5671e693c01bb04db7d312ce548cf621d89`**，随后用 GitHub 连接在该不可变 SHA 读取根 `README.md` 和 `q1234_paper_structure_handoff/论文结构与表述修订_模型指示文档.md` 全文。两份文件的 Git blob SHA 与固定提交 URL 保存在 [github_reads.json](github_reads.json)，不是仅凭旧摘要或本地记忆工作。

同一干净已跟踪基线中读取：论文 `README.md`、`main.tex`，`sections/` 中的 `problem_model.tex`、`q1.tex`、`q2.tex`、`q3.tex`、`q4.tex`、`validation_limits.tex`、`references.tex`、`appendix.tex`；以及整体复审的《问题清单与改进优先级》《最小补算计划》《物理闭合独立裁定》。另检查 `AGENTS.md`、`.gitattributes`、构建、PDF 检查和源稿打包入口。

初始未跟踪 `template/cumcm/` 为此前已有第三方模板及占位示例，本轮保留本地，不并入论文或提交。旧交付、审核与清单保持原字节；本轮证据集中在本目录。

## 实际修改

|文件|修改及范围|
|---|---|
|`sections/validation_limits.tex`|第八章仅更改为“模型检验：降维有效性与后段环境灵敏度”，其余章内字节内容一致。第九章改为“模型评价、适用范围与推广”，新增 9.1“模型优点”六条，分别对应共同收支、圆环通量、原函数、事件极值、材料坐标、分层验证与输出追溯；每条以既有章节或公式引用定位。原三个限制归并为 9.2，公式与全部数字不变；原改进小节改为 9.3，纳入结论删除段的气固关系、可达性、同一界面水通量与能量、规定半径的外推限制。|
|`main.tex`|仅删除结论第三段；保留前两段逐字原文。摘要及格式、AI 声明及位置均不改。|
|`sections/appendix.tex`|仅在运行顺序说明后增加一句代码导览，定位第一问、二三问全程、第三问现行表、第四问与二维检验。文件名使用 `nolinkurl` 自动断行，源码全文与引入顺序不改。|
|`sections/q4.tex`|为满足强制全文词语检查，对两处 `60.6526%` 明确写“模型临界时间缩短”，删除原否定句中的禁用词，保留边界解释、全部数值与全域待核验表述。此项为红线清理，没有改变第七章模型或数值。|
|三个 README|更新根、论文与结构交接入口，注明实际基线、目标结构、27 页及本轮检查位置；交接指示及提示词保留为输入合同。|
|PDF、源稿 ZIP 与当前清单|重新编译主论文，更新源稿 ZIP、`source_package.json` 与 ZIP SHA256；旧当前源稿清单备份为 `prior_source_package.json`，旧 `final_artifacts.json` 等历史证据不覆盖。|

## 实际核验

下列九项按指示文档第 7 节逐项执行。语义审阅与机械检查分开记录，不能用关键字命中代替模型证据。

|项|实际结论与证据|
|---|---|
|1. 四处“各处达标”强度|通过主代理及独立代理语义对读：摘要问题三与第六章均仅由二维节点和配对根差支持保守方向；摘要问题四、第七章与第十章均保留精细二维正式点尚待核验的范围。未新增连续界、真实保证或全域充分验证。详见 [独立措辞审阅](independent_wording_review.md)。|
|2. `60.65%` 语境|摘要 `60.65%` 及正文两处更精细的 `60.6526%` 全部明确对应“模型临界时间缩短”。主论文源文及最终 155 页 PDF（包括代码附录）对“能耗”“节能”零命中。此结论不针对历史审核、任务指示或独立 AI 详情文档。|
|3. 潜热口径|摘要保留“不显式计入潜热”；9.2 保留条件性 `45.59 kJ` 对 `4.801 kJ`、`9.50` 倍，以及第四问 `12 h` 的 `164.10 W/m²` 反例。未写忽略影响或已被比热吸收，保留同一真实界面通量及不能重复计算局部相变的限制。|
|4. 中截面措辞|`problem_model.tex` 原文未改，仍把中截面解释归于本文选择；源文与最终 PDF 对“题面指定中截面”零命中。|
|5. 符号与单位|只读核对干基 `C`、绝对温度和两类密度定义；原 Q3/Q4 的 `T+273.15` 与已定义 `T_K` 等价，不为字面统一改公式。六张表源文件与单位原字节保持。9.2 两个公式块及原第九章全部数字词元序列一致；第七章数字序列一致。|
|6. 交叉引用|85 次 `ref/eqref` 使用、47 个不同目标均在最终 AUX 中有唯一定义。最终日志无 undefined reference/citation、重复标签、缺字或 overfull；最终 PDF 无 `??`、`[?]`。原有两处 underfull 保留，已查看对应页面，未见裁切。|
|7. 一级标题|源稿仍为 10 个正文一级章节；PDF 逐项定位“一、”至“十、”，每个标题独占一行。中文编号及 `runin=false` 设置未动。|
|8. 页数|摘要物理第 1 页；正文第 2–28 页，含 AI 声明与参考文献，共 **27 页 ≤ 30**；附录第 29–155 页共 127 页；总计 155 页。|
|9. AI 声明|声明文字与基线相同，位于第 27 页，先于参考文献；独立 AI 详情源文及 4 页 PDF 原字节保留，没有将旧人工审核缺口改写为已完成。|

机器证据：[26 项静态一致性检查](consistency_check.json)、[37 项 PDF 检查](pdf_validation.json)、[PDF 检查摘要](pdf_validation.txt)。六张规定表共 **279 个显示单元格**与冻结清单一致；该项核对显示字符串，不是新数值求解。四份正式 Excel 的 SHA256 与初始独立读数一致；所有不在授权修改集合中的已跟踪文件无 diff，覆盖原数组、求解器、表图、代码快照和旧审核证据。

页面检查：全部 155 页已实际渲染为 [全页缩览](rendered_page_overview.pdf)。与基线按 72 dpi 逐页像素比较，145 页完全一致；变化页为 22、23、25、26、27、28、29、30、31、32，均作了较高分辨率详细目视检查，未见裁切、重叠或乱码。第 23–28 页首次编译后的详细视图已与最终 PDF 逐页像素确认一致。摘要另按 108 dpi 确认像素一致。第 33–155 页由独立代理查看缩览，未逐字重读源码；[页面比对 JSON](page_comparison.json)和[独立完整性记录](independent_integrity_review.md)明确了范围。

实际成功命令（根目录下执行；编译命令在论文目录）：

```text
latexmk -xelatex -interaction=nonstopmode -halt-on-error -file-line-error -outdir=build/structure_revision main.tex
python -X utf8 paper/q1234_draft_v1/validate_paper.py --pdf paper/q1234_draft_v1/build/structure_revision/main.pdf --aux paper/q1234_draft_v1/build/structure_revision/main.aux --ai-pdf paper/q1234_draft_v1/AI工具使用详情.pdf --output paper/q1234_draft_v1/evidence/structure_revision_20260912/pdf_validation.json
python -X utf8 paper/q1234_draft_v1/evidence/structure_revision_20260912/check_consistency.py.txt
python -X utf8 paper/q1234_draft_v1/package_source.py
```

编译及两项检查退出码均为 0；日志见 `compile_console.txt`、`compile_final.txt` 和 `compile_exit_code.txt`。源稿打包和远程同步另按最终外置清单及 Git 提交核验，最终提交 SHA 由交付消息给出，避免在提交文件内记录无法自包含的自身 SHA。

首轮没有隐去失败：附录新导览的不可断行长文件名造成 21.53197 pt overfull，并被 PDF 边距检查检出；改用可断行文件名后复编和重检通过。该轮检查保存于 [attempt1/](attempt1/)。首次 PDF 检查器还因终端 cp1252 无法打印中文路径退出，之后使用 `python -X utf8` 重跑成功，未改检查器。首个编译输出目录变量未展开为预期路径，已移入忽略的临时构建区并以明确路径完成最终编译；一次缩览 PDF 打包因 Pillow JPEG 插件缺失失败，最终改用已具备的 PyMuPDF 写入同一批已查看 PNG，产物已重新打开核对。

主论文 SHA256：`a06ba72e92101311e23e55d757c9ae772224f126f808afa1d423cd13ed33d449`。当前 PDF 与独立 AI 详情哈希见 [本轮产物清单](final_artifacts.json)；源稿 ZIP 指纹由目录外的当前 `source_package.json` 与 `.zip.sha256` 管理，不把 ZIP 自身放入包内制造哈希循环。

## 未做或受限

- 没有重跑任何 PDE、二维补算、参数情景或工作簿导出，没有更新图表数据或数值答案；也没有重跑旧冒烟/复算脚本。
- 没有补齐第四问精细二维正式点及连续误差界，没有升级真实过程或全域保证。
- 没有修改摘要、前述两段结论、AI 声明、独立 AI 详情或历史验收记录；主论文禁词零命中不意味着存档材料也被改写为零命中。
- 本轮沿用已存档格式依据，仅核对用户要求的结构、页数及位置，没有重新联网审查所有竞赛规则、文献或完成最终提交资格认证。
- 页面缩览、自动核对与代理交叉审阅均不等同于参赛队员人工逐项审核；AI 使用详情中的人工记录缺口仍待队员按真实情况补充。
