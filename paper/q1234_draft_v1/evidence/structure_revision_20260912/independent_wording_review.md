# 结构修订独立措辞审核

日期：2026-09-12。比较基线：`b51ca5671e693c01bb04db7d312ce548cf621d89`。本专项通过本地 Git 对象读取该提交的原文，与当前工作区四个 TeX 文件的差异比较；GitHub 在线读取及远程 SHA 的确认由主任务记录，本专项不声称另行完成了远程读取。

本专项只写本审核记录，不修改论文、程序、数组、工作簿或历史证据；没有执行 PDE、参数情景、数值后处理、编译或工作簿导出。

## 1. 实际阅读与检查方法

阅读结构交接文档全部要求，重点核对第 7 节；阅读 `main.tex`、`sections/problem_model.tex`、`q1.tex`、`q2.tex`、`q3.tex`、`q4.tex`、`validation_limits.tex` 及附录开头和代码列表。另读取整体复审的《问题清单与改进优先级》《最小补算计划》《物理闭合独立裁定》，核实优点和限制的证据级别。

最终审查使用 `git diff`、逐段阅读及 PowerShell 字符串/正则比较。文本相等检查先统一 CRLF/LF；它不替代主任务的原始字节哈希核验。新增优点引用的标签已在源码中逐个定位，此项为静态定位，不冒称编译后的引用检查。

## 2. 实际修改符合性

- 第八章标题准确改为“模型检验：降维有效性与后段环境灵敏度”。除首行标题外，从第八章首个小节到第九章之前的文本比较相等；中截面、整根积分、环境情景及加密对照内容均保留。
- 第九章改为“模型评价、适用范围与推广”，包含“模型优点”“适用范围与主要限制”“改进方向与数据需求”三个二级小节。未新增或删除正文一级标题；静态计数仍为十个。
- 第十章当前恰为两段，分别与基线前两段文本相等；原第三段的改进路线已删除，相关要点并入第九章。
- 附录仅新增一句代码导览，位于现有运行顺序说明之后。第一问 `q1_solver.py`、第二三问统一全程 `solve_unified_q23.py`、第三问现行表来源 `q3_refined_reference.py` 和 `export_workbook.py`、第四问 `q4_spectral.py`、二维检验 `geometry_solver_executed.py` 均可在附录定位。另静态查看支撑启动器和导出源码，确认统一轨迹与第三问专用轨迹/导出的区别；未运行这些入口。
- 第七章有必要的最小措辞修正：两处 `60.6526%` 均明确对应“模型临界时间缩短”，数字未变；否定句中的禁用词已改为真实热量收支的限制，未提高结论强度。

## 3. 新增六条优点的证据定位

| 新增优点 | 已有正文位置与标签 | 本专项判断 |
|---|---|---|
| 共同收支框架 | `problem_model.tex:63–66`；`sec:common-model`、`eq:material-balances` | 四问逐步代入物性、几何和终点；未声称通用实验精度 |
| 圆环总量收支可核查 | `q1.tex:56–73`；`eq:q1-flux`、`eq:q1-balance` | 相邻格共享通量、内部项求和抵消有明确推导 |
| 原函数保持扩散通量 | `problem_model.tex:114–124`；`eq:primitive` | 保留可分离扩散率的原通量；未虚构新的性能测量 |
| 内部极值与时间裕量 | `q3.tex:51–76`、`93–115`；`eq:q3-extrema-candidates`、`eq:q3-reconstructed-max`、`eq:event` | 限定径向重构极值及条件数值执行，未升级成连续二维保证 |
| 材料坐标避免重复收缩 | `q4.tex:81–88`、`152–157`；`eq:q4-reference-pde`、`eq:q4-water-ledger` | 无额外浓缩项有推导，零交换纯收缩检验是已有证据 |
| 分层验证与可追溯输出 | `q1.tex:145–160`、`q2.tex:188–208`、`problem_model.tex:138` | 独立算法、加密、导出来源与二维证据级别分别说明，未把同源导出当作独立求解 |

六条均有对应的既有正文证据，未发现无证据的“精度高”“普适性强”或已充分验证全域达标等新主张。

## 4. 9.2 定量反例与 9.3 要点完整性

将基线原 9.1–9.3 与当前 9.2 提取为文本，比较全部阿拉伯数字词元序列，结果完全相等。包括文献键和公式常数在内的实际序列为：

`2014, 2020, 0.15, 0.15, 1, 1, 2, 7, 0.71952, 0.89029, 25, 34.7454, 28.0807, 2.45, 1800, 45.59, 4.801, 9.50, 12, 164.10, 2`。

逐段另核对以下量化含义：

- 气固边界：材料与空气质量基准区别、局部线性化条件及 `rho_d h_m = k_g K` 对应关系保留；目标 `0.15` 的平衡可达性限制保留，加密网格不能修复不可达性。
- 密度：7 h 与末态隐含干质量比 `0.71952、0.89029`；长度 `25 cm` 到 `34.7454 cm、28.0807 cm` 均保留。轴向收缩不能修复缺口、总量凑平不能替代局部相容性、该诊断不是独立账本泄漏等说明均保留。
- 相变：诊断常数 `2.45 MJ/kg`，第一问 `1800 s` 的 `45.59 kJ / 4.801 kJ` 与比例 `9.50`，第四问 `12 h` 的 `164.10 W/m²` 均保留。附加质量解释、近等温持续失水、非实际热量测量和不能固定原热输入倍乘时间的边界均保留。

当前 9.3 已覆盖被删除结论段的所有实质要点：先明确空气/材料水分关系并检查平衡可达性；继而采用相容质量基准及同一真实界面通量联立水分与能量；规定半径只能支持条件响应，不能证明另一工况收缩反馈；真实推广以所需数据和接口验证为先，不能以增加复杂度替代验证。原限制段的一致焓基准及避免重复计入体积相变项，也已移入 9.3。

## 5. 四处结论强度与红线

摘要文本与基线相等。第六章全文与基线相等；第七章全域终点段未修改；结论前两段与基线相等。

| 对象 | 实际口径 | 判断 |
|---|---|---|
| 摘要问题三与第六章 | 二维节点场及配对根差支持 57.4741 h 的保守方向；不是连续二维严格误差界 | 同级 |
| 摘要问题四与第七章 | 51.0921 h 保留为一维条件执行值；全域终点仍需精细二维正式点及误差/正裕量核验 | 同级 |
| 第十章 | 两问时长为一维条件执行值，特别指出第四问精细二维与裕量缺口，不能宣称连续有限圆柱严格保证 | 与摘要及第六、七章同级 |

对 `main.tex` 与当前 `sections/` 运行禁用词检索，“能耗”“节能”“可忽略”“题面指定中截面”均为零命中。摘要 `60.65%` 和第七章 `60.6526%` 均明确为模型临界时间缩短。摘要不显式计入潜热与 9.2 相变能量缺口的定量说明一致；没有新增潜热被容量吸收、潜热影响很小或已充分验证各处达标的主张。

符号检查未见本次改动混淆干基 `C`、有效容量密度与干质量密度，保留的表格单位无修改。Q3/Q4 部分既有指数用 `T+273.15`，与已定义的 `T_K` 等价，仍为绝对温度；本专项没有要求为字面统一改动公式。

AI 声明内容与基线相等，仍在参考文献之前。独立历史配套说明 `ai_details.tex` 未进入主论文 `main.tex` 编译链，本轮保持不动；因此上述禁用词零命中结论严格限于当前主论文源稿，不声称历史材料或全仓库零命中。

## 6. 未做与核验边界

本专项结论为：所审四个 TeX 的结构及措辞修改符合当前交接要求，未发现阻碍交付的文字或证据级别问题。

本专项没有编译 PDF、检查编译日志、逐页视觉审阅、确认最终正文页数、验证最终 PDF 中的交叉引用、核查 ZIP/清单一致性或确认推送状态。这些事项由主任务的实际日志和独立页面检查记录负责，不能由本专项的静态标签定位替代。本专项也没有新做 PDE、参数情景、数组或工作簿全量数值验算，没有完成参赛队员人工审核。
