# 模型假设与目的表述试排记录

## 实际读取与本轮要求

- 日期：2026-09-13。
- 本地HEAD与通过GitHub fetch取得的origin/main均为 `b54834e1290f3514e776c4ca57765336d5d3340b`。
- 用户要求把每条写成“加粗的具体假设＋冒号＋采用目的”，先修改后查看效果。
- 本轮读取AGENTS.md、当前模型假设及相关主稿/编译入口。既有参数、数学表达与必要适用边界保留；不把本轮试排记录为用户已最终验收。
- 开始时无已跟踪文件修改；未跟踪main.pdf及template目录未修改、未提交。

## 实际修改

只改 sections/problem_model.tex 的六条模型假设，每条冒号后首句直接说明目的：

1. 初始温湿均匀、轴对称连续介质、主模型忽略端面：简化为径向温湿场，减少空间变量并明确初始条件。
2. 空气记录通过恒等映射给出有效边界：把空气记录接入表面交换条件，为扩散方程提供边界输入。
3. 各问保持相同的表面交换系数：补齐后问未另给出的参数，使边界参数一致。
4. 题给密度用于有效热容量、干质量独立守恒：保留题给热物性，避免经验密度解释与干质量守恒冲突。
5. 前三问固定几何、第四问恒长度均匀径向收缩：在该运动假设下把半径记录转化为内部运动，并映射到固定参考域。
6. 4 h后取末小时61点均值：补齐观测后输入，为长时演化及干燥终点判定提供边界条件。

原保护声明继续紧随参数与目的说明；第4条仍明确未显式计入潜热、携焓和机械功，不能宣称真实能量闭合。第5条仍明确内部运动不能由外半径唯一识别。加粗范围仅对应假设主句。

本轮沿用既有Needspace控制；未新增分页参数。字号、行距、页边距和章节数不变。新旧全文见 `assumptions_comparison.md`。

## 实际核验

- 主代理与独立代理完成语义核对：六条均为具体假设后接目的，无新增物理验证主张。
- 假设区段外源码逐字不变；另89份正文、表格及图资源的Git blob哈希与基线相同。
- 整个problem_model.tex数字词元132→132、多重集相同；排除Needspace后128→128。行内数学表达多重集保持，环境等式逐字不变；六条关键保护声明逐字保留。
- 正文预览与完整PDF经XeLaTeX/latexmk编译成功；最终日志无Overfull、未定义引用、重复标签或缺字。
- 完整PDF的37项自动检查通过、2项信息；六张规定表279个显示单元格与冻结清单一致。
- 正文仍12个一级章节、30页，总PDF158页（摘要1页、正文物理页2–31、附录127页）。
- 全158页按72 dpi逐页像素比较，仅物理页3、4变化，其余156页相同，摘要及全部附录保持。预览前31页与最终完整PDF相同。
- 实际目视第3–4页：前四条位于第3页，第5–6条位于第4页，各条完整；加粗假设与普通目的可辨，环境等式及后续19行符号表完整，无截断、重叠或页脚碰撞。
- 用Poppler重渲染最终完整PDF第3–4页，和已目视预览PNG逐像素相同。展示局部图直接从最终PDF裁取，未改写或重排文字。
- 正式PDF已替换，SHA256：`3c3e18bcee955af20a895efe8274d51f15e15988652fb625a63f04b8a72c8a1b`。

证据见 `baseline.json`、`source_integrity.json`、`page_integrity.json`、`pdf_validation.json`及文本版、编译控制台日志。源稿ZIP逐成员和清单核验见本目录 `source_package.json`。

## 未做或受限

未重跑PDE、参数情景或科学绘图；未修改任何客观结果、摘要、结论或图片。没有把排版与源稿一致性核验作为物理有效性或连续二维严格达标的新证据。其他页面通过像素比较确认保持，没有声称本轮逐页重新人工审读。新版供用户继续审阅措辞与版式。

受影响README、源稿ZIP与清单随本轮更新；推送结果以Git历史及最终回复为准。
