# 摘要第二问结论句修订记录

## 实际读取与起始状态

- 本地 main 与 fetch 后的 origin/main 均为 `aa055a3c07b99fc1d569b590290f67a507e7c4dd`。
- 已核对根 AGENTS.md、根 README、论文 README、main.tex、构建及源稿打包入口。
- 起始工作区 main.tex 已包含用户确认的新结论句，但前面的“回落”后仍为逗号；该修改予以保留并完成句间标点调整。
- 起始未跟踪的 paper/q1234_draft_v1/main.pdf 与 template/ 保留原样，不纳入本轮提交。

## 实际修改

用户确认的新句为：

> 结果表明，温度分布已接近均匀，而含水率仍存在明显的径向梯度。

基线旧句：

> 说明温度趋匀并不代表干燥均匀，升温也不能持续抵消失水造成的扩散衰减。

仅替换摘要第二问原有加粗结论块，并将其前面的“2.5—3 h 内两处扩散率均已回落，”改以句号结束。新句仍在原加粗块内；未新增其他加粗。该段其余内容、数据与正文各章保持。README、正式 PDF、源稿 ZIP 与外置清单随此次修订更新。

## 实际核验

- XeLaTeX/latexmk 在 build/abstract_q2_sentence 独立目录重新编译，退出码为 0；没有运行任何 PDE 求解、参数情景或绘图脚本。
- 与基线逐字比较：main.tex 仅有上述结论块与句间标点变化；数字 token 的值、数量、顺序不变（142 个）；加粗命令数量仍为 24。各章节源文件无修改。
- PDF 仍为 157 页：摘要 1 页、正文 29 页、附录 127 页。现有 validate_paper.py 的 37 项自动检查通过，六张题设表全部 279 个显示单元格一致。
- 最终日志未发现未定义交叉引用或 Overfull hbox/vbox。
- 新旧 PDF 全部 157 页以 PyMuPDF 72 dpi 比较：仅物理第 1 页变化，第 2—157 页像素相同。
- 摘要页用 Poppler 120 dpi 渲染并目视：新句文字和加粗正确，关键词完整，未见溢出或重叠；摘要仍在 1 页内。
- 正式 PDF 已替换，SHA256 为 `d5930757014058d46b1cef9ce32a77ccdf4b54c491afab2310fb61b396e63767`，与核验后的编译 PDF 相同。
- 源稿 ZIP 使用既有 package_source.py 重建；包内文件、磁盘文件与内外清单的核对结果见本目录 source_package.json。外置 source_package.json 不入包，避免自引用。

## 未做或受限

没有修改模型、计算数据、图表、正文、结论或全局版式；没有重跑求解或参数情景；没有从解压后的源稿包再次编译，也没有进行实体打印检查。本轮自动检查和目视仅用于确认此次编辑及其排版，不构成对数值模型或最终参赛材料的重新验收。

## 证据入口

- revision_check.json：源文、数字、加粗、页数、页面像素及正式 PDF 哈希核对。
- pdf_validation.json / pdf_validation.txt：37 项现有 PDF 检查及六表核对。
- compile_console.txt / compile_exit_code.txt：本轮完整编译输出与退出码。
- prior_source_package.json：此前源稿包清单原字节备份。
- source_package.json：本轮源稿包独立读回核对与最终产物哈希。
