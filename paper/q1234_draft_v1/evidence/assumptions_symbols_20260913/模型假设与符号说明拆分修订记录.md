# 模型假设与符号说明拆分修订记录

## 实际读取

- 本轮本地 main 与获取后的 origin/main 均为 `b361bf98b5005063617dd7aef5c7d5dd763d76a4`。
- 已读取 AGENTS.md、当前主稿第三大节、四问正文、根/论文说明、打包与PDF校验入口，并核查环境平台与物性表的引用。
- 按用户最后选择：物性总表仍集中放在论文前部，与符号说明归属同一大节；未拆入各问。

## 实际修改

- 原“数据处理、基本假设与符号”拆为“三、模型假设”和“四、符号说明”。第四节下设4.1主要符号、4.2分问物性。一级标题继续居中，正文由十一章顺延为十二章。
- 删除“数据时域与插值”小节及其数据清单、处理过程文字。原始数据、代码和计算结果没有删除或修改。被后文引用的4小时后环境平台作为第6条假设保留：末小时61点均值、原数值、原 `eq:future` 等式与标签保持；继续明确其属于观测外延拓。原5条模型假设逐字保持。
- 符号表由8个合并行展开为19个单符号行，分别列出符号、含义及单位；补明确cp、k、D的单位，区分有效密度与干物质密度、摄氏与绝对温度、临界与执行时刻。
- S1采用局部行距系数1.10，保留原表格字号和全局行距；表格固定在符号说明后，标题前预留空间，避免表格与标题分离。S2分问物性说明和完整表格原文保持，仍位于四问之前。
- 只有 `sections/problem_model.tex` 的论文源文发生修改；摘要、前两章、各问、统一方程与结论文字、全部图表数据及计算源码保持。根README、论文README、正式PDF、源稿ZIP与清单同步更新。

## 实际核验

- 完整XeLaTeX/latexmk编译成功，退出码0。最终158页：摘要1页，正文30页（物理页2–31，含AI声明/参考文献），附录127页；与本轮基线页数相同。
- `validate_paper.py` 的37项检查通过、2项信息提示；六张规定表共279个显示值与冻结清单一致。见 `pdf_validation.json`、`pdf_validation.txt`、`compile_console.txt`。
- 独立源码核验：原5条假设、环境平台等式、完整物性表逐字相同；符号表恰好19行且一行一符号；170份其他稿件、图表和支撑源文件保持基线原字节。无缺失引用或重复标签。见 `source_integrity.json`。本轮删除了数据处理文字，不声称全文数字出现次数不变。
- 12个一级章节居中；S1/S2与正式结果表编号保持。环境平台仍为式(1)，限制/改进方向/环境情景引用自动顺延为11.3、11.4、10.2。四问标题及流程图仍同在物理页7、11、16、20；编译无未定义引用、缺字或Overfull警告。
- 摘要首页和全部127页附录（包括页脚）在72dpi下与基线逐页像素相同；正式全文前31页与最终预览逐页像素相同，验证结果见 `page_integrity.json`。
- 独立代理从最终预览PDF直接渲染并逐页目视物理页1–15，主代理查看16–31页。符号表19行均清楚，4.2标题后有完整引导文字、物性表紧接下一页，图表与段落未见裁切、重叠或孤标题。受检预览SHA256为 `baa99daf51606c8e8117c7c58dacddf3fc86a323a8a3f8a54fd615e60d2ec826`，已绑定正式全文。
- 打包后独立检查ZIP的CRC、文件集合、逐文件SHA256、内外清单、磁盘源文件及正式PDF与构建产物的一致性，结果见本目录 `source_package.json`。该检查文件按既有规则不进入ZIP，避免自引用。

## 未做或受限

- 未重跑PDE、参数情景或作图，未改变模型、客观结果或已验收原交付。
- 未从解压源稿包另行编译，未做实体打印；代理与程序核验不替代队员最终事实审核。
- 保留原未跟踪的 `paper/q1234_draft_v1/main.pdf` 和 `template/`，未纳入本轮提交。
