# 图表与上下文联动修订记录

## 实际读取与任务边界

2026-09-12 开始时，本地 `main`、获取后的 `origin/main` 同为 `a2c8baf0175c29a4fe21145ba222cb8e19d84199`，没有分叉。通过 GitHub 连接按该 SHA 读取根 README，返回的文件 blob 为 `e816e6d1d4cf159a4620d45070b256931232d644`；实际返回见 `github_read.json`。并在本地读取现行论文源码、六幅原图、已有绘图脚本、此前措辞修订记录及所需冻结数据。开工时已有未跟踪 `template/`，本轮未改、未收入提交。

本轮执行用户授权的图表与上下文联动改善。摘要、结论、AI 声明、十个正文一级章节、公式、六张规定表和客观数值结果冻结。只重绘六图，调整五份章节文件中直接相关的图注/导览，以及图2之后的一处浮动体屏障。未运行 PDE、事件重定位或任何参数情景。

## 判断与实际修改

与六张旧图实际并排查看后，认为新版的整体可读性、图形分工和视觉辨识度更好。理由是不同证据采用不同编码：过程用时间—径向色图，状态关系用路径，终点用离散事件标尺，微小差异用差值剖面，条件比较用共轴点图。统一字体、颜色与线条后，图之间有联系而不重复同一版式。这是基于图面和整页排版的编辑判断，没有做读者实验，也不声称已优于其他论文。

| 图 | 已采用的修改 | 论证如何保持 |
|---|---|---|
| 1 | 四时刻径向曲线组改为时间—径向温湿色图，直接标末时轴心/表面值 | 原四个时刻包含于全部保存采样；正文温差、表层失水、扩散时间尺度和规定表不改，导览改为解释两个轴和色阶 |
| 2 | 温度径向面板改为轴心/表面温湿路径；水分面板从六时刻选取三个代表剖面 | 六时刻规定表、原温差与含水率差、机制分解完整；文字明确路径箭头是时间推进，右图承担径向梯度论证 |
| 3 | 保留全过程曲线，增加原等号根—执行点离散时间标尺及未舍入最大值 | 正式时长、阈值和两问物性差异不变；没有构造亚秒含水率曲线或新的误差区间 |
| 4 | 增加当前实际收缩材料域色图，保留原四剖面和完整观测半径小图 | 原同物性对照分析不变；明确输入边界、材料域外留白、色图止于正式执行点，观测输入另保留完整时域 |
| 5 | 重叠的一/二维曲线改为终点差值，下排三组均值时程合并为终点配对均值 | 六均值及 7.2533%/4.7118%/2.7379% 与表7完全一致；原时段最大差数字保留，明确其统计范围不同于图中终点差；不再用终点图描述完整时程 |
| 6 | 两幅条形图合并为共零线的双系列点图 | 八个原情景根差逐项一致；保留人工幅度、同阶基线、第四问规定半径及跨扰动尺度解释边界 |

新图2采用现行收尾交付的冻结轨迹，旧图2用此前冻结轨迹。独立检查六时刻×21位置的温度126格和水分126格，四位显示全部一致；两份轨迹的未舍入差别如实记在 `process_notes.md`，没有将不同来源称为同一字节文件。新图5删去可重复的均值中间轨迹，原数据、旧图和原曲线证据保留；不是声称图形信息毫无取舍。

五份源码的完整实际差异见 `正文图注与导览差异.diff.txt`。所有旧含数字结果段和保护性正文保留；图5的差值公式新增两个数字词元，实际编译源数字计数 1200→1202，原数字无减少。第二问在新图2后加 `\FloatBarrier`，使其读图说明不被表格和图片从一句话中间隔开，未修改该段的任何数值。

## 数据来源与绘图执行

新脚本入口是 `../../build_visual_assets.py`，三组脚本在 `../../visual_revision/`。本轮实际分别运行 `process.py`、`geometry.py`、`validation.py`，退出码均为0；未运行包装入口或原资产重建脚本。三份来源清单共记录23个不重复的文件（包括坐标解释所需的只读求解源码），输入前后 SHA-256 均保持。求解源码只读，不导入执行。

图1采用原采样最近单元着色；图2/3仅抽取明确已存时刻并连接；图4只在原时间—材料网格内插值颜色，使用 `r=R(t)√x`，不生成新的求解状态或越界外推；图5只在保存节点作差和按原权重汇总；图6只对原根值作差。完整来源、数组或关键取值、处理方式、运行依赖和输出哈希见 `process_sources.json`、`geometry_sources.json`、`validation_sources.json` 及三份 notes。

新图置于 `../../figures/visual_revision/`；旧图、旧绘图脚本、旧来源清单及历史证据全部保留原字节。另生成 `六图新旧对照.pdf`，六页并列展示实际旧、新 PDF 图形，便于人工审阅。

## 实际核验

最终编译命令：

```powershell
latexmk -xelatex -interaction=nonstopmode -halt-on-error -file-line-error -outdir=build/figure_revision main.tex
```

最终退出码0，正式 PDF 与构建 PDF 字节一致，SHA-256：`f70a1bb14ed72d64d9bca0f54ba5d64aa068b2ecf2b0c56d5e540921b75a2b50`。总157页：摘要1页，正文29页（物理页2–30，含AI声明与参考文献），附录127页（31–157）。六图仍为1–6，分别在物理页9、13、18、23、25、26。

| 一致性项目 | 实际结果与证据 |
|---|---|
| 1. 原结果与模型文件 | 独立基线保护2,562个Git blob，四份正式Excel、公式、六表、旧图和旧证据未变；见独立检查 |
| 2. 摘要、结论与结构 | `main.tex`整文件字节不变，十个一级标题与9.1–9.4顺序不变；摘要不得强于正文的已有口径保持 |
| 3. 图的数据来源 | 三组脚本实际运行成功，23个不重复来源哈希一致；仅冻结数据后处理 |
| 4. 图表数值 | 六张规定表279个显示单元格一致；图5六均值/三增幅、图6八差值均逐项核对 |
| 5. 图文和主张边界 | 坐标、颜色、点形、时窗和面板解释逐图对应；原数字句/保护声明保留，没有增加连续二维或实际执行保证 |
| 6. 编号与交叉引用 | 六图编号连续；99次ref/eqref使用、51个目标唯一解析，文献引用有效，最终日志无未定义引用或缺字 |
| 7. 页数与机器检查 | 37项PDF检查通过，摘要1页、正文29≤30页；不代替人工事实审阅 |
| 8. 目视与像素比较 | 全157页已渲染，摘要/正文物理页1–30逐页查看；图1–6重点详看。摘要与旧版像素相同，127附录页仅屏蔽页脚页码后像素相同 |
| 9. 源稿与清单 | 更新源稿ZIP、内置SOURCE_MANIFEST及外置清单；CRC、成员集合、逐文件哈希和目录一致性结果见本目录外置`source_package.json` |

第一次排版审阅发现第二问一句解释被两块浮动体隔开，已修复。最终重编译只使物理页12–17相对此前候选改变，六页均已重新查看；其余151页像素相同。初审和复验记录及图像哈希分别见 `visual_review_pages_01_15.md`、`visual_review_pages_16_30.md`、`float_adjustment_check.json`。主流程还实际并排查看六组旧/新图。没有将缩览、静态检查或代理审阅冒充队员人工验收。

正式PDF第一次覆盖曾被WPS占用；用户关闭该PDF后已成功替换。临时分页比较脚本曾因在关闭PDF后读取页数而退出，修正脚本调用顺序后比较完成，最终结果保存在 `float_adjustment_check.json`。这些中途失败不计作已通过检查。

## 包装、同步及未做事项

根README和论文README更新当前入口、页数、绘图复现方式及本记录；源稿包纳入新脚本、六组PDF/PNG及本轮审阅证据。大数组继续留在仓库原目录；独立源稿包使用已生成图片即可编译。外置 `source_package.json` 不进入ZIP，以避免包哈希自引用；旧包清单已保留为 `prior_source_package.json`。

按项目AGENTS约定正常提交并推送到`origin/main`，不强制覆盖历史；远程核对的提交链接随本轮回复提供，记录不以预写本次提交SHA制造自引用。

未进行新PDE/参数情景、精细二维补算、潜热联立或新的模型/实验验证，原相应局限仍在正文。不修改摘要或结论，不把图形平滑、放大差异、离散阈值标记或观测边界绘制当作证据等级提升；未做读者偏好实验或与其他论文的优劣测评。
