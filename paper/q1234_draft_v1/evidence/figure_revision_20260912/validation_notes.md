# 图 5、图 6 重绘记录

本轮基线：`a2c8baf0175c29a4fe21145ba222cb8e19d84199`。实际操作日期：2026-09-12。

本记录只覆盖 `visual_revision/validation.py`、两组 `figures/visual_revision/` 下的新 PDF/PNG 及 `validation_sources.json`。未改旧图、旧绘图脚本、历史证据、TeX 或公共样式文件。脚本只读取已保存 NPZ 和 JSON；没有导入或执行求解器，没有 PDE、时间积分、事件重定位或参数情景执行。

## 图 5：降维证据

新文件：`figures/visual_revision/dimension_reduction.pdf` 和同名 PNG。PDF 单页，实际尺寸约 16.4000003 × 8.9999997 cm（PDF 点坐标浮点换算误差）；设计尺寸 16.4 × 9.0 cm。

| 旧图信息 | 新图对应及取舍 |
| --- | --- |
| 上排三个面板分别叠加一维、二维终时径向含水率曲线，微小差异难以分辨 | 改为同一终时、同一物理径向位置的 `C_2D-C_1D` 差值剖面；仍覆盖问题一 1800 s、问题二 3 h、问题四 6 h。每个面板直接标明纵轴数量级，不能比较三个图上曲线的视觉高度来排名。 |
| 原上排显示绝对含水率剖面 | 新图不再显示绝对含水率剖面；原始两条 81 节点剖面与差值数组都完整写入 `validation_sources.json`，旧图保持原字节。 |
| 下排三个面板显示整根均值时间曲线 | 合并为一幅各时窗终点的配对均值图。舍去中间时刻轨迹，保留三个终点的六个原均值和三个原失水增加百分数。此次图形信息经过精简，不将终点图声称为完整时间历程。 |
| 原图以终点注释表达失水增加 | 新图单独列出失水增加，原数值及定义不变。每行明确所属问题和不同终时，不按失水增加排序。 |

实际读取的配对数据为 `q4_complete_delivery/v1/validation/geometry/` 下六份冻结文件：

- `q1_80x128.npz` / `q1_80x0.npz`，读取精确 1800 s 快照。
- `q23_80x256_integral.npz` / `q23_80x0_integral.npz`，读取精确 10800 s 快照。
- `q4_80x128_integral.npz` / `q4_80x0_integral.npz`，读取精确 21600 s 快照。

二维数据表示半柱；`z_m=0` 对应索引 0，即中截面对称面。读取 `snapshots[指定终时, 0, :, 1]` 的完整 81 节点含水率，不将保存的 21 点 `mid` 重采样表当作完整网格场。已核对每个剖面的中心、表面两端与 `mid` 的相应端点一致，绝对容差为 `1e-12`。这一取值约定与只读查看的 `geometry_solver_executed.py` 第 137—143 行、第 197—199 行相符；该源文件没有导入或执行。

当前径向坐标由 `reference_r_m * radius_m(t) / reference_r_m[-1]` 直接映射，再换算为 cm。每组一维、二维坐标数组已逐元素核对相同。问题四在 21600 s 的保存半径为 `0.013740000000000002 m`，图线止于该当前表面；没有越界外推或空间插值。

下表中的差值极值仅是对应终时已保存 81 节点差值的后处理统计，供核对图轴，不是新增求解结果、连续界或纯物理降维误差评估：

| 终时 | 新图差值单位 | 终时节点最大绝对差 / (kg/kg) | 原二维均值 | 原一维均值 | 原失水增加 |
| --- | --- | --- | --- | --- | --- |
| 问题一，1800 s | `10^-10 kg/kg` | `7.010665381557146e-10` | 2.274929 | 2.293532 | 7.2533% |
| 问题二，3 h | `10^-5 kg/kg` | `2.2470580948841246e-5` | 1.327482 | 1.382492 | 4.7118% |
| 问题四，6 h | `10^-7 kg/kg` | `8.877834876130919e-7` | 1.024545 | 1.065198 | 2.7379% |

原正文的 `4.09×10^-9` 和 `2.24706×10^-5 kg/kg` 是问题一、二在已检时段中截面采样的最大含水率差。新图逐点终时差没有使用这两个数字作为终点差；尤其问题一两者不同。应保留原正文的时段证据表述，并明确图中展示终时剖面。问题四的终时差值也不能用于外推长期或连续二维精度。

整根均值采用保存控制体权重 `sum(C*w)/sum(w)`；问题四仿射收缩的共同当前体积系数在归一化中抵消。在空间均匀干密度假设下，体积加权平均才等于干质量加权平均。失水增加仍为 `100*((2.55-Cbar_2D)/(2.55-Cbar_1D)-1)`，不是均值相对误差。六个六位小数均值与三个四位小数百分数已通过脚本断言逐项核对现有表值。

下排二维圆点与一维方点仅在行方向作轻微错位以避免近点相互遮挡；行方向没有新增物理量，只有横坐标编码均值。连线表示同一行配对，不表示时间演化。每个均值直接标数，颜色和点形双重区分。

建议图前句：

> 图\ref{fig:dimension-reduction}并列给出三个时窗终点的中截面配对差值和整根均值，显示局部近似与总体失水应分别判断。

建议 caption（主代理决定最终 TeX，不由本子任务写入）：

> 已保存一维、二维配对场的降维证据：上排为问题一1800\unit{s}、问题二10800\unit{s}及问题四21600\unit{s}的中截面终时含水率差$C_{2D}-C_{1D}$，各面板的纵轴数量级不同；下排为相应终时的整根体积加权平均，六个均值及失水增加与表\ref{tab:wholebody}一致。上排为终时网格节点差，不等同于时段采样最大差；问题一、二在已检时段中截面采样最大含水率差仍分别为$4.09\times10^{-9}$、$2.24706\times10^{-5}\unit{kg/kg}$。体积加权平均在空间均匀干密度假设下等于干质量加权平均。三组时窗不同，只作各组配对，不据此排名。来源沿用原六份配对\texttt{.npz}；二维半柱坐标的$z=0$为中截面对称面。

需将原 caption 的“下排为整根体积加权平均随时间的变化”改为上述终点均值口径，图引用路径指向 `figures/visual_revision/dimension_reduction.pdf`。原表和正文时段采样最大差数字没有理由修改。

## 图 6：后段环境情景

新文件：`figures/visual_revision/environment_sensitivity.pdf` 和同名 PNG。PDF 单页，实际尺寸约 16.4000003 × 6.2 cm；设计尺寸 16.4 × 6.2 cm。

旧图为问题三、四分开的两幅四行情景条形图；新图将相同四行情景合并到一个横轴，问题三用蓝色圆点、问题四用橙色方点，共用零线。每个原情景差值完整保留，并以六位小数直接标注。去掉重复坐标轴和大色块，没有误差棒、置信带，也没有将 Q3/Q4 之间距离表达为不确定区间。横轴负值表示相对于各自同阶基线的等号根提前，正值表示后移。

实际读取 `q1234_overall_review_delivery/v1/evidence/environment/` 下两份 `q3/q4_baseline_n40.json` 及八份 `temperature_minus_1K`、`temperature_plus_1K`、`boundary_minus_0p005`、`boundary_plus_0p005` 的同阶 JSON。绘图值直接取冻结 `critical_s`，与各自同阶基线相减后除以 3600；没有以正式四位小时执行值或另一阶基线为参照。两问均从保存的 4 h 状态开始，每问各情景的来源状态 SHA 与各自基线一致，脚本已逐项核对。

| 人工设定的后段情景 | 问题三根变化 / h | 问题四根变化 / h |
| --- | --- | --- |
| 温度 −1 K | +1.867279 | +1.637343 |
| 温度 +1 K | −1.785639 | −1.566660 |
| 边界 b −0.005 | −0.091249 | −0.098185 |
| 边界 b +0.005 | +0.110117 | +0.126750 |

八个原情景根、八个根差显示值均通过脚本断言与既有表值一致。两个温度扰动与两个边界扰动的尺度不同；图中幅度不是测量不确定度，不据图给温度或湿度误差作普遍重要性排序。第四问规定 `R(t)` 在各情景中保持不变。

建议 caption：

> 4\unit{h}后人工设定环境情景相对各自40阶基线的临界根变化；圆点、方点分别表示问题三、四，横轴负值为根提前、正值为根后移。各问沿用同一保存的4\unit{h}状态，第四问规定半径保持不变；扰动幅度不是测量不确定度或置信区间。

正文现有后段环境限定可保留，仅将图路径指向 `figures/visual_revision/environment_sensitivity.pdf`；新图不要求改动任何根或差值。

## 实际核验与限制

- 实际运行 `python -X utf8 paper/q1234_draft_v1/visual_revision/validation.py`，最终退出码 0；冻结输入 17 个、新图输出 4 个，全部输入 SHA-256 在运行结束重新核对未变。六份 NPZ、十份 JSON 及只读坐标源代码的哈希，全部曲线/点值、输出尺寸、运行版本和脚本哈希见 `validation_sources.json`。
- 图形迭代真实发生：首次发现微软雅黑缺少部分 Unicode 上下标字形，改为数学排版；随后实际通过 `view_image` 查看两张 300 dpi PNG，发现图 5 总标题/子标题及两图脚注/横轴标题重叠，调整标题定位和下边距。最后再次实际查看两张新 PNG，未见上述重叠、缺字、遮挡或裁切。图 5 六均值、三百分数，图 6 八差值均可直接辨认。
- 通过 PyMuPDF 读取最终两个 PDF：均为单页、尺寸符合分配范围，字体条目为 TrueType（微软雅黑，图 5 数学标签另用 DejaVu Sans）。最终脚本运行没有字体缺字警告。另将四个输出重新计算 SHA 与记录核对一致。
- 本子任务没有编译主论文、没有检查主论文分页或其最终图注；上述目视结论仅绑定所生成的两张独立 PNG。整篇 PDF 的图内字读数、图注排版及交叉引用由主代理最终编译核验。
- 新图不会提供新的二维连续严格界、实际工况保证、实验验证或参数置信区间。很小的终时相减值可能处在数值残差尺度，不能单独隔离为物理降维误差。所有旧图和原始结果仍保留。
