# 第三、四问图形重绘记录

基线为 `a2c8baf0175c29a4fe21145ba222cb8e19d84199`。本子任务只新增 `visual_revision/geometry.py`、`figures/visual_revision/` 下的两组 PDF/PNG，以及本记录和 `geometry_sources.json`。旧图、正文、共同绘图模块和历史证据均未修改。实际执行 `python -X utf8 paper/q1234_draft_v1/visual_revision/geometry.py`，最终退出码为 0；输入与输出哈希见本目录 `geometry_sources.json`。

## 图3：全过程与离散事件证据

上排继续展示第三、四问的轴心和当前表面含水率、对数纵轴、0.15 阈值及 57.4741 h / 51.0921 h 正式执行标记。第三问读取 `q4_complete_delivery/v1/q123_closeout/output/q23_unified.npz`，每 60 s 选一个已存点，并保留精确执行点；第四问读取 `q4_complete_delivery/v1/output/main.npz` 的全部 3067 个已存时间输出。两问标题明确附录3固定半径与附录4规定收缩，不把两条曲线的差解释为单一收缩因素。

下排是离散时间标尺，只用 `q23_unified.json` 与 `end_event.json` 的已存临界根和执行点，未构造两点间的含水率轨迹。横段高度恒定，无含水率纵轴，不是亚秒级采样曲线、误差带或置信区间。标尺以各自临界根为 0，执行点分别标为根后 +0.3701 s 和 +0.3483 s；下方只列已有正式执行最大值 0.149999892208 和 0.149999821161。脚本实际核查两数与相应冻结轨迹末点一致，且均低于 0.15；时间差的四位显示与原正文一致。

旧图的信息全部保留，新图增加了“等号根与执行点的区分”这一原正文已建立的证据。它没有展示二维空间场，也没有借标尺新增连续二维或实际工况保证。

### 建议图注与正文连接

原 `q3.tex` 图注应把“轴心、当前表面含水率演化”明确为上排，并补下排含义，可采用：

> 问题三（左）与问题四（右）的干燥过程与事件判定。上排为一维径向有效模型的轴心、当前表面含水率；纵轴采用对数刻度，水平线为0.15 kg/kg阈值，终点为正式执行时刻。下排仅以已存临界根和执行点构成离散时间标尺，并列出执行最大值。两问采用各自附录物性，第四问另施加观测半径历程，曲线差异不能全部归因于收缩。

可在正文相邻处增一句：“上排显示表面先下降而轴心控制尾段，下排将等号临界根与已选执行点分开，二者的时间差来自原事件记录。”不应称下排为亚秒级变化曲线，也不应将其作为新的误差证明。

## 图4：实际材料域中的模型场、剖面与观测输入

(a) 色图读取 `main.npz` 的 `full_TC`、`time_s`、`radius_m`、`x`，以 `r=R(t)√x` 映射全部 121 个径向节点到当前实际半径，时间只到 51.0921 h 正式执行点。每个网格外缘都位于该时刻的 `R(t)`；图中无材料域外网格或填充值，域外显示白色。色标为对数色标，范围 0.05–2.55 kg/kg，颜色表达有效模型含水率而不是实测含水率。

网格内采用 Gouraud 颜色插值，展示已保存的 3067 × 121 个节点值。该显示插值不生成新的 PDE 状态，不向执行时刻之后或当前半径之外外推，也不把平滑颜色当成连续场严格精度。没有标绘可能被误读为严格全域边界的达标色带。

黑色外缘是输入半径边界；空心点为附件2观测半径的显示抽样。半径原始输入读取 `q4_complete_delivery/v1/inputs/attachment2.xlsx` 的全部 145 条记录，脚本实际核查其线性重建与 `main.npz` 保存半径一致。

(b) 保留旧图全部四个剖面时刻：6、12、24、51.0921 h，分别从精确保存时刻取值，使用当前半径横轴并以圆点标出当前表面。它继续支持不同时间的内部与表面含水率差异以及“曲线止于材料表面”的原论证。各条曲线使用的当前外半径分别为 1.374、1.248、1.204、1.2 cm，来自原数组，数值仅在来源记录中保留用于核查。

(c) 保留旧图覆盖 0–72 h 的完整观测半径历程及正式执行时刻竖线。该小图是输入记录，不是把模型场延长到 72 h。因此旧图的观测长度、四条剖面和执行标记均有对应，新图仅增加实际材料域中的含水率色图。

### 建议图注与正文连接

原 `q4.tex` 图注的“半径历程（左）/剖面（右）”已不适用，建议改为：

> 规定收缩下的径向有效模型场与观测输入。(a) 当前实际半径坐标中的含水率色图，仅显示至正式执行时刻，采用对数色标，材料域外留白；边界线及空心点区分输入半径与观测记录。(b) 四个已保存时刻的径向剖面，各曲线止于对应材料表面。(c) 附件2的0–72 h半径观测输入，虚线标示正式执行时刻。

原紧邻句“图…给出半径输入与相应的径向含水率变化”仍成立，可具体改为：“图…在实际半径坐标中展示规定收缩与含水率演化；色图在正式执行时刻停止，右下小图保留完整半径观测输入，左下剖面均止于当前材料表面。”

原 `q4.tex` 中同物性恒半径对照的129.848360839 h、施加收缩后的51.092003254 h、78.7564 h与60.6526%分析没有由本图新增或替换；两问约6.382 h净差不能全部归因于收缩的句子仍应保留。新图不把输入观测半径重建当作温湿预测验证，也不支持删除原二维证据层级限制。

## 实际核查与目视结果

脚本检查：五份输入文件前后 SHA-256 不变；执行最大值与保存轴心末点一致；两条标尺时间差匹配原正文显示；半径重建与保存半径一致；四个剖面都来自精确保存时刻且止于当前表面。未导入或执行求解器，未执行参数情景。

输出尺寸：图3为16.4 × 8.0 cm，图4为16.4 × 9.2 cm。实际检查两个输出 PDF 均为单页并嵌入 Microsoft YaHei 字体；图3保持矢量图形，图4仅将密集颜色网格栅格化，文字、坐标和曲线保留为 PDF 对象。最终 PDF SHA-256 分别为 `63ab98afe1a69d04b24b0118d39adf028472d0b1caa17eb2f671583fc2b0b7bc` 与 `ce1e5e47efed77236448d5ba652164a185552117e37a379c610148716ff81c12`。

实际使用 `view_image` 查看生成 PNG，并另外查看由两个实际 PDF 在160 dpi下渲染的页面。首版图4的标题/图例及域外标注有重叠，已调整位置后重新生成并复看；最终图例、标题、色标、事件标签与坐标可读，没有发现裁切或相互遮挡。主论文中的图号、整页排版和交叉引用由主修订流程编译后核查，不以本子任务的独立图面检查替代。
