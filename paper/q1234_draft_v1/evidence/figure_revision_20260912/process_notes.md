# 第一、二问图形重绘记录

实际运行 `python -X utf8 paper/q1234_draft_v1/visual_revision/process.py`，退出码 0。两份冻结输入前后 SHA-256 不变，数据与输出哈希见 `process_sources.json`。

图1将原来数条时间剖面改为两幅时间—半径色图。左侧温度、右侧干基含水率均使用原始逐秒、21 个径向采样点；没有平滑拟合、阈值前沿或额外状态计算。原 300、600、1200、1800 s 的径向剖面包含在这些已存采样中。末时轴心/表面温度 33.5753/36.7856 °C、含水率 2.5500/1.5102 kg/kg 标注来自原数组；未舍入轴心含水率的解释继续留在正文，不能把显示 2.5500 误读为数学上不变。

图2左侧把轴心、表面分别放到温度—含水率坐标，标记 0.5、1.5、3 h，用箭头说明时间推进。为控制矢量文件规模，每 15 s 取一个已存状态，箭头也连接明确的原保存时刻；没有外推。右侧从原六个时刻中选取 0.5、1.5、3 h 的含水率径向剖面，继续支撑“初期失水集中于外层、随后内部降低”的空间论证。原温度径向曲线未再重复绘出，六时刻规定温度/水分表、正文温差及左侧轴心/表面状态仍保留；本轮没有删除温度梯度或热扩散相关数值结论。

新图2读取现行收尾交付的 `q23_unified.npz`，原图2读取旧 `q2_unrounded.npz`。独立审查对照六时刻、21 径向点的 126 格温度及 126 格含水率，四位显示全部一致，未舍入最大差分别约 4.16×10⁻¹⁰ 和 9.00×10⁻¹²；这是两份既有冻结轨迹之间的差别，本轮未修改任一数组或重算结果，不将二者称为字节相同。

图2底部 3 h 温差 0.1169 K、含水率差 0.7581 kg/kg 均由该时刻原轴心/表面值相减，脚本核对四位显示与原正文一致。这是保存数据的显示汇总，不是新情景结果。

两图使用统一字体、蓝绿/赭色、细网格和直接标注。实际查看最终 300 dpi PNG；图1调整色条与刻度间距，图2调整状态时刻标签位置，未见文字、图例和坐标裁切或明显重叠。整篇论文中的缩放、分页和图号交叉引用另由主流程编译核查。
