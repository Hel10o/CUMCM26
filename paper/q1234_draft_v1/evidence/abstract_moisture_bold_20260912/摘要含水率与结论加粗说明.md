# 摘要含水率与结论加粗说明

本轮基于已同步提交 `50e7b544aff9082c6c4cbfa7f9b226c98feaa1ec`，落实用户两项追加要求：问题一表面含水率、轴心含水率加粗；摘要每一问所得结论加粗。

## 实际修改

`main.tex` 仅新增六组 `\textbf{...}`：问题一的 **1.5102 kg/kg** 与 **2.55 kg/kg**，以及四问各自的结论句。第三问的保守方向及严格界限制、第四问的归因限制和待核验说明均随结论一并加粗。文字、数值、标点、换行及正文源文均不改。

重新编译并替换主论文 PDF，更新根 README、论文 README、源稿 ZIP 及其成员清单与 SHA256。此前各轮审核证据和独立 AI 使用详情文件保持原字节。

## 实际核验

- 移除本轮六组加粗命令后，完整主源文件与基线字节相同；独立代理复核亦确认只有六组插入。
- XeLaTeX / latexmk 完整编译退出码为 0；最终日志未发现未定义或重复交叉引用、缺字、溢出版心及待重编译提示。
- PDF 摘要提取文字去除空白后与基线相同；六处完整目标文字逐字字体检查通过，中文采用 SimHei，数字采用粗体罗马字体。
- 摘要 1 页，正文 27 页（物理页 2–28，含 AI 声明和参考文献），附录从第 29 页开始，总计 155 页。
- 全部 155 页按 72 dpi 渲染比对，仅第 1 页变化，正文与附录其余 154 页像素一致。
- 第 1 页另按 150 dpi 渲染并目视检查：四问结论及其限制、两项含水率均已加粗，无截断或重叠，摘要保持单页。

当前 PDF 哈希与检查数据见 [revision_check.json](revision_check.json)。当前 ZIP 哈希、成员文件数和逐项哈希见 [source_package.json](../source_package.json)，打包器实际执行 CRC 与成员 SHA256 校验。

## 未做或受限

未改任何数值结果，未运行 PDE 求解、参数情景或支撑代码。此次仅为加粗排版，没有重跑此前整套 37 项 PDF 与 26 项结构检查；上述结果仅指本轮实际执行的定向检查。模型适用范围与待核验限制没有变化，代理目视检查不代替队员人工审核。

目录内 `moisture_only_*` 保留用户追加“四问结论加粗”之前、仅两项含水率加粗的中间编译记录；当前版本以 `compile_*` 和 `revision_check.json` 为准。`prior_source_package.json` 为本轮前的源稿清单快照。
