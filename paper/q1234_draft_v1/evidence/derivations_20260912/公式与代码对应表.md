# 新增推导与实际代码对应

基线提交：`63d4ca00c75e4e907caeaa2136763a379754a588`。本表路径相对仓库根，行号指本轮实际读取且未修改的代码。新增公式是现行方法的推导和说明，不代表重新求解或另换模型。

| 论文位置及公式标签 | 方程或求解步骤 | 实际依据与核对事项 |
| --- | --- | --- |
| 第三节 `eq:material-balances`、`eq:heat`、`eq:moisture` | 材料控制体的干物质与水分收支，展开并消去干物质变化项 | 由本文已经声明的干基定义、材料相对扩散通量和有效热容量假设推导；热式仍缺潜热及组分携焓，不新增真实能量闭合主张。 |
| Q1 `eq:q1-pde`、`eq:q1-bc` | 常热物性参数代入与径向边界 | `q1_complete_delivery/q1_delivery/source/q1_solver.py` 的参数与 `RadialFV`；本问温湿场可分开，水分扩散仍非线性。 |
| Q1 `eq:q1-ring/flux/ode/balance` | 圆环积分、调和均值、内部通量相消及端点容量 | 同上，第 124–164 行的节点、面、权重及 RHS；轴心／表面不是去掉容量的代数点，温度边界系数为 `h/(rho*cp)`。 |
| Q1 `eq:q1-bdf` | 隐式残量、Newton 修正、解析稀疏 Jacobian | 同上，第 185–203 行及 `jac`；写出通用隐式结构，并明确实际 SciPy 变阶 BDF 有精度修正，不称为固定阶经典 BDF。 |
| Q2 `eq:q2-coefficients/radial/coordinate` | 附录3反馈及平方坐标变换 | `q4_complete_delivery/v1/q123_closeout/source/legacy_q3/q3_reference.py` 第 94–111 行；保持 `S^-1 div(k grad T)`，不改为 `div((k/S) grad T)`。 |
| Q2 `eq:q2-semidscrete` | 原函数通量和配点矩阵半离散 | 同上，第 99–111 行；代码实际采用 `q + X D_x q`，不是直接用 `D_x X q` 代替。求导时减去常量用于数值稳定，因矩阵行和为零而保持算子相同。 |
| Q2 `eq:q2-robin/surface-elimination` | 联立 Robin、消去表面温度、表面响应进入 Jacobian | 同上，第 61–93 行 `surface`；系数为 `2/R0`，温度因子使用当前表面温度，正浓度约束通过阻尼 Newton 处理。 |
| Q2 `eq:q2-radau` | 联立阶段方程与自适应联合积分 | `q4_complete_delivery/v1/q123_closeout/source/solve_unified_q23.py` 第 46–87 行；现行配置读取 `output/q23_unified.json`，为 N=80、Radau、rtol=2e-13。 |
| Q3 `eq:q3-extrema-candidates/reconstructed-max` | 单调原函数反演、端点与内部实驻点候选 | `legacy_q3/q3_reference.py` 第 18–20、39–50、115–123 行；代码在 Chebyshev 的 [-1,1] 变量上求导数根，论文等价地在 x∈[0,1] 表示。 |
| Q3 6.2 的节点事件流程 | 节点候选事件 → 根和执行点重构核验 | `solve_unified_q23.py` 第 58–60、72–79 行；实际 `event` 是内部节点最大值。另读取本机 SciPy `_ivp/ivp.py` 第 52–77 行核对步内插值与 Brent 括区求根。 |
| Q3 `eq:q3-local-event-scale`、`eq:q3-execution` | 局部误差尺度与向上量化执行时间 | `q3_refinement_delivery/output/end_event.json` 中既有根斜率、0.03 s 预算、0.36 s 量子、根两侧和执行点记录；一阶展开不构成严格误差界。 |
| Q4 `eq:q4-chain-rule`、`eq:q4-reference-pde` | 固定物理点导数与材料对流项抵消 | 由本文 `r=R(t)xi`、恒长度及均匀径向收缩假设用链式法则推导，接入 `q4_complete_delivery/v1/source/q4_spectral.py` 的当前半径系数。 |
| Q4 `eq:q4-x-operator/semidiscrete/discrete-robin` | 平方坐标、内部与表面几何因子、配点推进 | `q4_spectral.py` 第 65–116 行；内部用 `4/R(t)^2`，表面用 `2/R(t)`，均在当前时刻更新。 |
| Q4 极值检查及 `eq:q4-output-map` | 节点候选事件、原函数重构、映射至当前实际采样位置 | `q4_spectral.py` 第 120–129 行 `extrema` 及 `run_q4.py` 的事件／输出处理；域外不外推，当前表面另列。 |

公式标签中的斜杠简写表示同一前缀下的多个标签，例如 `eq:q1-ring/flux` 对应 `eq:q1-ring` 与 `eq:q1-flux`。正式 PDF 的自动编号及页码以 [derivation_check.json](derivation_check.json) 和当前 PDF 为准。
