# 模型假设重构与免责落位修订记录

## 1. 实际读取与基线

- 实际通过 GitHub 读取 main 根 README 和[模型指示文档](https://github.com/Hel10o/libai/blob/167a27455a5f2655cf07dc30471febcb67bd7a12/q1234_assumptions_handoff/模型假设重构_模型指示文档.md)，固定到提交 `167a27455a5f2655cf07dc30471febcb67bd7a12`，并以 GitHub raw 读回与该提交文件字节对照。两份远程读回的 SHA256 见 [baseline.json](baseline.json)。
- 起始本地 `main`、`origin/main` 与实际读取提交相同；手稿所述 `bfb04e8` 是前一轮写作基线，不冒充本轮 main。开始时无受版本控制的未提交变化；原有未跟踪 `paper/q1234_draft_v1/main.pdf`、`template/` 保留且不纳入本轮。
- 读取并遵循根 AGENTS.md。实际阅读当前 `problem_model.tex`、`validation_limits.tex` 的11.2/11.3/11.4和后段情景，`q3.tex` 的8.2及相邻终点/环境段，`q4.tex` 的材料映射、统一方程、主稿与现行打包入口。
- 初始正式 PDF 为158页：摘要1页、正文30页（物理页2–31，含AI声明与参考文献）、附录127页。交接文档中的“当前28页”已经过时。本轮按真实30页基线核验。
- 当前正文12个一级章不变。第三章为模型假设；限制/数据需求的标签分别为 `sec:limitations` / `sec:data-needs`，当前解析为11.3/11.4。源文回指一律使用标签，不写死小节号。

## 2. 假设条目改前改后

原6条与新8条的实际源码全文见 [assumptions_comparison.md](assumptions_comparison.md)。本轮将列表从“假定什么、采用目的、保护说明”的混合内容改为只陈述假设；每条保留粗体引导与冒号。

| 旧条目 | 重构后的落位 |
|---|---|
| 1：轴对称与端面简化，含降维目的和二维验证说明 | 新1明确均匀初态、轴对称、中截面径向解释和局部状态物性；验证工作留在第十章与11.3，不充当假设。 |
| 2：空气恒等映射，夹带C定义、用途与未标定免责 | 新4保留有效干基驱动和恒等映射；C定义移入符号表S1的C行，空气符号记号移至5.2，免责由11.3承接。 |
| 3：系数沿用，夹带题给值、目的与气膜系数免责 | 新3只写沿用及单一h_T处理；原h_T=25、h_m=8×10^-7带原单位移入5.2，系数不可直接互换的限制保留11.3。 |
| 4：有效热容量、干质量、用途与能量免责声明 | 新6保留有效容量、独立干质量及潜热/携焓/机械功未显式计入的披露；真实湿密度和真实能量闭合限制留在11.3。 |
| 5：固定几何/均匀径向收缩，含映射目的与可识别性免责 | 新7写静止骨架、同半径比例径向运动与恒长度；内部运动不可由外半径唯一识别保留11.2与Q4正文。 |
| 6：后段末小时均值，含补齐输入目的与选参声明 | 新8陈述分段线性插值与4h后末小时61点均值；原环境等式与eq:future留在最后一条，选参声明迁入11.4。 |
| 原列表未列出的本构/忽略项 | 新2补线性Robin通量；新5补相对干骨架仅扩散、无压力驱动和额外相对对流、空间均匀环境；新3补不另区分对流与辐射。 |

相对草案的三处措辞处理：新1写明径向位置与时间依赖，避免把“二维计算用于校核”作为假设；新5加“相对干骨架”，防止被误解为取消Q4材料导数中的运动；新7用“同比例”说明均匀径向收缩，避免误解为各位置有同一个径向位移。

题给定义和交换系数没有作为假设条目。实际S1原表只有“材料干基含水率”的名称，并未写出定义，因此本轮确实将 `C=m_w/m_d` 移入C行；符号表仍19行、每行一个符号。手稿草案列出的输入采样数量属于已有资料事实，不再塞入假设列表；这些资料没有被修改或删除。

## 3. 免责落位核对表

删除前先固定原条目和目的小节原文，见 [disclaimer_precheck.json](disclaimer_precheck.json)。原第6条的选参声明实际搜索了8.2与11.3/11.4；未找到承接该选参原则的原句，因此迁移，未直接删除。另发现问题分析中“并非题面指定位置”的限定也缺少集中落位，随题表解释的回指改动一并补入11.3。

基线行号固定对应上述提交，当前行号对应所列源码哈希。

| 基线原句 | 当前落点与结论 |
|---|---|
| `problem_model.tex:32`：它是未经材料标定的恒等映射，不能把$b$直接称为实测药材平衡含水率。 | `validation_limits.tex:90`（11.3）：未提供本药材标定值，未经标定边界情景不能称为实测；新假设4继续明确恒等映射。 |
| `problem_model.tex:33`：$h_m$属于材料干基驱动力标度，不自动等于气相浓度模型中的气膜系数。 | `validation_limits.tex:90`（11.3）：只有符合气固映射条件才可建立rho_d*h_m=k_g*K，不能直接交换系数数值。 |
| `problem_model.tex:34`：不同时强制$\rho/(1+C)$为真实干密度。 | `validation_limits.tex:97`（11.3）：原湿密度解释公式、不相容性说明及完整量化反例覆盖该限制。 |
| `problem_model.tex:34`：不能据有效热方程宣称真实能量闭合。 | `validation_limits.tex:99`（11.3）：相变能量缺口尚未闭合，不得据有效模型推断真实热量收支或执行保证。 |
| `problem_model.tex:37`：内部仿射运动是额外假设，单凭外半径不能唯一识别。 | `validation_limits.tex:77`（11.2）：内部运动仍需独立依据，仅有外半径不能唯一确定内部变形；Q4第25–35行亦保留。 |
| `problem_model.tex:43`：这是观测范围外的环境延拓假设；使用其他后段工况会形成不同情景，不因某情景更接近通常2--3天而选择其参数。 | `validation_limits.tex:110`（11.4）：迁入11.4工况段，将‘这是’明确为eq:future，其余原句含2--3天选参限制保留。 |
| `problem_model.tex:27`：题表距离采用中截面径向解释，并由二维对照检验，不视为题面已指定的位置。 | `validation_limits.tex:83`（11.3）：等义迁入11.3开头，保留二维校核及并非题面指定的位置；第二章使用ass:midsection动态回指。 |

前五处由既有11.2/11.3覆盖；环境选参与中截面题意归属两句等义迁入。环境句仅将原‘这是’的指代改为eq:future，其后包含2--3天选参限制的原句连续文本完整保留；几何句压缩措辞，二维校核及并非题面指定的位置均明确保留，不宣称两句全文逐字相同。第六条假设仍明确披露未显式计入蒸发潜热、液态扩散携焓和机械功，没有改成潜热较小、已被比热吸收或真实能量闭合的主张。

## 4. 配套排版与原文保护

- 问题分析末段改为 `题表距离的空间解释见假设\ref{ass:midsection}。`；其后有限圆柱全域终点不能由一维事件直接推出的保护句原文保持。
- `eq:future` 仍位于第三章最后一条、符号说明之前，当前源码第50行、PDF物理页4、等式(1)。原10.2引用保留，新11.4的指代也用 `\eqref{eq:future}`；定义1处、引用2处。
- 删除旧条4/5间 `\Needspace{4\baselineskip}`。新版条1–6完整位于页3，条7–8完整位于页4，无需该强制留页。符号表前22行、统一模型章前7行以及5.2前15行的Needspace保留，实测表头与表格同页。
- 参数移入5.2后，将S2局部改为 `[H]` 并在分组内设 `\intextsep=6pt`；题注、单元格、字号和公式均不改。11.3/11.4分组内设 `\parskip=0pt`，仅去掉额外段间留白，保留首行缩进、粗体引导、原字号和行内行距，结束分组后恢复原全局设置。
- 初次预览正文31页，未把它报告为通过。通过精简新增几何声明、把环境声明融入11.4既有工况段，以及上述局部留白调整，最终正文恢复30页。没有以删除量化段、结论、文献或省略限定换页数。
- main.tex、四问正文、全部图像、六张规定表和其余章节原始字节保持；原11.3从气固边界起的全部内容原始字节不变，包含45.59、4.801、9.50、164.10、0.71952、0.89029、34.7454、28.0807的完整原段逐字核对。原显示公式块不变，所有题给系数保持。

## 5. 实际核验

[九项清单](consistency_checklist.md)与[JSON](consistency_checklist.json)逐项对应交接文档第5节，9项均完成。支持证据包括：

- [完整PDF检查](pdf_validation.json)：37项通过、2项信息说明，六表279个显示单元格全部一致。
- [独立语义审查](semantic_audit.md)：21项静态检查通过，含八条假设、免责落位、公式/11.3原文/参数/引用检查。
- [源文与编译产物核验](source_integrity.json)：正文12章、标签与引用有效；四组禁用措辞在主稿/章节/表源码和完整158页PDF均零命中；摘要主稿字节和页面像素保持，AI声明仍在参考文献之前。
- [页面核验](page_integrity.json)：全158页按108dpi同渲染器比较，含页脚，仅物理页3、4、6、29、30、31变化，其余152页完全相同，包含摘要及全部127页附录。
- 变动6页另用Poppler输出1600像素图逐页目视。主代理检查3/4/6页：假设编号、粗体与条目分页、环境等式、S1定义、S2参数/公式无裁切重叠；独立代理检查29/30/31页，见 [尾部目视记录](visual_review_tail.md)。结论第二段由页30首行延续到页31，跨页现象如实保留记录；页脚分离，文献1–7完整。未伪称其余152页又作了逐页人工审核。

实际编译/核验命令（在论文目录）：

```powershell
latexmk -xelatex -interaction=nonstopmode -halt-on-error -file-line-error -outdir=build/assumptions_restructure body_preview.tex
latexmk -xelatex -interaction=nonstopmode -halt-on-error -file-line-error -outdir=build/assumptions_restructure main.tex
python -X utf8 validate_paper.py --pdf build/assumptions_restructure/main.pdf --aux build/assumptions_restructure/main.aux --output evidence/assumptions_restructure_20260913/pdf_validation.json
python -X utf8 build/assumptions_restructure/check_revision.py
```

实际读取源码、常规PDF校验之外的比较脚本仅用于静态文本/哈希/像素核对，不调用求解器；脚本内容见 [verification_script.md](verification_script.md)。其一级标题计数范围排除附录的独立标题：正文12章，含附录则13个带编号section命令。最终日志无未定义引用、重复标签、缺字或Overfull，详见 `full_compile_console.txt`。AI使用详情PDF作为既有辅助文档由校验器读入，本轮没有重编该独立文档。

## 6. 交付与未做事项

正式PDF已替换为最终编译结果：158页（摘要1、正文30、附录127），SHA256：`77199330726ac83dfde71e380116d7b37741f4517b1dcbe1ee304cddcd68bce1`。根README、论文README、交接README均增加本轮入口；原交接指示文档与历史修订证据原字节保留。

源稿ZIP及外置清单按本轮源码和证据重建。ZIP自身哈希、成员校验与目录一致性见外置 [source_package.json](source_package.json)；该同名核验文件按既有打包规则不进入ZIP，避免自引用哈希循环。当前论文目录的总清单为 `evidence/source_package.json`，ZIP旁有SHA256文本清单。

本轮未运行PDE、参数情景、重绘脚本、数值再标定或潜热修正，没有新增物理验证、连续二维严格界或实际执行保证。原第6条选参声明与题意归属限制均已找到新落位后保留，不能把它们当作重复噪声删去。本轮为代理编辑与核验，未取得参赛队员逐项人工审核，不作最终参赛内容真实性认证。

GitHub同步遵循AGENTS.md，不强制推送；最终提交号和远程一致性以本轮交付回复为准（不把提交号写入其自身版本化内容）。
