# 模型假设条目去加粗修订记录

日期：2026-09-13。本轮仅去掉第三章“模型假设”六条条目的加粗，不改文字、公式、数值与图表，不重跑任何求解。

## 实际读取

本地与远程 main 均为 `d5787b1c21856049f6e234588cad389587b7f1eb`，工作区除未跟踪的 `main.pdf`、`template/` 外干净。读取 `sections/problem_model.tex:29-45` 的假设列表、`build.ps1`、`validation_paper.py` 入口。

## 实际修改

`sections/problem_model.tex` 的六条假设，去掉每条起首的 `\textbf{…}` 包裹，文字一字未改：

|条|原|现|
|---|---|---|
|1|`\item \textbf{假设药材为轴对称连续介质，初始温度、干基含水率及干密度在空间上均匀分布。}\label{ass:midsection}`|`\item 假设药材为轴对称连续介质，初始温度、干基含水率及干密度在空间上均匀分布。\label{ass:midsection}`|
|2|`\item \textbf{假设烘房环境在空间上均匀，…}`|`\item 假设烘房环境在空间上均匀，…`|
|3|`\item \textbf{假设药材表面的传热、传质通量…}`|`\item 假设药材表面的传热、传质通量…`|
|4|`\item \textbf{假设烘干过程中药材干物质质量守恒…}`|`\item 假设烘干过程中药材干物质质量守恒…`|
|5|`\item \textbf{前三问假设药材尺寸保持不变…}`|`\item 前三问假设药材尺寸保持不变…`|
|6|`\item \textbf{假设4\unit{h}后烘房环境保持稳定…}`|`\item 假设4\unit{h}后烘房环境保持稳定…`|

假设列表不含其他加粗（`sed -n '29,45p'` 内 `textbf` 计数为 0）。`\label{ass:midsection}` 保留原位，其两处引用（`problem_model.tex:27`、`:85`）未受影响。

## 实际核验

|项|结论|证据|
|---|---|---|
|字体|假设六条由 SimHei（加粗）改为 SimSun（正体），逐条抽验通过|PDF 第 3 页 文本 span 字体|
|编译|`build.ps1` 中论文编译成功，158 页，`build/main.xdv → build/main.pdf` 写出 1811971 字节|`build/compile_assumptions_bold.log`|
|页数|158 页（摘要 1、正文 30、附录 127），与基线一致；正文仍在上限内|`pdf_validation.json`|
|PDF 检查|37 项通过，六张规定表 279 个显示单元格全部匹配|`pdf_validation.json`|
|页面像素|72 dpi 逐页比较：**仅物理页 3 变化**，其余 157 页像素一致|`page_integrity.json`|
|交叉引用|`假设\ref{ass:midsection}` 两处仍可解析，编译日志无 undefined reference|编译日志|

## 过程异常（已核实为无害）

本轮 `build.ps1` 退出码为 1，原因在其最后一步：复制 `AI工具使用详情.pdf` 时该文件被其他进程占用（`Copy-Item : The process cannot access the file … because it is being used by another process`）。论文编译与 `四问论文初稿_v1.pdf` 的复制均在此之前完成。经哈希比对，`AI工具使用详情.pdf` 与 `build/ai_details.pdf` 完全一致（`dedf1eed…`），本轮也未改 `ai_details.tex`，故该步失败不影响任何交付内容。建议关闭占用该 PDF 的阅读器后再跑 `build.ps1`，或忽略该步。

## 未做或受限

- 未改任何数值、公式、图表、摘要、结论与其余正文；未重跑求解器或参数情景。
- 正文与部分图注仍保留“第N问”表述，与上一轮相同，本轮未统一。
- 未做队员人工审核。
