# 问题一图片字号与底色修订记录

## 读取基线与范围

2026-09-13 开始时，本地 `main` 与实际 fetch 后的 `origin/main` 均为 `c516364c6934721b07c10533d9e878cff3db0aff`。读取当前论文、问题一源稿、绘图脚本和冻结数组证据。原有未跟踪的 `main.pdf` 与仓库根 `template/` 未修改或提交。

用户本轮要求：修复6.3图片字号太小和标题居中问题；追加要求去除问题一流程图框内底色。仅修改上述两幅图片的排版或填色，以及论文的两处图片路径与配套入口。全部正文文字、图注、公式、表格、模型结果和一级标题均保持。

## 实际修改

热图画布保持16.4×7.1 cm，论文仍以 `0.94\textwidth` 插入，实际约14.10×6.10 cm。此前子图标题已对齐热图区中心，但色标说明为左对齐；本轮将两类文字都居中，同时扩大字号：

| 图中文字 | 原源图字号 | 新源图字号 | 新成稿字号约 |
| --- | --- | --- | --- |
| 刻度、轴心/表面与末值标注 | 9 pt | 11 pt | 9.46 pt |
| 坐标标题 | 9.2 pt | 11.3 pt | 9.72 pt |
| 色标说明 | 9 pt | 11.3 pt | 9.72 pt |
| 子图标题 | 9.6 pt | 12.2 pt | 10.49 pt |

为容纳较大文字，仅重新分配图内留白和绘图区位置；原数值网格、坐标范围、色标范围、配色、nearest着色方式和末值字符串保持。两行末值文字在各自半幅内居中。热图色块仍为600 dpi栅格层，其余文字和坐标为矢量；中文宋体、英数 Times New Roman。

问题一流程图的7个彩色框改为白底。画布15×3.2 cm不变，完整文字及位置不变；26个PDF绘图对象中只有7个框的填充色改变，边框、线宽、箭头及位置均保持。

新图位于 `figures/q1_legibility/`，绘图入口 `build_q1_legibility.py`。原图、原脚本和历史证据不覆盖。

## 实际核验

- 直接核对已保存两场1801×21数组、绘图网格、色阶及末值字符串，全部匹配上一轮。输入NPZ SHA256仍为 `891a9520497b9b831bf7fe4917a95ec8efd346cc62fd9c6c217ca4906046fe77`，见 [figure_check.json](figure_check.json)。
- 检查四个标题/色标说明的中心、全部文字边界、底部两幅标注不碰撞，以及PDF实际嵌入字体。
- XeLaTeX/latexmk 完整编译退出0；未见未定义引用、重复标签、缺字、Overfull或LaTeX错误。日志见 `compile.log` 与 `latex.log`。
- 仍为158页：摘要1页、正文30页、附录127页。与基线PDF逐页72 dpi像素比较，仅物理第7、10页改变，其余156页完全一致。
- `q1.tex` 仅两处图片路径改变；其余主文件、各章TeX及全部结果表TeX逐字节匹配基线，见 [final_check.json](final_check.json)。
- 实际查看两张图片及最终PDF物理第7、10页。图中无文字重叠或裁切，流程框全部白底，标题及色标说明居中，刻度和末值可读。独立只读审核与本次PDF哈希一致。
- 更新README和图表清单，重建源稿ZIP并逐成员核对磁盘内容、内部清单及SHA256；最终包信息见同目录的 `source_package.json`。

最终论文PDF SHA256：`e01b23daa501dfc97871f8def2d29f7780e677a5485274cafe622d56cab36031`。

## 未做

未运行任何PDE、参数情景或重新生成数值工作簿；未修改数据或科学结论。本轮执行与两图修改直接相关的检查，没有重跑上一轮整套联合PDF/AI说明检查，也不将其既有结果称作本轮通过。
