# 图11与图12字号核验

基线：`4f09fdad4182b6f220e801af54dffd0be37ad04c`。

本次仅处理图11 `dimension_reduction` 与图12 `environment_sensitivity` 的字号和图内排布。独立生成脚本为 `paper/q1234_draft_v1/build_legibility_validation.py`，新图位于 `figures/legibility_validation/`。未覆盖旧图、旧证据或原绘图脚本，未修改正文、图注、TeX、README、压缩包，也未执行任何 PDE 求解、积分或参数情景。

## 字号与排布

参考图3正文显示字号：刻度、注释约9.46 pt，轴标约9.72 pt，标题约10.49 pt。按当前正文插图宽度与16.4 cm原图宽度折算：

| 图 | 正文宽度 | 新刻度/注释 | 新轴标 | 新分图标题 | 原图尺寸 |
|---|---:|---:|---:|---:|---:|
| 11 | 14.7 cm | 9.50 pt | 9.77 pt | 10.49 pt | 16.4 × 9.0 cm，保持 |
| 12 | 13.95 cm | 9.53 pt | 9.70 pt | 10.55 pt | 16.4 × 6.2 cm，保持 |

图11总引导行折算约10.04 pt，公式上下标保留自然缩放。中文使用宋体，英文、数字及数学变量使用 Times New Roman；PDF嵌入字体已用 PyMuPDF读出核对。

图11移动总引导行、三栏标题与单位标注，调整上下排留白和下排类别标签距离，避免放大后文字相碰；图12调整图例、脚注及配对数值注释的非数据偏移。数据点的位置、差值折线、坐标范围、刻度与颜色全部保持。

## 实际执行的核验

1. 两图在字号修改前的完整绘图签名与 `evidence/figure_fonts_20260912/font_content_check.json` 中的已验收记录完全一致。
2. 修改前后逐项比较全部文本、数字、曲线数组、散点坐标、坐标轴范围/刻度/尺度、颜色、画布尺寸，完全一致。比较仅排除有意变更的字号、文字锚点及轴区矩形。
3. 原绘图脚本的断言均执行：6个整根平均、3个失水增加百分数、8个情景根与8个差值均与冻结值一致；三组配对半径及目标快照检查通过，情景共享初态哈希检查通过。
4. 读取的18个输入及历史证据文件在执行前后哈希不变，详见 `validation_sources.json`。
5. 全部可见文字边界框均在画布内；任意两个可见文字边界框无超过0.5像素的双向重叠。
6. 已打开最终PNG进行逐图视觉检查：三栏标题、量级单位、上下排分隔、三组平均值与百分数、情景图数值及脚注可辨认，无裁切或文字遮挡。
7. 输出PDF宽高与原尺寸相符；字体读取仅含宋体、Times New Roman正体及数学所需斜体。数学上下标小于正文主字号是有意保留的公式排版。

机器证据见 `content_and_legibility_check.json`（含前后签名、文本位置、字号、边界框及脚本哈希）与 `validation_sources.json`（含输入来源、输出哈希及原数值断言）。

未在本子任务中执行论文编译、正文页数核对、全稿视觉检查或压缩包校验；这些由主任务集成后完成。
