# 图8与图10字号核验说明

本轮绘图读取基线为 `4f09fdad4182b6f220e801af54dffd0be37ad04c`。新增脚本 `build_legibility_process.py` 复用既有 `visual_refinement/geometry.py` 的绘图与已存结果读取逻辑，不覆盖历史图和证据，不运行 PDE 或参数情景。

## 实际修改

- 图8 `q3_q4_drying`：刻度、图例及说明文字放大；根和执行点说明、执行最大值改为分行显示，并整理标题、图例和下方说明间距。画布仍为 16.4×8.0 cm，正文插入宽度仍为 13.95 cm。
- 图10 `q4_shrinkage`：放大刻度、色标、图例、标注和标题；剖面图例改为四项单行排列；阈值 `0.15` 标注移至相应虚线右端外侧，避免覆盖曲线；整理条件对照标题与两条结果说明的间距。画布仅高度由 9.2 cm 增至 10.6 cm，宽度仍为 16.4 cm，正文插入宽度仍为 13.8 cm。整篇最终页数由主任务编译后核验，本子任务未独立编译论文。
- 中文仍为宋体，英文和数字仍为 Times New Roman；不更改结果数值、轴范围、刻度值、线条数组、散点数组、色标范围、配色和图中文字内容。允许的文字变化仅为空白和换行位置。

## 实际核验

1. 已运行新增脚本，输出两图各一份 PDF、SVG 和 PNG，共六个正式图文件。
2. 七份冻结数据及元数据来源在绘图前后 SHA-256 均相同。来源与哈希见 `geometry_sources.json`。
3. 逐图比较原绘图对象与字体修改后的对象：所有图线数据、散点和网格几何数组、颜色范围、坐标范围与显示刻度一致；忽略换行和空白后所有文字条目一致。画布高度和文本、图例及坐标轴布局位置是本轮明确允许的变动。具体签名及断言结果见 `legibility_process_check.json`。
4. Matplotlib 渲染边界检查未发现任何文字越出画布。该检查是画布边界检查，不替代视觉检查。
5. 已目视查看两图最终 PNG：根与执行点说明可区分；图10上排图例、执行标注、下排标题、单行剖面图例、条件对照与底部说明不存在前几次迭代的文字重叠。早期失败迭代的临时 `*_before.json`、`*_after.json` 已清理，没有作为最终证据保留。
6. 使用 PyMuPDF 直接读取最终两图 PDF：嵌入文字字体仅为 `SimSun` 与 `TimesNewRomanPSMT`。按正文插入宽度换算，图8实际文字约 9.697—10.505 pt；图10约 9.593—10.518 pt，与参考图3的约 9.46—10.49 pt 接近。实测值见 `pdf_font_check.json`。

本轮仅为已保存数据的图形后处理，没有进行新的数值计算验证或物理模型验证。
