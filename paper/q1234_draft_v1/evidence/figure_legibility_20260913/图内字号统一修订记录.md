# 图内字号统一修订记录

## 实际读取与范围

开始时工作区干净，读取本地和 origin/main 提交 `4f09fdad4182b6f220e801af54dffd0be37ad04c`，fetch 后两端一致。以现稿6.3图3为实物字号参照：刻度/注释约9.46 pt、轴标签9.72 pt、标题10.49 pt。本轮只调整图片字号及为排版所需的图内位置，不改模型、数值、图注或读图段，不运行PDE或参数情景。

工作期间另一个任务完成AI说明精简并提交 `285f3d984190b0655e1a46b8cfffe91b656cc7ea`，本轮保留该提交；支撑ZIP的AI成员由原707f5590…基线到8409a3c6…基线之外93个成员逐字节一致，见支撑包的 concurrent_ai_isolation.json。随后出现未提交的题面引用删除，修改了5处 cite 和参考文献条目。为保证本轮交付范围可核查，使用已提交的285f3d9论文源稿加本轮10个图片路径建立独立编译/打包副本；该并行文字修改及其核验文件留在工作区，不由本轮提交或覆盖。完整编译源哈希见 compile_source_snapshot.json。

## 实际修改

| 图片 | 正文中的主字号 | 调整 |
| --- | --- | --- |
| 图1、2、4、9 | 10—10.5 pt | 放大流程、边界文字；流程框白底。Q4两圆角输入框略增宽高以保内边距，节点中心和连接逻辑不变。 |
| 图5 | 9.50—10.49 pt | 放大坐标、色标、等值线与底部注释；底部两行略下移。 |
| 图6 | 9.5—10.5 pt | 放大机制图的坐标、图例和峰值标记文字。 |
| 图8 | 9.70—10.51 pt | 原约6—7 pt；放大后重排图例、根/执行点及最大值注释。 |
| 图10 | 9.59—10.52 pt | 原约5.5—7.7 pt；重排图例、剖面与条件对照。画布由16.4×9.2增至16.4×10.6 cm，插入宽度仍13.8 cm。 |
| 图11 | 9.50—10.49 pt | 原约6.3—7.8 pt；整理三栏与下排平均值、百分比位置。 |
| 图12 | 9.53—10.55 pt | 原约6—7.7 pt；放大情景标签和数值注释。 |

图3参照图和已为10 pt的图7均保留原字节。以上是主体字、轴、标注和标题字号；数学上下标自然更小，未将其误记为主体小字。中文继续嵌入宋体，英文/数字Times New Roman。除图10高度及Q4圆角框内边距外，各图画布尺寸不变。正文只替换10个 includegraphics 路径，插入宽度、正文格式指令与文字全部保留。

新脚本为 `build_legibility_schematics.py`、`build_legibility_q2.py`、`build_legibility_process.py`、`build_legibility_validation.py`。原脚本、旧图和历史证据保持。分别在 schematics、q2、process、validation 子目录记录图形/文本/数组签名、来源哈希、文字边界与最终输出哈希。

## 实际核验

1. 原图3实测字号作为参照，12张当前图片按实际LaTeX插入比例核算，见 font_sizes_in_paper.json；10张主体字均约9.5—10.5 pt。
2. 重绘前后数据、曲线、散点、刻度、范围、文字与数字签名一致；部分说明仅换行。数值数据源哈希未变，图11的7.2533%、4.7118%、2.7379%保留。
3. 所有新图PDF已核对宋体/Times New Roman，主体文字无画布越界，流程图文字在框内；图例、标题与说明经渲染目视调整。
4. 独立副本用TeX Live 2026 XeLaTeX/latexmk编译，最终成功；日志无缺字、未定义引用、重复标签或Overfull警告。首次相对输出参数被PowerShell拆分而失败，改用明确输出目录后成功，未把失败尝试记为通过。
5. 论文专项检查25项通过、1项字体分布信息；全文158页，摘要1页、正文30页、附录127页，交叉引用及六表279格核对通过，见 paper_validation.json。未重新审核独立AI说明真实性，不冒称全项目验证。
6. 与起始论文逐页72 dpi像素对照，只有第7、8、11、14、15、19、20、25—31页变化，共14页；其余144页相同，摘要及全部附录像素一致。末部换页由图10加高引起，正文文字不变。
7. 对改变图页渲染核查，并补看末部分页，见《最终图页视觉核查.md》。支持包已刷新10图与来源清单，12图和5个数值成员核对见支撑目录记录。
8. 源稿ZIP以本轮独立交付副本生成，包含新图与4个重绘入口；成员清单、哈希及与暂存提交一致性在最后打包后记入本目录 source_package.json（该文件按既有规则不进入ZIP，避免自引用）。

## 未做与边界

没有重新求解PDE、参数情景、拟合或导出正式数值工作簿；没有新增模型验证结论。本轮只确认图形数据与现有结果一致及论文编译排版。未提交的并行题面引文改动继续保留在工作区；本轮发布PDF与源稿ZIP对应独立副本，重编整个工作区会包含那些并行修改。
