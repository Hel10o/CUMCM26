# 第六章文字与图示修订记录

## 实际读取与授权

- 2026-09-13 本地 `main` 与实际 fetch 后的 `origin/main` 均为 `0b402a4b372c8f46cebc74abe0b63186a805d520`。读取当前根 README、AGENTS.md、论文源稿、第六章 Word 方案及两张用户示意图。
- 用户先审阅修改建议与待审稿，随后明确要求“你来完成修改”。本轮仅落实该第六章方案及其必要的图文、分页、文档和打包同步，不采纳 Word 内的其他章节草稿。
- 用户文件哈希、旧 PDF 与 Q1 源稿哈希见 [baseline_inputs.json](baseline_inputs.json)；Word 第六章内容见 [原方案摘录](用户第六章方案摘录.txt)，新旧源稿全文见 [chapter_comparison.md](chapter_comparison.md)。
- 开始时已有未跟踪的 `paper/q1234_draft_v1/main.pdf`、`template/`，未修改或纳入本轮提交。

## 实际修改

1. 章首增加由统一模型、问题一物性到径向温湿预测的衔接，明确常热物性不等于常扩散率。6.1 补明固定骨架、初始干密度均匀和干物质守恒的约化前提。章节、公式、假设及图片均用交叉引用。
2. 按用户图形方案重绘问题一双支路流程图，输出框表示 0–1800 s 求解时域；正式工作簿仍从第1秒逐秒输出，未改变输出契约。
3. 新增中截面与径向边界示意图，区分热量传入、水分迁出，保留轴心对称、表面 Robin 条件及有效边界值定义。图中箭头只示意本问预热工况。
4. 问题一热图仅以已有数组重绘。温度/含水率两个 1801×21 数组、nearest 网格坐标、坐标及色标范围、四条末值标注均与旧图证据一致；扩大字号、加深文字、居中子图标题。热图色块沿用高分辨率栅格层，文字及坐标为矢量，避免密集矢量网格白缝。中文宋体、英文和数字 Times New Roman。
5. 6.4 改为“数值验证与适用范围”，删除 Word 方案中重复的结果总结及对一般“远离端面区域”的扩大表述；保留全部数值、舍入核对声明、中截面证据范围及两句物理适用限定。没有新增求解或新的验证结论。

原式（9）起至6.3末的方程、科学文字与结果保持；仅更新热图文件路径及局部排版。主文件中的摘要与结论、其他章节的科学文字、表格、已验收旧图与数据保持。新图及脚本位于 `figures/ch6_revision/` 和 `build_ch6_assets.py`，旧图和旧证据不覆盖。图形输入、数组比较、嵌入字体与实际渲染记录见 [figure_sources.json](figure_sources.json)。

## 分页处理与待审稿的差别

初次完整编译为159页（正文31页），不满足原页数要求。最终仍为158页。没有改变全局字号、正文行距、页边距和一级标题样式；具体处理如下：

- 对待审稿新增的引导语压去重复过渡词，保留模型定位、条件、求解目标和干密度约去依据。所有科学数值不变。
- 第六章在独立分组内将段间距设为0 pt、主要显示公式上下间距4 pt、图表浮动间距8 pt、题注间距5 pt，两张表的行高系数设为1.0；其正文字号、正文行距及表格字号不变，章末恢复原设置。标题、图、公式与表格实际渲染均已检查。
- 新分页触发第四问旧的18行预留，造成约半页留白。仅把该处预留改为6行，并移除紧接收缩图的浮动屏障，让解释文字正常接续；在 PCHIP 段前增加3行预留，避免图隔开“临界时间为”与数值。第四问的文字、数值、公式、图和图注全不改。
- 参考文献条目间距调整为1 pt，段间距和条目内部段间距为0；文献文字、链接、顺序和字号不变，避免末条文献的两行单独溢页。

以上不是其他章节整文件字节不变；独立审核对这些精确布局变化单列豁免，并验证其余内容一致。

## 实际核验

| 核验项 | 实际结果与证据 |
| --- | --- |
| 1. 基线与修改范围 | 基线本地/远程一致；只更改批准的第六章内容、新图、必要分页和同步入口。 |
| 2. 方程与结果保护 | 44个显示方程及编号保持，式（9）至6.3科学内容保持，原六张结果表保持。见 [独立审核](independent_audit.json)。 |
| 3. 6.4 数字与边界 | 数字、数学表达、舍入保护、中截面限定、有效边界/参考密度/无显式潜热说明保持。 |
| 4. 作图数据 | 全部两场数组及网格哈希匹配历史绘图证据；坐标、色标、nearest 着色和末值不变，输入文件前后哈希相同。见 [图形来源](figure_sources.json)。 |
| 5. 图内字体与可读性 | 检查三张 PNG、实际 PDF 渲染及嵌入字体；中文宋体，英数 Times New Roman，无文字溢出。 |
| 6. 编号与引用 | 正文12个一级章保持；10图增至11图，原图2及以后顺延；公式编号不变，未见未定义/重复标签、缺字或 overfull 日志。 |
| 7. 完整编译与页数 | XeLaTeX/latexmk 完整编译退出0，158页＝摘要1＋正文30＋附录127。见 [compile.log](compile.log)。 |
| 8. PDF、表格与目视 | 论文相关自动检查通过，六表279格全匹配；物理7–31页实际查看，最终正文预览与完整PDF前31页逐页像素一致。摘要与全部127页附录像素不变，见 [页面核验](page_integrity.json)、[目视记录](visual_review.md)。 |
| 9. 源稿包与清单 | 使用更新后的既有打包器，纳入新脚本/图及本轮记录，验证ZIP成员、磁盘文件、内外清单及哈希。实际计数与结果见 [source_package.json](source_package.json)。 |

联合 PDF 检查实际为 **36通过、2信息、1失败**，不能写成全套通过。唯一失败是本轮未改的独立 `AI工具使用详情.pdf` 第3页一行文字超25 mm边距框2.046 pt，而既有脚本容差为2 pt。该文件与基线字节完全相同，证据见 [pdf_validation.json](pdf_validation.json)、[既有AI边距项](preexisting_ai_margin.json)。检查显式使用正式AI说明PDF，没有误用 `build/ai_details.pdf` 中的旧编译文件。

最终论文 PDF SHA256：`3879db2d5619b14cc86668935051bc8a109fd45f773a792445bce3041aef08b6`。正式 PDF 与 `build/main.pdf` 复制后哈希一致。

## 未做或受限

- 未运行任何 PDE、参数情景或重新生成正式数值工作簿；未修改已验收数值。
- 未将 Word 内其他章节内容覆盖当前论文；未修改用户原 Word 或原始图片。
- 本轮未修改独立AI说明PDF或掩盖其既有边距检查失败。
- 排版与数值一致性检查不构成真实物理过程验证或参赛队员全文人工验收；没有新增连续二维严格界或实际执行保证。

根 README、论文 README、图形入口与支撑材料来源清单已更新；源稿ZIP和清单随本轮重建。Git同步结果以本轮提交及远程读回核对为准。
