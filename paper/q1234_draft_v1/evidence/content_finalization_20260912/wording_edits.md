# I1 与 I4 分问源稿编辑记录

日期：2026-09-12。比较基线：`9c26eb4b13a236326e6605106934e35325c1a748`。本专项实际通过本地 Git 对象读取基线四问源稿，并读取当前内容改进合同全文、结构修订记录及 `q1.tex`、`q2.tex`、`q3.tex`、`q4.tex` 全文。GitHub 在线读取与 SHA 确认由主任务单独记录，本专项不冒称另行在线读取。

授权修改范围仅为上述四个 TeX 文件和本记录。没有修改摘要、结论、其他章节、模型、表格、数组、工作簿或求解器；没有运行 PDE、参数情景、数值后处理或工作簿导出。本专项冻结源稿后，由主任务进行编译及最终页面核验。

## 1. 合同冲突与执行优先级

合同 I1 要求压缩若干指定段落、章末限定句不超过两句，同时硬边界要求保留含具体数字的整句及保护性声明。实际原文中：

- Q2 账本解释与 `3.11×10^-11` 残差在同一个完整句内。
- Q3 “保守方向—继承前史—非连续严格界—非精确二维最短时间”由分号、逗号连接成一个含 `57.4741 h` 的完整句。
- Q4 “一维条件执行值—保守方向—精细二维待核验”也是含 `51.0921 h` 的完整句；紧随的“不推翻精细主解、未取得连续保证”属于明确保护声明。
- I4 又明确要求 Q1、Q2 末尾各追加一句相变量级/限制引用，原限定句保持。

因此本专项保留上述整句原文及关键保护范围，仅压缩其他无数字重复说明、使用交叉引用集中定位。没有通过改句号、分号或缩窄统计范围来制造“不超过两句”的结果。I1 的硬性句数目标未完全满足，见第 4 节；不能将本记录解释为 I1 所有指标通过。

## 2. 实际删改对照

以下位置均相对 `paper/q1234_draft_v1/sections/`；引用使用标签，最终节号由主任务的章节重排自动生成。

### q1.tex

4.4 既有物理不确定性声明逐字保留，在其后仅追加 I4 句：

> 本问预热段的相变能量缺口量级显著，相关诊断与解释范围见第\ref{sec:limitations}节。

没有复制潜热反例数字，没有声称跨问/跨时段的绝对最大影响，没有给潜热耦合后的温度或时长偏差定方向。

### q2.tex

5.5 无具体数字的情景说明由：

> 这是人为设定的确定性情景，不是参数置信区间，也不能用它替代空气量与材料平衡含水率的基准识别\cite{dasilva2014,adrover2020}。

改为：

> 该对照为人为情景而非参数置信区间（见第\ref{sec:environment-scenarios}节），不能替代气固水分基准识别\cite{dasilva2014,adrover2020}。

保留人为设定、非参数置信区间、不能替代气固基准识别三层限制及原文献键。所指环境小节的通用情景解释不意味着 Q2 的交换系数对照变成后段温度情景；原对照的参数、网格及数字整句原样保留。

5.6 账本解释整句、导出一致性不等于逐秒独立求解的整句、几何检验数值及其解释均保留。在小节末尾追加 I4 句：

> 本问早期大通量阶段亦受相变能量缺口约束，相关量级诊断与闭合限制见第\ref{sec:limitations}节。

该句仅定位现有相变量级及闭合边界，不把其他问的量化反例当作 Q2 新算结果，不给真实时长变化定方向。

### q3.tex

6.3 经验余量不包含环境、边界映射及物性假设不确定性的原句保留。6.4 含 `57.4741 h` 的完整证据限定句原样保留，其后追加：

> 上述证据层级集中见第\ref{sec:limitations}节。

末段原无数字句“这是人为设定的单因素情景，不是观测误差范围。”改为：

> 该单因素情景为人为设定而非观测误差范围（见第\ref{sec:environment-scenarios}节）。

原温度情景数字、末尾 `57.4741 h` 的工况绑定及不能给真实工况确定性保证的整句不变。

### q4.tex

7.2 在相同有效热方程和两类密度定义之后，重复说明由：

> 热式仍为无显式潜热的容量型有效方程，不能仅凭数值积分闭合将其解释为真实组分总焓守恒。

压缩为：

> 热式仍无显式潜热，数值闭合不构成真实组分总焓守恒的证明（见第\ref{sec:limitations}节）。

无显式潜热和“数值闭合不等于真实总焓守恒”的保护含义完整保留；公式和相邻密度解释不变。

7.4 全部原句保留，在最后一个保护句之后仅追加“上述证据层级集中见第\ref{sec:limitations}节。”未删局部续算继承前史、粗网格未达阈值、精细二维待核验、非连续保证或不推翻精细主解等内容。

7.5 原末尾两句：

> 这属于输入重建敏感性，不能当作半径测量误差的置信区间。
>
> 此外，$R(t)$本身是施加的观测，复现该曲线不能作为温湿预测的独立验证；更换环境或交换系数而仍使用原$R(t)$，只能解释为该几何历程下的条件响应。

压缩为：

> 输入重建差不能当作半径测量误差的置信区间，适用边界见第\ref{sec:limitations}节。
>
> 复现给定的观测$R(t)$不能作为温湿预测的独立验证；改变环境或交换系数而沿用该半径历程时，结果仅为条件响应。

保留“非测量置信区间”“复现输入不是独立预测验证”“改变工况但固定半径仍是条件响应”三层含义；前面的 PCHIP 终时和提前量原句不变。

## 3. 原数字、整句与关键保护的核验

对基线和本专项最终四源稿逐文件进行实际静态比较：

| 文件 | 原/现全部数字词元数 | 数字序列 | 检查的含具体数字整句 | 原整句缺失 | equation 环境 |
|---|---:|---|---:|---:|---|
| q1.tex | 189 / 189 | 完全相同 | 25 | 0 | 完全相同 |
| q2.tex | 210 / 210 | 完全相同 | 37 | 0 | 完全相同 |
| q3.tex | 129 / 129 | 完全相同 | 29 | 0 | 完全相同 |
| q4.tex | 213 / 213 | 完全相同 | 44 | 0 | 完全相同 |
| 合计 | 741 / 741 | 逐文件顺序不变 | 135 | 0 | 无模型公式改动 |

方法：数字词元使用 `\d+(?:\.\d+)?`，故包括公式、标识符中的原数字，是保守的整源稿检查；比较的不只是集合，也是顺序。含数字句按中文句号识别，筛选前剔除 `ref/eqref/label/cite/input/includegraphics` 的纯标签/路径，使文献键年份不被误认为具体结果；找到的原整句仅去空白后在新源文逐一匹配。该统计不是对任意中文分句的完美语言解析，另由逐行 diff 检查确认本次没有改动任何实际数值及其所在的完整句。新追加引用均不含新数字，因此最终追加后数字检查结果不变。

关键保护句核对：Q1 4.4 原物理不确定性句不变；Q2 5.6 导出一致性句和完整有效账本句不变；Q3 6.3 经验余量句、6.4 含数证据句和真实工况限制句不变；Q4 7.4 原文整块保留，仅尾随新引用句。其他无数字保护说明仅作第 2 节明确列出的同义压缩，没有删除保护含义。

四源稿实际检索未发现禁用的四个词组，也未新增“偏乐观”“偏保守”“真实时长一定更长”的潜热方向断言。两处模型临界时间缩短百分比及其语境未动。新增引用所需的 `sec:limitations`、`sec:environment-scenarios` 已在主任务编辑的 `validation_limits.tex` 中静态定位；最终引用是否排版正确仍须依实际编译日志及 PDF 检查。

## 4. 限定句统计口径及未达到的指标

限定句指限制结果适用范围、证据强度、验证含义或不确定性解释的完整句，按原/现中文句号计数；一句中的多个分号或限制分句仍记一整句。公式定义、单纯数值陈述、一般算法步骤不计；只有位置导览而无新限制的“上述证据层级集中见……”不计。I4 的两句虽同时导览，也新增明确相变缺口限制，采用保守口径计入。未人为改动原有句号或分号来降低计数。

“章末”可能被理解为最后自然段或最终结果解释小节，故同时给出两种可审阅范围，不只选较小数字：

| 章节 | 最后完整自然段：原 → 现 | 最后相关小节：原 → 现 | 说明 |
|---|---:|---:|---|
| 第四章 | 2 → 3 | 4.4：3 → 4 | 原带数字的中截面/端部界限与物理不确定性句保留；I4 追加一条 |
| 第五章 | 2 → 3 | 5.6：4 → 5 | 原含残差账本句、带早期时域的几何界限、来源限制保留；I4 追加一条 |
| 第六章 | 2 → 2 | 6.4：4 → 4 | 人为情景和真实工况保证边界仍各一整句；数值证据句不得删除 |
| 第七章 | 2 → 2 | 7.5：4 → 4 | 输入统计含义和外推边界仍各一整句；含数字的收缩归因限制保留 |

另核对合同明确保护的 Q4 7.4，共五条具有证据或适用范围限制含义的完整句，原文完整保留，未强行压为两句。第一至三章由主任务负责，本专项不把四问文件的局部统计称作第一至七章全量统计。

结论：无数字重复说明已作同义压缩并通过引用集中定位，但本专项没有减少保护性限定的整句数。I1 的“每章末尾不超过两句”在 Q1/Q2 最后自然段，以及按完整小节统计时均未全部满足；不能报告该指标通过。优先级依据为同份合同更高优先的含数字整句/保护声明保留要求及 I4 必须追加，不是隐藏检查失败。

## 5. 未做及交付边界

I4 在两个指定位置完成无数值、无方向的单句追加；I1 仅在可安全编辑的无数字重复说明中完成集中定位，硬性句数目标受上述合同冲突限制。全部数值、模型、关键证据与其保护范围保留。

本专项未运行编译、检查最终页数、渲染 PDF、验证 ZIP/清单或推送；这些由主任务提供真实结果。没有运行 I6 精细二维补证，也没有改变摘要、第六、第七、第十章既有全域结论的证据级别，更没有完成队员人工审核。
