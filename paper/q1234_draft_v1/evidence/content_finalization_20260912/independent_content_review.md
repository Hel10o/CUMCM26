# 内容定稿独立审阅

复核日期：2026-09-12。基线：`9c26eb4b13a236326e6605106934e35325c1a748`。本子代理实际读取内容交接合同、上一轮结构修订记录、当前主文件及正文各节；通过本地 Git 不可变对象冻结基线，没有独立重复 GitHub 下载。主代理的远程读取凭据见本目录 `github_reads.json`。

本复核仅检查源稿、存档字节和编辑含义，未执行求解、参数情景、工作簿导出或原求解器。文件冻结清单和实算 Excel 哈希见 `independent_baseline.json`；可复用核对脚本为 `check_content.py.txt`。正常运行脚本只向标准输出打印结果，本轮实际输出已保存为 `independent_content_check.json`。

## 冻结与静态核验

不可变基线清单包含 2,508 个受保护 Git blob，覆盖 1,652 个科学文件和 99 个既有论文证据文件。Git blob 标识是带对象头的 Git SHA-1，并非原文件 SHA256；四份正式 Excel 另实际计算原文件 SHA256，均与基线对象内容相符。最终源文检查时，受保护路径相对基线无差异，四份正式 Excel 的 SHA256 与冻结值一致。

实际运行静态脚本退出码为 0，17 项机械检查通过。这不等于内容合同九项检查均自动通过，以下将语义判断及编译后待检查项目分开记录。

- 摘要四问原段落包含全部 `textbf` 宏在内逐字不变；`main.tex` 的全部差异仅为两处相同的非加粗量级句插入。原结论两段完整保留；AI 声明原文和源中位置保持。
- Q1—Q4 的 `equation` 环境原字符串不变，检测到的含数字整句无缺失。全部纳入检查的 TeX 源数字词元总数由 1,156 增至 1,200，每个既有词元的出现次数均未减少；该词法指标包括重复出现次数，是编辑完整性指标，不是数值求解验证。
- 正文仍有 10 个一级章节。第九章顺序为“模型优点”“模型推广”“适用范围与主要限制”“改进方向与数据需求”。基线及当前正文均未出现硬编码 `9.2`、`9.3` 的小节引用；`9.33` 是误差系数，不能误判为编号引用。
- 原文四处强制保护说明保持，分别涉及 Q1 有效边界／密度／潜热不确定性、Q2 导出与独立逐秒求解的区别、Q3 经验余量不含工况与物性不确定性、Q4 前史误差及未取得连续二维保证。

## I1 的实际结果及限制

本轮保留全部含数字证据句，并保持不可给出真实工况保证的保护含义。Q2 人为情景、Q3 人为情景、Q4 热账本和观测半径说明作同义压缩；Q3 含 `57.4741 h` 的节点证据整句和 Q4 7.4 全域保护原块保留，以交叉引用引导至集中说明。没有删去数字来满足篇幅目标。

人工对读原句与改句后，四处检测为文字变化的保护说明仍包含原有边界：人为情景不是参数置信区间且不替代气固基准识别；无显式潜热且数值闭合不证明真实总焓；插值差不是半径置信区间；复现给定半径不是温湿独立验证，改变环境而沿用半径仅形成条件响应。没有将限制改成肯定保证。

但合同“章末限定句不超过 2 句”不能无条件报告通过。按完整自然段保守计数，第四至七章分别为 3、3、2、2 句；按各章最后相关小节完整范围计数，4.4、5.6、6.4、7.5 分别为 4、5、4、4 句，Q4 7.4 的独立全域保护块还保留 5 句。Q1、Q2 各新增 I4 相变缺口引导句，使相应计数增加；保留这些说明是遵守合同“含数字句及保护性声明不得删除”的硬边界。

脚本同时列出最后自然段和末小节的关键词候选句，但明确标记需要语义复核；候选器没有把所有“而非”“相变缺口约束”等表达识别为限定句，不能用其较低数值代替上述人工计数。I1 只能报告“部分压缩与集中索引已实施，句数上限未全面满足”，不能写成限定句数量均已减少，也没有通过改变句号或缩窄统计边界凑指标。

## 推广、物理限制与结论强度

新增“模型推广”逐项说明实际可替换的物性、工况和阈值接口，几何算子／权重／边界的重新处理，事件搜索的空间重构与时间穿越条件，已知外半径下仍需内部运动依据，以及取得工况记录后才可使用的情景框架。文本明确未在其他物料验证过，未承诺沿用本题精度，未把可扩展写成已经验证。

9.1 说明本题已实现能力及其证据，9.2 说明如何迁移及迁移前提；虽共同提到守恒、材料坐标和事件判定，但没有原样重复六条优点。此次对读未发现必须修正的无证据推广主张。

摘要与第六章的 Q3 仍仅以二维节点及配对根差支持工程保守方向；摘要、第七章、结论的 Q4 仍保留精细二维正式点待核验。9.3 新集中段也采用相同强度。所有原节点／误差／条件时长保护说明保留，未新增连续严格界、真实执行保证或全域充分验证结论。

I4 的两处追加句分别引向 `sec:limitations`，说明早期相变缺口量级及闭合限制；没有判断耦合后时间一定更长或更短。I5 的两处相同非加粗句只将既有条件时长换算为约 2.39 天和 2.13 天，指出与题述区间的量级一致，没有以该对照宣称模型得到验证。

## 合同九项检查的当前覆盖

| 检查项 | 本独立复核的实际结论 |
|---|---|
| 1. 十章及摘要保护 | 源文通过；十章、摘要四段及加粗重点原字节不变，仅增加一句非加粗量级说明。 |
| 2. 限定句与数字 | 数字和保护含义保留；句数上限未全面满足，按前述范围据实报告受限。 |
| 3. 新图百分比 | 未运行作图；直接提取最终独立图 PDF 及主论文第 24 页文本，两者都只有 `7.2533%`、`4.7118%`、`2.7379%` 三个百分比，逐项与源表一致；图注含规定的空间均匀干密度加权解释。 |
| 4. 措辞红线 | 论文源文及最终 156 页 PDF 全文对四个指定词语零命中；摘要 `60.65%` 和 Q4 两处 `60.6526%` 的全部最终 PDF 上下文都指模型临界时间缩短。 |
| 5. 全域强度同级 | 源文语义对读通过，摘要／第六／第七／第十章未升级强度；I6 未执行。 |
| 6. 第九章小节 | 源中四小节名称、次序通过；最终 AUX 实测编号顺序为 9.1、9.2、9.3、9.4，所在页为 26、26、27、28。正文编号仍为一至十。 |
| 7. 引用与占位符 | 98 次 `ref/eqref` 使用、51 个不同目标全部唯一解析；74 个 AUX 标签无重复；20 次引用、7 个文献键各有唯一定义。两处 I4 均解析到第 27 页的 9.3。最终日志无未定义引用或缺字，156 页 PDF 无 `??`、`[?]` 或替换字符。 |
| 8. 正文页数 | 实测主 PDF 共 156 页；摘要第 1 页，正文第 2—29 页共 28 页，附录第 30—156 页共 127 页。正文包括 AI 声明及参考文献，28 ≤ 30。 |
| 9. AI 声明 | 源文原字节不变；最终 PDF 中 AI 声明和参考文献均在第 29 页，AI 声明在参考文献之前。 |

最终编译后只读检查直接读取 `build/content_finalization/main.pdf`、`.aux`、`.log`，未重编译，结果见 `compiled_content_check.json`。第二轮检查保留在 `compiled_second_check.json`；主代理为避免表格浮动隔断新图引导句，仅补入 `FloatBarrier` 和空行后进行了第三轮编译，本子代理重新执行全部静态和编译产物检查。新增降维图仍为图 5（第 24 页），原环境图仍为图 6（第 25 页）。最终 19 项编译产物检查全部通过，源文静态检查 17 项保持通过；这些自动检查不把 I1 的句数上限受限变成通过，也不替代渲染页面的视检、科学重算或参赛队员人工审阅。

本次核对绑定的最终主 PDF SHA256：`9012365c7252904b182a7a00f0b408e36a7ec1acfd25da53a0198acccfb9126b`。`main.tex` SHA256：`11564d8746de28c05ff0e8d6ce0c4af20a6598d0ea6bf6e0b972ecf2aa7a1d40`；`sections/validation_limits.tex` SHA256：`aab25cd6b11af479ceaa9d4e08b0f4436e41fd4521091d11638d1febf3bcbd90`。其他源文件和编译输入的最终 SHA256 一并记录在 `compiled_content_check.json`，核验期间输入未变化。最终 ZIP、远程同步仍须在全部记录冻结后单独核验。
