# 四问论文内容改进与定稿记录

2026-09-12。本轮完成 I2–I5 和 I1 在保护约束下可改的范围；I1 的严格句数上限未全部达到，I6 未执行。编辑不改变任何正式结果、PDE、参数情景或既有数值证据。

## 实际读取

检查本地状态、远程地址与分支，`git fetch origin` 后确认本地与远程 main 均为 **`9c26eb4b13a236326e6605106934e35325c1a748`**。通过 GitHub 连接在此不可变 SHA 读取根 README、`q1234_paper_content_handoff/论文内容改进与定稿_模型指示文档.md` 全文，以及指定的 `paper/q1234_draft_v1/evidence/structure_revision_20260912/论文结构与表述修订记录.md` 全文；路径、Git blob SHA 和固定链接见 [github_reads.json](github_reads.json)。这已包含结构提交 `50e7b54` 与摘要加粗提交 `fca3c12`，没有重复结构修订。

本地读取当前 `main.tex`、各正文 `sections/`、构建与检查脚本、源稿打包器、AGENTS 和 gitattributes；I3 另只读六份冻结 geometry 数组及实际执行过的几何源码，I5 核对题面可读全文第 80 行“烘干过程一般持续 2-3 天”。只读最小补算计划 E1，未执行补算。此前模板目录 `template/` 仍未跟踪，本轮不提交。沿用已存档的格式依据，不把本轮论文编辑称为重新核验全部竞赛规则或最终提交资格。

## 实际修改

| 文件 | 修改 |
|---|---|
| `sections/q1.tex`、`q2.tex` | 4.4、5.6 各追加一句不含具体数值的潜热诊断引用，指向新 9.3；原强制保护句完整保留。Q2 无数字情景说明简化并指向 8.2。没有断言补入潜热后结果的偏差方向。 |
| `sections/q3.tex`、`q4.tex` | Q3 6.4 与 Q4 7.4 含数字整句及保护说明保留，追加集中说明的定位；简化 Q3 情景说明、Q4 重复总焓及输入重建说明，保护含义不删。逐句对照见 [wording_edits.md](wording_edits.md)。 |
| `sections/validation_limits.tex` | 新增 9.2“模型推广”，约 385 个汉字，说明同类物料、几何、阈值事件、已知收缩及工况情景的迁移接口和条件；9.1 原六条不变，原限制与数据需求顺延为 9.3/9.4。9.3 承接数值证据、前史、半径输入和实际工况范围。8.1 插入新图及引导、数据口径与来源图注；增加标签，保持一级标题不变。 |
| `main.tex` | 在摘要关键词前及结论第二段末各追加同一句未加粗的量级说明：57.4741 h 与 51.0921 h 分别约 2.39 天、2.13 天，落在题述区间。四问摘要原段、全部加粗及原两段结论均逐字保留。没有把量级一致说成模型验证。 |
| `build_dimension_reduction.py`、新图、作图证据 | 新脚本只提取六份已存数组，未导入或执行求解器。输出 `figures/dimension_reduction.pdf/.png`，来源、脚本、依赖、提取数据与输出哈希单列 [figure_sources.json](figure_sources.json)，保留旧 `assets_manifest.json`。 |
| 三份 README、PDF、源稿 ZIP 与清单 | 更新根、论文、内容交接说明中的本轮状态、图数与页数；打包器纳入新脚本和 PNG。旧源稿清单备份为 [prior_source_package.json](prior_source_package.json)，新 ZIP 及当前清单同步更新。 |

### I3 的数据口径

图 5 上排使用指定末时的中截面完整径向场，下排连接各已存快照的体积加权平均。六份 `.npz` 的 `snapshots` 与 `weights` 作点积，不重建新的时间状态；Q4 用保存的当前半径映射横坐标。

实际数组存半柱，**`z_m=0`、索引 0 才是中截面**，不是数组的中间索引。`mid` 是 21 点重采样，不能当成完整 81 节点径向场。该解释经原几何源码静态定位，与指示文档的示意写法存在区别，已据实际保存格式作图。详见 [figure_notes.md](figure_notes.md) 和 [提取绘图数据](dimension_reduction_plot_data.json)。

| 情形 | 二维均值 | 一维均值 | 失水增加 |
|---|---:|---:|---:|
| Q1，1800 s | 2.274929 | 2.293532 | 7.2533% |
| Q2，10800 s | 1.327482 | 1.382492 | 4.7118% |
| Q4，21600 s | 1.024545 | 1.065198 | 2.7379% |

上述九个显示值与原表 `tab:wholebody` 完全一致，百分比表示相对于配对一维的失水增加，不是平均含水率相对误差。图注沿用 Q1/Q2 已检时段采样最大差，不把它们改称图示单个终点最大差。均匀干密度条件下体积加权平均等于干质量加权平均的前提已写入图注。

## 实际核验

以下按指示文档第 9 节逐项报告。机械核对与语义审阅分别记录，不能用其他通过项掩盖第 2 项受限。

| 项 | 结果 | 实际证据 |
|---|---|---|
| 1. 一级标题、摘要原文与加粗 | 通过 | 正文仍 10 个一级标题；摘要四段及其 `textbf` 原字节保持；主文件仅两处相同的普通句追加；原两段结论不改写。 |
| 2. 限定句减少、数字保留 | **部分完成，句数上限未通过** | 四问 741 个原数字词元逐文件顺序一致，135 个含数字整句无缺失；论文 TeX 统计口径原数字出现次数 1156→1200，各既有词元次数未减少。原第一至三章未改，章末自然段限定句为 1/1/0；第四至七章按保守完整句口径为 3/3/2/2，原为 2/2/2/2。4.4、5.6 各因必须追加 I4 增一条；按完整末小节统计也并非全部 ≤2。没有删含数字句、保护声明或改标点凑数。无数字重复说明仅作同义简化及集中定位，不能称限定整句总数下降。 |
| 3. 图表一致 | 通过 | 图 5 三个百分比、六个均值与表 7 逐字一致；六数组哈希保持，图脚本有实际执行及显示值断言。 |
| 4. 红线与百分比语境 | 通过 | 主论文源文与最终 156 页 PDF 对“能耗”“节能”“可忽略”“题面指定中截面”零命中；三处 60.65%/60.6526% 仍是模型临界时间缩短。此范围不包括任务合同、历史审核或独立 AI 详情。 |
| 5. 四处全域强度 | 通过 | 摘要、Q3、Q4 与结论均保持：Q3 节点证据支持保守方向；Q4 一维条件值、精细二维正式点待核验；无新增连续界或实际保证。 |
| 6. 第九章编号 | 通过 | 插入前实际查无硬编码 9.2/9.3 引用；最终 AUX 为 9.1 模型优点、9.2 模型推广、9.3 适用范围与主要限制、9.4 改进方向与数据需求。 |
| 7. 引用与编译 | 通过 | 最终 98 次 ref/eqref 的 51 个目标唯一可解析，74 个 AUX 标签无重复；20 次 cite 的 7 键均有效。两处 I4 指向 9.3。日志无 undefined、重复定义、缺字或 overfull；全文无 `??`/`[?]`。两处 underfull 保留，相关页已目视。 |
| 8. 正文页数 | 通过 | 摘要 1 页，正文物理第 2–29 页含 AI 声明和参考文献，共 **28 页 ≤30**；附录第 30–156 页共 127 页；总计 156 页。 |
| 9. AI 声明 | 通过 | 原声明字节未变，与参考文献同在第 29 页且位于其前；独立 AI 使用详情源文/PDF 未改。 |

I1 的冲突发生在同一合同内：指定压缩的 Q3/Q4 段及 Q2 账本说明含具体数字，另有必须保留的保护性声明；I4 又要求明确追加潜热限定。本轮按“含数字整句与保护声明保留”执行，不把严格句数未达到写成通过。完整候选句、统计口径与原/新对照见 [措辞记录](wording_edits.md) 和 [独立审阅](independent_content_review.md)。

实际成功运行新作图脚本、XeLaTeX/latexmk 三轮编译、最终 PDF 检查和独立静态/编译产物核对，退出码均为 0。最终 [PDF 检查](pdf_validation.json) 为 **37 项通过**，六张题设表 **279 个显示单元格**匹配；[静态检查](independent_content_check.json) 17 项通过，[编译产物检查](compiled_content_check.json) 19 项通过。四份正式 Excel 的实际 SHA256 与基线一致；2508 个保护路径、1652 个科学文件以及旧审核证据无变更，来源冻结见 [independent_baseline.json](independent_baseline.json)。这些是静态/后处理检查，不是重新求解验证。

### 页面与过程记录

全部 156 页按 72 dpi 渲染为 [全页缩览](rendered_page_overview.pdf)，摘要与正文第 1–29 页另按 130 dpi 逐页目视：第 1–15 页由独立代理查看，第 16–29 页由主代理查看。新图引导句被浮动表隔断的问题用 `FloatBarrier` 修复后重编，第 23 页重新查看，文字与数据未变。第 12、13 页既有段首孤行与基线像素完全一致，本轮保留，不称完美分页。

附录 127 页均作缩览，并与基线相应页比较：**除实际页码所在的中央小矩形外，所有内容像素一致**；该排除框内核实没有其他文字，包含代码末行的其余区域仍全部参与比较。最终对象与各代理已查看 PNG 的 SHA256 对齐，见 [页面比对](page_comparison.json)、[最终目视绑定](visual_final_check.json)、[1–15 页审阅](visual_pages_01_15.md) 和 [附录缩览审阅](visual_appendix_overview.md)。缩览和代理检查不等于队员逐字人工审核。 缩览 PDF 在提交前仅作无损流压缩，全部缩览页在 72 dpi 下与压缩前像素完全一致，记录于同一最终目视清单；主论文 PDF 未改。

过程异常没有伪装为通过：附录像素比对最初使用过窄的页码高度框，仍包含数字上部，触发断言；随后根据实际页码字框修正，并确认框内无非页码文字。该次断言使最终 PDF 尚未复制到公开文件名，紧接着一次检查误用了旧 PDF 与新 AUX，产生页数和 AI 位置两项失败，保留于 [误配产物检查](pdf_validation_mismatched_publish_attempt.json)。之后已将真正最终 PDF 复制、重新完整检查，当前 `pdf_validation.json` 绑定正确文件并通过；不是数值结果错误，也未删旧证据。

最终主 PDF SHA256：`9012365c7252904b182a7a00f0b408e36a7ec1acfd25da53a0198acccfb9126b`。源稿包按当前打包器纳入新图、新脚本与本轮证据，CRC、逐成员 SHA256 和目录一致性由打包步骤实际核验；当前包哈希见目录外 `evidence/source_package.json` 及 `.zip.sha256`，避免包内自引用。

## 未做或受限

- 未重跑任何 PDE、参数情景、正式工作簿导出或历史整套求解；仅 I3 读取保存数组作加权和及绘图。
- 可选 I6 未执行，也未声称具备或测得其计算成本；第四问精细二维正式点缺口保持，不升级全域结论。
- I1 严格句数上限受上述保护要求限制，没有全面达到；这使本轮不能被描述为所有内容合同指标全部通过。
- 未改已验收原始数值、求解器或旧审核文件；旧 `assets_manifest.json` 由新图独立清单补充。
- 未完成参赛队员逐项人工审核、补齐真实 AI 使用详情或最终竞赛提交认证；本轮是可审阅的内容修订成果。
