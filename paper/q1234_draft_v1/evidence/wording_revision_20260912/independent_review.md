# 摘要语气与措辞修订独立核验

日期：2026-09-12。不可变基线：`ff5011e441e7ba47904549db0cc754a506b4a4d9`。本独立子代理实际读取本轮指示文档、主论文源文及上一轮已存证据，通过本地 Git 对象冻结基线；未独立重复 GitHub 下载。主代理的 GitHub 读取记录用于说明远程来源，本记录不冒称另行读取了远程。

仅创建本轮独占的 `baseline_integrity.json`、`independent_check.py.txt`、`independent_check.json` 和本说明。未编辑论文、README、源稿包或旧证据，未运行 PDE、参数情景、工作簿导出或新数值求解。

## 实际核验与保护范围

`baseline_integrity.json` 冻结 2,540 个受保护 Git blob，覆盖 1,666 个科学文件和 125 个既有论文证据文件；四份正式 Excel 另计算原文件 SHA256，并核对其 Git blob 与基线相符。最终检查中保护路径无差异，四份 Excel SHA256 与冻结值一致。`sections/problem_model.tex`、`q1.tex`—`q4.tex`、`validation_limits.tex` 的原字节全部保持；公式、图、数组、所有含数字句和保护声明均未修改。

实际运行命令为：

```text
python -X utf8 paper/q1234_draft_v1/evidence/wording_revision_20260912/independent_check.py.txt --pdf-dir paper/q1234_draft_v1/build/wording_revision
```

程序仅只读核验并输出 JSON，退出码为 0，41 项检查通过；实际标准输出保存为 `independent_check.json`。其内容含最终源稿、PDF、AUX 和日志的 SHA256。主论文发布位置与受检 `build/wording_revision/main.pdf` 的字节完全一致。

## 数字次数的明确口径

数字统计使用源稿十进制词元，排除引用标签、文献键与输入路径等非正文参数，但保留重复出现次数。本轮所检查 TeX 范围总数为 **1,200 → 1,200**。这不是“每个数字出现次数均不减少”：

| 词元 | 出现次数变化 | 原因 |
|---|---:|---|
| `57.4741` | −2 | A5 摘要末句和 B 结论末句各删除一次重复小时数 |
| `51.0921` | −2 | 同上 |
| `0.149998606290` | +1 | A3 引用既有二维正式节点最大值 |
| `80` | +1 | A3 明示既有同网格配对的径向网格范围 |
| `256` | +1 | A3 明示既有同网格配对的轴向网格范围 |
| `7.4740` | +1 | A3 引用既有配对根提前量 |

新增的四个词元均服务于授权的正面证据陈述及算例范围；没有为了平衡计数补重复数字。A/B 的删除位于明确授权的重复句内，结果本身没有改变。任务 C 的六份相关正文源文件保持原字节，因此其逐文件数字序列、含数字整句和保护句均完全保留。

## 九项一致性检查

| 项目 | 独立实际结论 |
|---|---|
| 1. 摘要单页、加粗不增 | 摘要仍为物理第 1 页，正文从第 2 页开始。摘要内 `textbf` 块数 24→24，参数去空白字符 397→317，去宏后的可见字符代理 347→267；末句未加粗。字符代理去掉宏名、花括号及空白，保留显示参数并把摄氏宏计为一个字符，不冒充 PDF 字形数。 |
| 2. 方法与数字 | Q1/Q2 整个摘要段包含加粗宏逐字不变；所要求的十个原数字均在各自摘要段正确保留。Q3 新增节点值与 80×256 配对提前量逐项对应既有正文。 |
| 3. 结论去重 | 主文件除 Q3 摘要、Q4 摘要、摘要末句及结论末句四个授权区段外，屏蔽后字节与基线一致。结论中 `57.4741`、`51.0921` 现在各出现一次，其余结论内容未变，末句以“二者”接续。 |
| 4. 措辞红线 | 源文及最终全部 156 页 PDF 对四个指定禁词零命中。PDF 中一处 `60.65%`、两处 `60.6526%` 的全部上下文均明确为模型临界时间缩短。 |
| 5. 摘要不得强于正文 | 按任务 D 的新口径语义对读通过，理由见下文。没有要求摘要继续罗列未完成事项，也没有借省略事项新增保证。 |
| 6. C 清单与数字 | C 实际删除项为零，正文六份相关源文件字节不变；候选句受数字或保护声明约束，允许无可删项。全文词元总数持平，各词元变化另按 A/B 授权明确报告，未声称每个数的次数不减。 |
| 7. 引用、编号与日志 | 98 次 `ref/eqref`、51 个不同目标全部唯一解析；74 个 AUX 标签无重复；20 次文献引用、7 个不同键均有效唯一。正文一至十连续；9.1—9.4 顺序正确。最终日志无未定义引用或缺字，156 页 PDF 无 `??`、`[?]` 和替换字符。 |
| 8. 正文页数 | 总 156 页；摘要第 1 页，正文第 2—29 页共 **28 页 ≤ 30**，附录第 30—156 页共 127 页。正文计数含 AI 声明和参考文献。 |
| 9. AI 声明 | 源文保持原字节；最终 PDF 中 AI 声明与参考文献都在第 29 页，AI 声明在前。 |

## 任务 C 与 D 的语义审阅

C 的主要候选均不能在硬边界下直接删除：Q2 账本说明含残差数字并明确热账本不证明真实总能量闭合；Q3 二维层级句含 `57.4741 h` 并限制前史与连续严格界；Q4 正式时刻层级句含 `51.0921 h` 并保留精细二维正式点待补的范围。Q2/Q3 的人为情景句排除置信区间或观测误差解释，属于保护性声明。其与 9.3/8.2 的主题相近，不等于可删除的无信息重复。交叉引用引导句承担导航功能，并未重新宣告一遍限制。本轮零删除是逐项保护后的实际结果，没有设置删减率，也没有按上一轮句数上限调整标点。

D 的当前标准是“摘要不得强于正文”。Q3 段先给出一维径向有效模型与后段环境假设，再陈述正式时刻二维离散节点最大值和全部节点低于阈值；80×256 同网格配对限定了端部交换提前约 7.4740 s 的范围，结论仍为支持工程保守方向，没有从离散节点推出连续全域严格保证。

Q4 段保留恒长度、均匀径向收缩、同一后段环境及有效方程条件对照，明确几何尺度和干基边界标度共同作用；移出摘要的精细二维待核验事项仍完整保留在第七章、9.3、9.4 和结论。摘要未说已完成连续全域达标或真实执行保证，因此有意省略未完成事项没有使结论强于正文。

摘要末句仅报告 2.39 天、2.13 天与题述周期的量级一致，没有写成由题述周期证明模型有效。未新增潜热修正方向断言。

## 页面检查与产物绑定

实际查看了最终摘要第 1 页和结论所在第 28 页的 130 dpi PNG，未见文字裁切、重叠或页码异常；摘要末句和关键词仍在第 1 页。随后从最终 PDF 在内存中按 130 dpi 重新栅格化这两页，与所查看 PNG 的像素字节逐一比较，两个页面均完全相同，确认视图对应最终 PDF。

主代理保存的 `page_comparison.json` 记载全部 156 页按 72 dpi 比较，仅第 1、28 页变化，其余 154 页像素一致。本子代理读取了该记录，但没有独立重跑全部 156 页的像素比较；本独立视检限于上述两个变化页，不声称逐字重读代码附录。

最终主 PDF SHA256：`4f2a1098f16a6abcfb272ba01ee8a814a56ecbd6e950e93b2da4a5583add4e0b`。最终 `main.tex` SHA256：`5dc5f65b932e10a2f4d6e5355569636c174a842aae3bcda0bf2885dd1197636a`。本轮没有重编译或重跑模型；最终 ZIP、暂存内容及 GitHub 同步仍须在所有新记录冻结后另外核验。
