# 按用户参考图改写摘要

本轮实际读取的 Git 基线为 `573348fd60e1ea26ff9874c570f8fe2f1c0f690f`。修改对象为 `main.tex` 中的摘要文字和强调位置，保留现行模型、结果、正文、附录与排版设置。

## 写法与内容选择

用户参考图采用逐问“针对问题……首先……建立……采用……得到……说明……”的叙述方式。本稿据此保留简短引入，再让四问各自成段，依次交代任务、模型与求解、关键结果及结果含义。段首提示语、主要方法与关键数值选择性加粗，摘要文字共 17 处粗体区域，避免整段加粗削弱层次。

参考图只用于文字组织和重点呈现，其农业种植模型、算法和数值没有移入本题。已有建议及旧修订中“避免连续使用针对问题”的写作偏好，以本次用户明确指定的参考形式为准；原记录保留。

当前摘要见[摘要仿写稿](摘要仿写稿.md)。对应证据如下：

| 段落 | 本次突出的工作 | 已读取的证据入口与限定 |
| --- | --- | --- |
| 问题一 | 圆环有限体积共享面通量、隐式 BDF 积分、短时温湿分布 | `sections/q1.tex` 的离散求解与结果；原 `q1_complete_delivery/q1_delivery/source/q1_solver.py` 使用 BDF。保留常热物性导热与非线性水分扩散的表述，不将其写成第二问的双向变物性反馈。 |
| 问题二 | 变物性联立求解、Chebyshev 配点与 Radau 积分、扩散系数对数分解 | `sections/q2.tex` 和 `q4_complete_delivery/v1/q123_closeout/source/solve_unified_q23.py`。对数分解描述沿已有轨迹的温度与含水率项，不宣称实验因果贡献；“两处扩散率回落”限定为 2.5–3 h。 |
| 问题三 | 径向重构极值搜索、临界根与执行点区分 | `sections/q3.tex`。57.4741 h 仍为后 4 h 环境按末小时均值延拓时的一维条件执行值；二维节点场支持保守方向，未变为连续全域严格数学界。 |
| 问题四 | 干物质守恒、材料坐标、同物性恒半径对照 | `sections/q4.tex`。恒长度和均匀径向收缩仍为假设；51.0921 h 为条件执行值。129.8484 h 与约 60.65% 采用同物性对照的临界时间口径，包含几何及干基边界标度变化；精细二维全域终点仍需补证。 |

为形成参考图式的逐问摘要，本版删除上一版摘要末尾独立的“后段环境降 1 K”情景总结段；相关数值和分析完整保留在正文。问题三及问题四仍在摘要中明确后段环境条件，未将条件计算时长改写为实验保证值。

## 实际编译与检查

- 单页摘要试编译和完整 `build.ps1` 均成功，最终摘要页与已查看的试排页图像相同，已再次查看正式 PDF 首页的渲染图。
- 摘要保持 11.5 pt 字号、18 pt 行距、5 pt 段间距，仍为一页；正文含 AI 声明和参考文献 22 页，附录 127 页，总计 150 页。沿用本会话此前读取并保存的[官方格式依据](../official_sources.json)，本次没有新增格式规则。
- `main.tex` 除摘要文字外逐字符一致。完整 PDF 在 96 dpi 下与基线逐页比较，仅第 1 页变化，正文及附录其余 149 页图像一致；因此此前的中文一级标题、图表位置和正文留白均保留。
- 37 项现有 PDF 自动检查全部通过，六张题设表的 279 个单元格显示字符串一致。未重新运行科学求解，也未修改正式 Excel 或历史模型证据。
- 编译仍有原正文／参考文献的两处 underfull 提示及 Perl locale 回退提示；未出现 overfull 或编译失败，正文与附录的页面一致性另有上述图像比较支持。

证据：[本轮文字与页面对比](revision_check.json)、[完整 PDF 检查](pdf_validation.json)、`build.log`。本版论文 PDF SHA-256：`37fe344b74f9e976ee694e31b5b1544f24ac0de3b04b20282088b0f64f0c5dac`。

论文 PDF、源稿 ZIP 及当前清单一并更新。旧清单原字节保存为 `prior_source_package.json`，旧修订证据不覆盖。本轮启动时已有未跟踪的 `template/`（内部为独立 Git 仓库），未修改或提交该目录。
