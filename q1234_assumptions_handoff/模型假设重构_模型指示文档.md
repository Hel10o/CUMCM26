# 模型假设重构与免责落位 · 模型指示文档

## 0. 任务与当前状态

**任务：** 重构第三章"模型假设"的六条条目，并把这六条里携带的免责语气落位到第十一章的对应小节。

**当前结构（务必先核对实际编号，本文件写作时为 `bfb04e8`）：**

```
一、问题重述          二、问题分析
三、模型假设          ← 本次改写对象，problem_model.tex:29-44，共 6 条
四、符号说明          五、统一传热传质模型与数值方法
六、第一问  七、第二问  八、问题三  九、问题四
十、模型检验：降维有效性与后段环境灵敏度
十一、模型评价、适用范围与推广
     11.1 模型优点   11.2 模型推广
     11.3 适用范围与主要限制（标签 sec:limitations）  ← 免责内容的落位处
     11.4 改进方向与数据需求
```

注意：早前材料里说的"9.3 适用范围与主要限制"现在已是 **11.3**（标签不变，仍是 `\ref{sec:limitations}`）。**一律用 `\ref` 引用，不要写死数字。**

**性质：** 纯论文编辑。不改数值、不重跑求解、不增删一级章节。

---

## 1. 硬边界

- 不改任何数值结果、数组、求解器；不重跑 PDE 或参数情景。
- 不新增或删除一级章节；假设条目可增删（正文没有硬编码的"假设N"引用，已核实）。
- **`\label{eq:future}` 必须保留在原处**：`validation_limits.tex:35` 有 `\eqref{eq:future}` 引用它。
- **11.3 的量化内容一字不动**（45.59 kJ、4.801 kJ、9.50、164.10 W/m²、0.71952/0.89029、34.7454/28.0807 cm 等）。
- **不允许直接删除免责内容造成信息丢失。** 每删一条免责句，必须先确认 11.3 或 11.2 已有对应表述；若无对应，则把它移入 11.3 或 11.4，而不是删掉。
- 假设条目只能写模型**确实做了**的假设，必须与第五章方程一致；不得写入正文没有实施的事项。
- 不得把"本模型不显式计入蒸发潜热"这类**披露性内容**删去——它可以把"不能声称…"的免责语气改成陈述句，但披露本身必须留在假设里。

---

## 2. 当前六条的三个问题（先读诊断再动手）

### 2.1 一个列表混了五类东西

现在六条里混着：

| 类型 | 例子 |
|---|---|
| 物理简化（属于假设） | 第 1 条"轴对称连续介质，初始温湿均匀" |
| **题面给定的定义** | 第 2 条开头的 `$C=m_w/m_d$`（题面定义，且已在符号说明表中） |
| **题面给定的数值** | 第 3 条的 `h_T=25`、`h_m=8\times10^{-7}`（附录2 给定，不是我们假定的） |
| 假设 | 恒等映射、均匀径向收缩、环境平台 |
| **免责声明** | 第 2/3/4/5 条后半句的"不能把…称为实测""不自动等于…""不能据…宣称…""单凭…不能唯一识别" |

### 2.2 免责句与 11.3/11.2 几乎逐字重复

| 假设条 | 免责句 | 已有的对应落位 |
|---|---|---|
| 第 2 条 | "不能把 $b$ 直接称为实测药材平衡含水率" | 11.3 气固边界段："未经标定的边界情景也不能称为实测" |
| 第 3 条 | "$h_m$ 属于材料干基驱动力标度，不自动等于气相浓度模型中的气膜系数" | 11.3 气固边界段："才能建立 $\rhod h_m=k_gK$，不能直接交换系数数值" |
| 第 4 条 | "不同时强制 $\rho/(1+C)$ 为真实干密度" | 11.3 经验密度段："本文仅将题给 $\rho$ 用于有效容量，是建模处置而非题面规定" |
| 第 4 条 | "不能据有效热方程宣称真实能量闭合" | 11.3 相变能量段："不能据此推断真实热量收支或执行保证" |
| 第 5 条 | "内部仿射运动是额外假设，单凭外半径不能唯一识别" | 11.2 模型推广段 + `q4.tex:25`、`q4.tex:35` |
| 第 6 条 | "不因某情景更接近通常 2--3 天而选择其参数" | **需核对**：8.2/11.3 若无对应，移入 11.3 或 11.4 |

前五处可以直接删；第 6 条那半句先查再定。

### 2.3 最近加上的"目的"句属于重复

`bfb04e8`（"explain purpose after each model assumption"）给每条都加了一句设计目的，例如"将内部状态简化为径向温度与含水率场，减少空间变量并明确初始条件""为长时温湿演化与干燥终点计算提供明确的边界条件"。

这类句子是**建模思路**，而第二章"问题分析"已经逐问写过了（例如"对于问题二……观测时段之后的环境按明确的延拓假设处理"）。放在假设列表里会造成两处说同一件事，且把"假定什么"和"为什么这么假定"混在一行。

**建议：保留粗体引导句（便于扫读），删去目的说明句。** 如果决意保留目的句，则至少要删掉 2.2 表里的免责句，不要把两类都留着。

### 2.4 该列的假设反而漏了三条

- **表面交换取线性 Robin 形式**（`-k\partial_nT=h_T(T_s-T_a)`、`-D\partial_nC=h_m(C_s-b)`）。这是本构假设，现在只在第五章方程里出现，没进假设列表。
- **题表距离按中截面径向解释**。现在写在"问题分析"末段，但它本质是建模假设，直接影响一维降维的成立。
- **忽略项**：内部传质只考虑扩散（无压力驱动流动与内部对流）；环境量在空间上均匀（只用单一环境值）；`h_T` 按单一系数处理，不区分对流与辐射。写"忽略了什么"是假设列表最该承担的职责，现在一条都没有。

---

## 3. 目标假设列表（八条，可直接采用）

```latex
\begin{enumerate}
\item \textbf{药材为初始温湿均匀的轴对称连续介质，并按中截面径向降维：}
内部状态用径向温度场与含水率场描述，主模型忽略端面，二维轴对称计算用于校核
侧面与端面交换；题表的“到药材中心距离”按轴向中截面内的径向位置解释。
分问物性在当前位置按局部状态取值。
\item \textbf{表面交换采用线性Robin形式：}
表面与烘房之间的传热、传质通量分别正比于表面与环境的温度差和有效含水率差。
\item \textbf{各问沿用同一组表面交换系数：}
附录2给出$h_T=25\unit{W/(m^2K)}$、$h_m=8\times10^{-7}\unit{m/s}$，
后问未另给时沿用该值；$h_T$按单一系数处理，不区分对流与辐射。
\item \textbf{表面传质以有效材料干基驱动表示：}
取$b=g(y_a,T_s,\ldots)$，现行计算取恒等映射$g(y_a,\ldots)=y_a$。
\item \textbf{内部传质仅为扩散：}
水分在药材内部以有效扩散方式迁移，不考虑压力驱动流动与内部对流；
烘房环境量在空间上均匀，用单一的温度与空气水分量代表。
\item \textbf{题给密度仅用于有效热容量，干质量独立记账：}
题给$\rho c_p$记为有效热容量$\rhoe c_p$，真实干质量由$\rhod$另行描述；
本模型不显式计入蒸发潜热、液态扩散携焓与机械功项。
\item \textbf{前三问几何固定，第四问为恒长度均匀径向收缩：}
前三问干骨架静止；第四问材料随外半径作均匀径向运动、长度取$L=L_0$，
表面位置按附件2的半径记录给定。
\item \textbf{环境与半径输入按规定方式取值：}
附件1覆盖0--14400\unit{s}、采样间隔60\unit{s}，区间内按记录点线性插值；
4\unit{h}之后无实测数据，假定环境维持记录末小时（3--4\unit{h}，61个采样点）
的算术均值
\begin{equation}
T_a=49.9989344262\celsius,\qquad b=0.04998754098\unit{kg/kg}.
\label{eq:future}
\end{equation}
附件2共145条半径记录、间隔1800\unit{s}，按分段线性插值。
\end{enumerate}
```

要点：每条只写"假定什么"，一到三句；定义与题给数值不再单独成条（`C` 已在符号说明表、方程形式在第五章）；免责全部移交 11.3；补齐 Robin 形式、中截面解释与忽略项。

**注意 `\label{eq:future}` 留在第 8 条内**，与现有引用保持一致。

---

## 4. 配套改动

1. **"问题分析"末段的题表解释**：假设 1 已包含该解释，原处保留一句回指（如"该解释见第 3 节假设 1"），不要两处都完整复述。
2. **`\Needspace{4\baselineskip}`（`problem_model.tex:36`）** 现在位于第 4、5 条之间，会把后半段推到新页。改写后条目长度变化，须重新确认假设列表是否被拆得不合理；若不再需要该命令，删除它。
3. `\Needspace{22\baselineskip}`（符号说明前）按改写后的分页实测决定是否保留。
4. 第 6 条那半句"不因某情景更接近通常 2--3 天而选择其参数"：先 grep 8.2 与 11.3/11.4，无对应则移入 11.3 或 11.4。

---

## 5. 一致性检查

1. 假设条目编号连续（一至八），正文无硬编码"假设N"引用（改后再 grep 复核一次）。
2. `\label{eq:future}` 仍唯一定义、`\eqref{eq:future}` 仍可解析。
3. 2.2 表中五处免责内容在 11.3/11.2 均可查到，逐条记录位置；第 6 条那半句给出处置结论。
4. 11.3 的量化数字与改写前逐字一致（45.59、4.801、9.50、164.10、0.71952、0.89029、34.7454、28.0807）。
5. 假设列表与第五章方程一致：Robin 形式、仅扩散、`ρ_eff c_p` 与 `ρ_d` 的口径。
6. 全文"能耗""节能""可忽略""题面指定中截面"零命中。
7. 编译日志无 undefined reference、无重复标签；正文页数 ≤ 30（当前 28 页，本改写预期略减，须实测）。
8. 摘要未受本次改写影响（不改摘要）。
9. AI 声明仍位于参考文献之前。

---

## 6. 交付物

1. 修改后的 `paper/q1234_draft_v1/sections/problem_model.tex`、`sections/validation_limits.tex`（仅 11.3/11.2 的落位）、必要时 `sections/q4.tex` 的去重。
2. 本轮修订记录 `paper/q1234_draft_v1/evidence/<修订名>_<日期>/`：改前改后条目对照、五处免责的落位核对表、九项检查结果、页面检查 JSON。
3. 重新编译并更新 `四问论文初稿_v1.pdf`；若修改了源稿 ZIP 内包含的文件，同步更新 ZIP 与 `source_package.json` 清单。
4. 更新受影响的 `README.md`。
5. 按 `AGENTS.md` 提交并推送到 `origin/main`；不强制推送；报告区分"已本地提交"与"已远程同步"。

---

## 7. 验收标准

- 假设列表每条只含假设陈述，无"题面给定的定义/数值"单独成条，无"所以不能声称…"式免责。
- 免责内容在 11.3/11.2 逐条可查，无信息丢失；第 6 条那半句有明确处置。
- 补齐 Robin 形式、中截面径向解释、忽略项三类遗漏。
- 粗体引导句保留；目的说明句已删（或已按 2.3 的替代方案处理，且免责必须删）。
- 编译通过；正文 ≤ 30 页；11.3 与六张表数字逐字未变。

---

## 8. 报告格式

1. **实际读取**：提交 SHA、读过的文件（含 11.3 与 8.2 的原文）。
2. **实际修改**：改前改后假设条目逐条对照、免责落位核对表、其他文件的去重清单。
3. **实际核验**：第 5 节九项逐项结果、编译与页数、数字抽样比对。
4. **未做/受限**：例如某处免责判定为需移入而非删除、未重跑求解、未做人工审核。

不得把未运行的检查写成已通过；不得为了清爽而删掉无处落位的免责内容。
