请重构 Hel10o/libai 项目四问论文的"模型假设"章节，并把它携带的免责内容落位到第十一章的限制小节。先通过 GitHub 读取：
https://github.com/Hel10o/libai/blob/main/q1234_assumptions_handoff/模型假设重构_模型指示文档.md
然后记录实际读取的提交 SHA（写作时基线为 bfb04e8）。

这是纯论文编辑任务：不改数值、不重跑求解、不增删一级章节。注意当前编号：模型假设是第三章；"适用范围与主要限制"已是 11.3（标签 sec:limitations，早前材料里写作 9.3）。一律用 \ref 引用，不要写死数字。\label{eq:future} 必须保留原位，validation_limits.tex:35 有 \eqref{eq:future} 引用它；11.3 的量化数字（45.59 kJ、4.801 kJ、9.50、164.10 W/m²、0.71952/0.89029、34.7454/28.0807 cm）一字不动。

要解决的问题：现在六条假设混了五类东西——物理简化、题面给定的定义（C=m_w/m_d，已在符号说明表）、题面给定的数值（h_T=25、h_m=8e-7 是附录2给的）、真正的假设、以及免责声明。而且第 2/3/4/5 条后半句的免责与 11.3/11.2 几乎逐字重复（对照表见文档 2.2 节）。另外最近给每条加的"目的"句属于建模思路，第二章"问题分析"已经逐问写过，重复。

改法：按文档第 3 节给出的八条草案替换（可微调措辞），每条只写"假定什么"，粗体引导句保留，目的句与免责句删去；补齐三类遗漏假设——表面交换取线性 Robin 形式、题表距离按中截面径向解释、忽略项（仅扩散传质无压力驱动流、环境量空间均匀、h_T 不区分对流与辐射）；把定义与题给数值从假设条中移出。删任何免责句前必须先确认 11.3/11.2 有对应表述，无对应则移入 11.3 或 11.4，不允许直接删掉造成信息丢失。第 6 条"不因某情景更接近 2-3 天而选择其参数"需先 grep 8.2 与 11.3/11.4 再定处置。

配套处理：问题分析末段的题表解释改为回指假设 1；检查 problem_model.tex:36 的 \Needspace{4\baselineskip} 在条目改写后是否还必要（现会把第 5、6 条推到新页）。

完成后重新编译，按文档第 5 节做九项一致性检查（重点是免责内容逐条可查、eq:future 引用有效、11.3 数字未变、正文 ≤ 30 页），新增本轮修订记录（含改前改后条目对照与免责落位核对表），更新 README、源稿 ZIP 与清单，然后按 AGENTS.md 提交并推送。汇报分四块：实际读取（含 SHA）、实际修改、实际核验、未做或受限。不得把未运行的检查写成已通过，不得为了清爽删掉无处落位的免责内容。
