"""Generate review text/tables from this review's computed diagnostics.
No numerical solve and no change to accepted files. Chinese narrative is reviewed
source text; numeric insertions are taken from the recorded postprocessing JSON.
"""
from pathlib import Path
import json
B=Path(__file__).resolve().parents[1]
v=json.loads((B/'data/mechanism_validation.json').read_text())
src=json.loads((B/'evidence/source_identity.json').read_text())
test=json.loads((B/'tests/results/guard_tests.json').read_text())
def write(name,text): (B/name).write_text(text.strip()+'\n',encoding='utf-8')
loc=v['locations'];by={(x['parameter'],x['location']):x for x in v['sensitivity_3h']}
commit=src['adopted_remote_commit'];matched=sum(x['matches'] for x in src['checked_file_identities'])
write('深化审查结论.md',f'''
# 第二问深化审查结论

**裁定：有条件保留当前答案。**不修改已验收的两张论文表及 `result2.xlsx`，不以缺少参数的相变模型替换现有有效模型。本轮需要修改的是机理解释、物理闭合表述、时域声明，以及后续复用时的缓存与输出保护。

适用条件仍为：从初始时刻统一采用附录3、固定圆柱中截面、局部双向耦合、等效水分Robin边界、前3小时输出。本轮没有发现支持推翻这些条件下数值答案的新反例，但它们并不是完整真实蒸发过程的充分物理描述。

## 1. 证据、收益与处理优先级

| 级别／问题 | 本轮证据或推导 | 改进收益 | 还缺什么 | 实际完成 | 是否改正式数值 |
|---|---|---|---|---|---|
| P0／结果不等于实测精度 | 复用9月11日验收；无内部响应观测 | 防止把453600个舍入一致扩大为实验验证 | 温度、失重、含水率响应 | 已明确证据边界 | 否 |
| P0／水分边界不可识别 | 原题未给质量基准映射；已建立系数—映射非唯一性推导 | 区分给定参数与假设 | 气相定义、解吸关系、干质量和动态失重 | 已完成，停止无观测参数拟合 | 否；有新观测后才可能 |
| P0／物理闭合 | 干密度矛盾与组成焓／携焓方程推导 | 避免仅加潜热、仅改乘积储存的伪修正 | 热焓参考态、真实质量通量、界面平衡 | 已完成三类模型对照和补测表 | 否 |
| P1／扩散机制解释 | H+M恒等式，三位置全轨迹与瞬时／区间变化 | 纠正D始终增加、累计项等于因果百分比 | 无需新观测 | 已实际生成10组图及源数据 | 否 |
| P1／时域表述 | 原PDF第二页与模板核对 | 不把前3h称为已算完数天 | 4h后环境、真正终点、长时验证 | 已裁定并给论文文字 | 否，3h工作解释保留 |
| P1／缓存与覆盖风险 | 原入口只看输入／文件存在；定向反例与代码检查 | 防止改模型后复用旧解，保护旧证据 | 无新物性数据 | 新目录补丁、{test['count']}项管理／核函数测试及实际入口tests阶段 | 否 |
| P2／补做更复杂物理 | 现有模型尚缺可辨识参数 | 避免无依据增加自由度 | 见补测优先级 | 仅闭合方案，未运行物理扩展 | 否 |

P0在此表示正式表述应先纠正，并不表示现有条件性数值解必须重新求解。潜热与边界定义缺失阻断的是“真实物理高精度预测”的主张；缓存缺陷阻断的是改变代码后未经核验的旧结果续用，不追溯否定已验收的新目录计算。

## 2. 本轮新增的主要计算结论

初始扩散系数为 {v['D_initial_m2_s']:.8e} m²/s。3小时的轴心、1 cm和表面D/D₀分别为{loc[0]['D_ratio_3h']:.4f}、{loc[1]['D_ratio_3h']:.4f}和{loc[2]['D_ratio_3h']:.4f}；尽管均高于初值，最后半小时已经分别净下降{-100*loc[0]['last_half_hour_D_relative_change']:.3f}%、{-100*loc[1]['last_half_hour_D_relative_change']:.3f}%和{-100*loc[2]['last_half_hour_D_relative_change']:.3f}%。

表面早期最低采样点在{loc[2]['global_sample_min_s']} s，D/D₀={loc[2]['D_ratio_min']:.6f}，之后回升。轴心、1 cm和表面在每秒轨迹上的最大值分别出现于{loc[0]['global_sample_max_s']}、{loc[1]['global_sample_max_s']}、{loc[2]['global_sample_max_s']} s；这些是采样极值，不是永久的机制切换时刻。

重用同一320区间网格的±20%情景后，3小时表面含水率对hₘ的归一化割线灵敏度为{by['hm','surface']['C_secant_elasticity']:.4f}，对hT为{by['hT','surface']['C_secant_elasticity']:.4f}。这是特定输出、时刻及扰动范围的确定性结果，不是置信区间，也不说明hT对早期温度不重要。

## 3. 不应再增加的工作

没有新物理关系、方程变更或新时域证据，不重复八级网格、两套谱解和完整3小时十阶段验收。不重新追查已有BDF警告，不屏蔽全局警告，也不移植其他食品的吸附参数。本轮不求第三问阈值时间或第四问收缩结果。

## 4. 新增与继承严格分开

旧Pro的未舍入解、原网格敏感性数据，与本地9月11日独立Radau审核各自保留来源。本轮只新做轨迹后处理、机理／闭合推导和管理代码回归；管理入口只实际跑tests阶段，不将其称为新十阶段全流程验收。

`portable`分支已由9月11日本地审核在Python 3.13.14、NumPy 2.3.5、SciPy 1.17.0下执行并通过全格审计；这是所读取的已有运行证据，不是本轮新执行的Excel导出。Microsoft Excel桌面原生打开仍未验证。

## 5. 版本与交付决定

实际远端提交为 `{commit}`，已含第二问及审核目录，不再是提示词准备时的旧提交。核对的{len(src['checked_file_identities'])}个文件身份中，原件、审核正文、入口和所查科学代码等{matched}项与上传包一致；交接提示词及MANIFEST发生版本变化，不能说整个仓库与ZIP完全相同。

最终动作是**保留旧Excel与60个表值，采纳新增机制分析与适用范围文字；未来程序复用改用独立新目录中的安全入口。**详细算法、来源及所有通过／未运行范围见本包README和各专题文件。
''')
rows='\n'.join(f"| {x['radius_cm']:g} | {x['H_3h']:.6f} | {x['M_3h']:.6f} | {x['net_log_3h']:.6f} | {x['D_3h']:.8e} | {x['D_ratio_3h']:.6f} |" for x in loc)
peaks='\n'.join(f"| {x['radius_cm']:g} | {x['global_sample_min_s']} | {x['D_ratio_min']:.6f} | {x['global_sample_max_s']} | {x['D_ratio_max']:.6f} |" for x in loc)
inc='\n'.join(f"| {x['radius_cm']:g} | {x['last_half_hour_delta_H']:.6f} | {x['last_half_hour_delta_M']:.6f} | {100*x['last_half_hour_D_relative_change']:.4f}% |" for x in loc)
elastic='\n'.join(f"| {label} | {by['hm',name]['C_secant_elasticity']:.6f} | {by['hT',name]['C_secant_elasticity']:.6f} | {by['hm',name]['T_rise_secant_elasticity']:.6f} | {by['hT',name]['T_rise_secant_elasticity']:.6f} |" for name,label in [('axis','轴心'),('r1cm','1 cm'),('surface','表面'),('rdr_mean','r dr加权平均')])
write('机制与不确定性分析.md',r'''
# 机制与不确定性分析

## 1. 数据身份和实际实施

本分析读取已验收的10801×21温度／含水率数组，包含初态；主分析选择轴心、1 cm和表面。参数灵敏度来自原Pro同一320区间网格的既有单因素情景，逐文件验证原清单SHA256后提取，并未重跑这些PDE或当作本地Radau重算数组。

文件 `data/mechanism_timeseries.csv` 保存三个位置全部每秒状态、D、H、M、导数和诊断标记，`data/mechanism_unrounded.npz`保留双精度。`reused/sensitivity_provenance.json`记录7份原数组的来源与哈希；本包保存其无插值的精确轨迹投影。

## 2. 累积贡献：严格恒等式，不是因果份额

由题给经验式，定义初始状态T₀=301.15 K、C₀=2.55，直接相减得到：

\[
\ln\frac{D(t,r)}{D_0}=H(t,r)+M(t,r),
\quad H=3850\left(\frac1{301.15}-\frac1{T_K}\right),
\quad M=0.45\left(\frac1{2.55}-\frac1C\right).
\]

''' + f'''三个位置全部采样点的重构最大绝对残差为 **{v['identity_max_abs_log_error']:.3e}**。这一检查验证代数分解和数值提取一致，不等于验证材料扩散率模型真实；H与M是沿实际耦合轨迹的对数贡献，不应换算成独立干预的因果百分比。

| 半径/cm | H(3h) | M(3h) | H+M | D(3h)/(m²/s) | D(3h)/D₀ |
|---:|---:|---:|---:|---:|---:|
{rows}

![图1 三位置扩散率与初值之比](figures/01_D_competition.png)

图1：D₀={v['D_initial_m2_s']:.8e} m²/s。表面早期失水较快，扩散率首先低于初值；之后升温促进作用累积增大，D上升。到较晚阶段，温度增幅缩小而含水率仍降低，D出现峰值后净回落，但并未回到初始水平。

| 半径/cm | 最低采样时刻/s | 最小D/D₀ | 最大采样时刻/s | 最大D/D₀ |
|---:|---:|---:|---:|---:|
{peaks}

表面再次越过D₀约在{loc[2]['return_to_initial_D_s_linear_between_samples']:.2f} s，是相邻每秒样点之间的线性定位，不是重新求解的精确事件时刻。表中的峰值也只表明已采样轨迹上的极值，不能据此宣称此后任何时刻都由同一机制占优。

![图2 轴心累积项](figures/02_log_contributions.png)

![图3 1 cm累积项](figures/03_log_contributions.png)

![图4 表面累积项](figures/04_log_contributions.png)

图2—4：正的H表示相对初始温度的净促进，负的M表示相对初始含水率的净抑制。两者通过耦合方程相互影响；若冻结温度或含水率，轨迹自身也会改变，不能把本图当作那类反事实模拟的结果。

## 3. 瞬时竞争与稳健的有限时间增量

''' + r'''
\[
\partial_t\ln D=G_T+G_C,
\qquad G_T=\frac{3850}{T_K^2}T_t,
\qquad G_C=\frac{0.45}{C^2}C_t.
\]

以1、2、5 s间距的五点四阶差分计算导数，不跨越环境60 s折点；折点取右侧格式，终点取左侧格式。初始0—9 s不作瞬时机制判定，保留原状态；差分产生的任何极小负含水率增量不解释为实质吸湿。

''' + f'''1与5 s导数方案在10 s以后最大总率差为{v['derivative_check']['step_1_vs_5_max_abs_s-1_after10']:.3e} s⁻¹，在60 s以后为{v['derivative_check']['step_1_vs_5_max_abs_s-1_after60']:.3e} s⁻¹。早期边界层的导数精度明显弱于状态精度，不能给它附加四位小数可靠性的主张。

脚本将绝对总率小于五倍步长方案差或1e-10 s⁻¹的点标为“符号未分辨”；该阈值是保守诊断，不是严格误差界。同时输出不依赖数值求导的60 s精确对数增量，作为发生小波动时的辅助判断依据。

![图5 轴心瞬时贡献](figures/05_instantaneous_rates.png)

![图6 1 cm瞬时贡献](figures/06_instantaneous_rates.png)

![图7 表面瞬时贡献](figures/07_instantaneous_rates.png)

表面响应会跟随环境的小幅波动，G_T可以变成负值；故不宜把温度项始终称为“升温促进”。2—3h内，三方案符号诊断给出的表面负总率采样比例约为{100*loc[2]['rate_negative_time_fraction_2to3h']:.1f}%，正总率约为{100*loc[2]['rate_positive_time_fraction_2to3h']:.1f}%，余下为未分辨或端点排除。

更稳健的阶段结论来自最后半小时增量：三个位置的H仍增加，但M下降幅度更大，所以D净下降。它不要求每一秒单调，也不受对某个极小导数作符号分类的影响，可作为论文中的主要机理解释。

| 半径/cm | ΔH，2.5—3h | ΔM，2.5—3h | D(3h)/D(2.5h)−1 |
|---:|---:|---:|---:|
{inc}

## 4. “水分主导”必须指定可检验的含义

3h时径向温差约{v['response_3h']['radial_T_span_K']:.4f} K，最冷点与当时空气温差约{v['response_3h']['max_T_lag_to_current_air_K']:.4f} K；径向含水率差仍约{v['response_3h']['radial_C_span_kgkg']:.4f} kg/kg，有效水分积分还剩初始的{100*v['response_3h']['remaining_effective_water_fraction']:.4f}%。这些支持“当前热响应接近均匀，而水分仍明显非均匀”。

它不直接证明所有后期状态都由同一种阻力控制，更不能从R²/D算出真实达标时间。“响应快慢”“达到阈值的瓶颈”“参数对指定输出的灵敏度”是不同命题；本轮只对前3h轨迹和已运行参数情景作判断。

## 5. 既有参数扰动的无量纲重表达

''' + r'''
以p表示hₘ或hT，使用两端±20%情景定义中心割线量：

\[
E_{C,p}^{(20\%)}(t,r)=\frac{C_{1.2p}(t,r)-C_{0.8p}(t,r)}{0.4C_p(t,r)}.
\]

温度用升温幅度T−28 ℃归一化，避免以摄氏绝对数值作分母；升温不足0.05 K的点不报该比值，只保留绝对差。0.05 K是诊断屏蔽标准而非题给物理参数；所有情景按同网格基线比较，不混用Richardson解。

''' + f'''| 3h位置 | E(C,hₘ) | E(C,hT) | E(升温,hₘ) | E(升温,hT) |
|---|---:|---:|---:|---:|
{elastic}

![图8 含水率对hₘ的割线灵敏度](figures/08_sensitivity_hm.png)

![图9 含水率对hT的割线灵敏度](figures/09_sensitivity_hT.png)

hₘ对3h含水率的影响更强，但hT的±20%情景在整个3h内可使表面温度与同网格基线最多相差{by['hT','surface']['maximum_abs_T_change']:.4f} K。不能只依据末时刻表面含水率对hT不敏感，推断换热过程在全时域都不重要。

PCHIP、冻结初始物性两份轨迹亦包含在投影文件中；本轮不额外拟合或重新运行。PCHIP差异沿用原验收记录，其外部输入重构误差不等于物性不确定性。这里没有给概率分布、蒙特卡洛区间或参数“最优值”。

## 6. 映射小扰动诊断不是识别结果

固定状态时q̂=hₘ(C_s−b)，对b的局部对数导数为−b/(C_s−b)，初始约{v['boundary_fixed_state']['b_elasticity_initial']:.6f}，3h约{v['boundary_fixed_state']['b_elasticity_3h']:.6f}。这只说明在当前恒等映射附近的小相对扰动瞬时效应有限，不约束未知映射的真实偏离。

![图10 固定状态边界映射局部灵敏度](figures/10_mapping_flux_diagnostic.png)

当C_s接近b时，该比值可能很大，改变b还会改变长期平衡；改变hₘ则主要改变接近平衡的速率。不能将“前3h局部小扰动灵敏度较弱”当作环境量已正确映射为药材平衡含水率的证据。

## 7. 数据与复现

运行 `python source/q2_mechanism.py` 可用本包内已有数组重新生成上述全部图、表和源数据，不需要访问网络，不调用任何PDE求解器。CSV除有明确屏蔽规则的温度归一化列外均为有限数值；未舍入值同时保留NPZ格式。

图题采用英文位置、时间和SI单位，正文图注明确空间口径；每张图独立绘制，无手选颜色。文件总索引见 `figures/manifest.json`；半小时表只为机制分析，不替换题目原有两张论文结果表。
''')
write('物理闭合与数据需求.md',r'''
# 表面边界可识别性、物理闭合与补测需求

## 1. 已知参数与未知物理关系不是一回事

原题给出药材干基含水率、烘房水分数值、附录3物性和前两问的任务；附录2给hₘ=8×10⁻⁷ m/s，第二问继承它是一项假设。原题未定义环境kg/kg的分母，也未给环境量到本药材平衡含水率的映射。[1]

当前记环境原始量为y∞，实际进入固相边界的是b(t)=g(y∞,T_s,…)；基线额外取g(y∞)=y∞。这个等效闭合产生确定数值解，但并不把“空气湿度比”和“药材干基含水率”变成同一种物理量。

| 项目 | 题面／附件给了什么 | 仍需额外假设或数据 | 当前处理 |
|---|---|---|---|
| 环境驱动 | 时间、温度、水分数值；覆盖0—14400 s | 环境水分定义、压力、测量位置／误差 | 线性插值，视为外部已知驱动 |
| 内部有效输运 | 附录3的k(C)、S(C)、D(T,C) | 经验关系的实验范围与不确定性 | 本问按题给式使用，不重新反演 |
| 外部交换 | 附录2的hₘ数值及m/s单位 | 第二问是否延用、系数按哪一种驱动定义 | 明示继承且采用等效尺度 |
| 气固映射 | 未给 | 本药材解吸关系或a_w(C,T) | 取恒等映射为题设简化，不宣称识别 |
| 真实界面质量通量 | 未测 | 干质量、失重历程、有效面积及相变位置 | 不把hₘ(C_s−b)直接当kg/(m²s) |
| 热焓与耦合 | 有有效热容量公式 | 组成焓、携焓、相变焓及参考态 | 无显式项是局限，不是小量定理 |

目前唯一实测响应是外部驱动，不是样品内部状态。把现有模型预测的C_s、T_s或失水量再当成观测来拟合g、hₘ或相变参数，会形成自我验证。给定输入后没有样品观测残差，不能形成这些未知量的有效识别问题。

即使将来同时知道某条表面轨迹C_s和归一化通量q̂，只要g允许任意随时间变化，对任意正hₘ*都可构造b*(t)=C_s(t)−q̂(t)/hₘ*重现该通量。只有独立约束映射形式或平衡关系，才可能分离交换速率与映射。

此构造是非唯一性的理论说明，不是本轮拿预测值作新的标定。对有限参数族，额外观测能否识别仍需检查灵敏度矩阵的秩、参数相关性和实验激励；这里没有拟合所谓最优参数，也没有估计置信区间。

## 2. 交换速率与平衡值有不同作用

对固定、均匀的长期环境且D>0、hₘ>0，当前模型的无通量平衡为C=b。hₘ主要影响接近该平衡的速率，b则直接改变平衡位置；二者在瞬时通量中同时出现，因此仅看短时整体失重也可能高度相关。

已有hₘ±20%情景用于确认速率敏感性，不能识别b。固定当前C_s时有∂lnq̂/∂lnhₘ=1、∂lnq̂/∂lnb=−b/(C_s−b)。后一个比值在当前轨迹较小，只约束恒等映射附近的小相对扰动，不保证任意真实映射误差很小。

文献提供的结构也要求区分这两个角色。da Silva等PDF第4页§2.2.4式(9)的外界量是干基平衡含水率M_eq；Adrover等PDF第5页式(5)—(7)需用解吸等温关系将固相状态映射为气相界面湿度。[2,3]

## 3. 不能因传质系数单位相同就原值沿用

若改用气相体积水分浓度，j_s=k_g(c_v,s−c_v,∞)，其中k_g虽以m/s计，但乘的是kg/m³；原边界hₘ乘的是kg水/kg干物质。因此，形式中的同量纲系数并不具有相同数值含义。

在线性化c_v,s≈K C_s+常数、恰当选取b并保持干密度口径时，才可得到ρ_d hₘ^eq=k_g K。若改用分压，j_s=K_p(p_v,s−p_v,∞)，则K_p单位为kg/(m²·s·Pa)，更不能直接代入原题hₘ。

## 4. 三类模型的完整闭合对照

下面固定模型均将圆柱视为连续介质，固体与水分具有共同局部温度；假定无化学反应和干物质流失。严格扩展还需说明压力功、骨架变形功和相间非平衡能否忽略，不能仅从当前单温度模型自动推出。

| 要素 | 当前有效模型 | 固定干骨架的组成守恒模型 | 骨架运动／收缩模型 |
|---|---|---|---|
| 状态量 | T、C | T、C，已定常ρ_d；必要时a_w闭合 | T、C、ρ_d、骨架速度v_s及边界位置 |
| 质量基准 | C为干基量；参考ρ_d可约去 | c_w=ρ_d C，ρ_d=m_d/V固定 | c_w=ρ_d C，ρ_d随材料运动由守恒决定 |
| 密度 | ρ(C)只进入有效S(C) | 真实ρ_wet=ρ_d(1+C)，不能再任意指定不相容的ρ(C) | ρ_d与体积Jacobian相容；ρ_wet=ρ_d(1+C) |
| 储存／热焓 | S(C)T_t，未声称存在完整组成焓状态 | 每kg干物质热焓Hcal(T,C)及偏水焓；S_phys=ρ_d Hcal_T | 相同组成热焓，另含骨架对流／必要机械项 |
| 通量 | −k∇T、归一化−D∇C | q=−k∇T，j=−ρ_dD∇C，能量含携焓通量 | 相对骨架的扩散通量，加材料对流 |
| 几何 | 固定R、L，v_s=0 | 固定R、L，水离开后孔隙状态需相容 | R、L或变形规律，网格速度与材料速度分开 |
| 初值／对称条件 | 题给T₀、C₀；轴心对称 | 加md、初始真实密度和热焓参考态 | 加初始形状、材料分布和运动边界 |
| 表面 | 等效b(t)、继承hₘ；仅对流热边界 | 气固平衡＋实际j_s＋同一j_s的界面焓差 | 移动材料边界上的同一相容热质交换 |
| 新未知量 | g的真实性无法验证 | a_w、气相状态、K_p或k_g、组成／相变焓、md | 另需收缩／变形本构或实测R(t)、L(t)及内运动假设 |
| 与当前模型关系 | 正式条件性基线 | 仅在相容有效容量、忽略携焓梯度及相变热等明确近似下可约化 | 还须v_s≈0、几何近似固定；本题未证明这些误差小 |
| 本轮状态 | 复用已验收解并新做轨迹分析 | 方程级扩展方案，未求解 | 方程级扩展方案，未求解 |

## 5. 密度矛盾：需要改口径，不应偷偷产生干物质

若把ρ(C)=650+128C当作真实湿体密度，则ρ_d=(650+128C)/(1+C)，并有ρ_d′(C)=−522/(1+C)²。固定体积且骨架静止时，这会在失水过程中增加局部干物质密度，与干物质不产生的假设不相容。

''' + f'''沿已验收状态，该解释会使初始干密度{v['density_diagnostic']['implied_rho_d_initial']:.4f} kg/m³变成3h轴心、1 cm、表面的{v['density_diagnostic']['implied_rho_d_3h_0_1_2cm'][0]:.4f}、{v['density_diagnostic']['implied_rho_d_3h_0_1_2cm'][1]:.4f}、{v['density_diagnostic']['implied_rho_d_3h_0_1_2cm'][2]:.4f} kg/m³。它是解释冲突的诊断，不是药材实际干密度的预测。

''' + r'''
因此保留当前答案时，继续将经验ρc_p解释为有效容量；要升级严格组成守恒，应选择与固定ρ_d相容的真实密度／热容关系。要同时保留真实变密度，还必须引入相容的材料运动和几何，不能只改一个乘子。

## 6. 一套量纲与参考态相容的扩展骨架

以下是本轮独立推导的模型结构，不是声称从某篇文章逐式复制。以固定骨架、恒定参考压力、忽略动能和机械功、内部无相变为条件，定义ρ_d为kg干物质/m³，Hcal(T,C)为J/kg干物质。

\[
\rho_d C_t+\nabla\cdot\boldsymbol j_w=0,
\quad \boldsymbol j_w=-\rho_dD\nabla C,
\quad \boldsymbol q=-k\nabla T.
\]

\[
\mathcal H(T,C)=h_d(T)+C h_\ell(T)+h_b(T,C),
\quad \bar h_w=\partial_C\mathcal H,
\quad S_{\rm phys}=\rho_d\partial_T\mathcal H.
\]

其中h_b表示结合、混合等组成贡献；只有其物理基准和本构被补齐，Hcal、偏水焓与热容量才相容。题给比热只限制温度导数，并不能独立确定随组成变化的任意函数或水分迁移携带的热焓。

总能量收支及其由质量方程消元后的温度形式为：

\[
\partial_t(\rho_d\mathcal H)+\nabla\cdot(\boldsymbol q+\bar h_w\boldsymbol j_w)=0,
\]
\[
\rho_d\mathcal H_TT_t=\nabla\cdot(k\nabla T)-\boldsymbol j_w\cdot\nabla\bar h_w.
\]

这一推导要求使用同一组成热焓及相应偏水焓。它说明乘积储存、组成项和携焓不能割裂处理；对理想组分且偏水焓仅依赖温度，最后一项变为−c_p,ℓ j_w·∇T。当前忽略它是有效简化，不是已定量验证的小项。

热焓参考态也要一致。将Hcal改为Hcal+a_d+C a_w并同时将偏水焓改为偏水焓+a_w，质量守恒保证能量方程不变；单独用∂t[S(C)(T−T_ref)]却不补齐迁移热焓，通常不具备这种组成参考态相容性。

以上a_d、a_w是参考焓常数，与水活度a_w(T,C)重名时容易混淆；在代码或正式论文中应将参考常数另记为A_d、A_w。此处建议最终采用A_d、A_w记号，水活度仍记a_w，避免把能量参考平移当作界面活度。

表面以外法向通量j_s>0表示失水，采用以下同一通量的闭合：

\[
j_s=\boldsymbol j_w\cdot\boldsymbol n
=K_p\{a_w(C_s,T_s)p_{\rm sat}(T_s)-p_{v,\infty}\},
\]
\[
\boldsymbol q\cdot\boldsymbol n=h_T(T_s-T_\infty)+q_{\rm rad,out}
+[h_v(T_s)-\bar h_w(T_s,C_s)]j_s.
\]

K_p、a_w及气相状态需要独立定义。当表面变冷导致驱动力反号时，质量通量和相变热也应同步转为凝结方向；不能保留原等效强失水轨迹而单方面扣除潜热，再把极低温结果称为真实答案。[3]

式中相变焓只计入一次；能量总通量在表面为对流、辐射及h_vj_s。若相变被假定只发生在表面，不能再把−L_vρ_d C_t当体热源，因为局部C_t还包括与相邻区域的扩散交换，并非局部蒸发率。

若考虑内部相变，必须另设液相／气相储存、相变率及相间质量能量交换，不能只从总含水率下降推得内部蒸发。COMSOL官方热湿接口也区分表面和域内潜热源，并另外处理扩散携焓；该文档只作机制组织佐证，不提供本药材参数。[4]

''' + f'''仅作尺度诊断，若另把初始976.4 kg/m³视为湿体密度、固定参考干密度并将有效失水全部视为蒸发，3h条件性失水约{v['density_diagnostic']['conditional_lost_water_kg_reference_dry_density']:.6f} kg。旧模型边界热输入为{v['density_diagnostic']['reused_effective_heat_input_J']/1000:.4f} kJ，等量相变热对应的L_v约{v['density_diagnostic']['latent_equal_effective_heat_input_at_lambda_J_kg']/1e6:.6f} MJ/kg。

本诊断没有指定实际L_v，也没有运行潜热PDE；热输入来自旧模型，其温度改变后也会变化。它只给出检查相变是否可能为小项的条件尺度，不能转化为新真实失水量、修正温度或已测热效率。

''' + r'''
## 7. 运动与辐射扩展的边界

运动模型至少加入ρ_d,t+∇·(ρ_d v_s)=0和ρ_d(C_t+v_s·∇C)=−∇·j_w，能量式相应含ρ_dHcal v_s。材料表面的移动应与骨架速度相容，不能仅在固定网格公式中替换R(t)而忽略干物质守恒。

附件2仅有半径轨迹，不能自动确定长度、内部变形或密度分布。第二问不直接接入第四问的收缩数据和物性；若未来独立研究收缩，应在新情景中同时声明几何、材料速度、质量权重与输出坐标。

原题写的是“对流换热系数”，没有证据把它解释成包含辐射的总系数。额外辐射边界可取εσ(T_s⁴−T_w⁴)的条件形式，温度用K；壁温、视角因子和发射率未给，故本轮不指定无来源固定值或新增辐射情景。

## 8. 有先后次序的最小补测包

| 优先级 | 信息／测量 | 主要消除的歧义 | 能否单独完成全部识别 | 可核验的验收 |
|---|---|---|---|---|
| 先确认口径 | 环境水分分母、压力或可转换的RH、传质系数的驱动定义 | 空气与固相变量误配、系数不能换算 | 否，但所有进一步闭合的前提 | 单位、分压、体积浓度互相转换一致 |
| 独立静态关系 | 本药材在相关温度范围内的解吸／水活度—干基含水率关系，明确解吸支与预处理 | b或a_w函数的不确定性 | 不能单独定外部交换速率 | 留出工况下平衡含水率预测，不用动态预测值作标签 |
| 配套动态响应 | 干质量md、样品质量m(t)、表面温度及至少一处内部温度 | 真实失水通量、热质反馈和容量／边界相关性 | 对受限参数族才可能；需检验激励与秩 | 用同一组参数同时解释温度与失重，留出试验核对 |
| 仅当阻力难分 | 少量不同风速／尺寸工况，或独立内部含水率剖面 | 内部D与外部交换阻力的混淆 | 依赖具体参数化 | 分别拟合后跨工况预测，检验参数可迁移性 |
| 仅当热收支需要 | 壁温／视角、托盘接触和表面状态；组成／解吸热焓资料 | 辐射、接触和相变焓互相补偿 | 不保证，须统一能量参考态 | 热量收支与温度、失重共同闭合 |

“最小”在这里指能逐步消除当前主要歧义的信息类别，不是保证任意高维本构可唯一识别的实验数量。若这些信息无法获得，保留等效模型和确定性敏感性，明确不确定性来源，不给唯一真实校正值或概率区间。

## 参考来源与实际阅读层级

[1] 原题PDF第二、四页已用上传同Git对象原件渲染核验；附录1／2见第三页。原题和原始XLSX字节身份见evidence/source_identity.json。

[2] da Silva W P等. Journal of Food Engineering, 2014,141:65–73. DOI:10.1016/j.jfoodeng.2014.05.010。实际阅读仓库readable/papers/da-silva-2014/pages/004.md，PDF第4页§2.2.4关于M_eq的完整正文文本。未取得该PDF图像；乱码公式不作为逐式抄写依据。

[3] Adrover A等. Foods,2020,9:1577. DOI:10.3390/foods9111577。实际读取仓库PDF第5、6页文本及出版社HTML方法部分，并目视核验上传第6页原页面图，定位§3.2式(8)—(13)。全文PDF未下载成功；此处只借鉴变量／通量组织，不移植梨的参数。https://www.mdpi.com/2304-8158/9/11/1577

[4] COMSOL 6.3官方文档. The Heat and Moisture Transport Multiphysics Interfaces，Latent Heat Sources和Diffusive Flux of Thermal Enthalpy部分。实际读取该页面说明；不以其替代本题材料闭合。https://doc.comsol.com/6.3/doc/com.comsol.help.heat/heat_ug_multiphysics_interfaces.11.36.html
''')
# Remove a deliberate reference-symbol ambiguity at the source, not by hand editing outputs.
p=B/'物理闭合与数据需求.md';s=p.read_text();s=s.replace('Hcal+a_d+C a_w并同时将偏水焓改为偏水焓+a_w','Hcal+A_d+C A_w并同时将偏水焓改为偏水焓+A_w')
s=s.replace('以上a_d、a_w是参考焓常数，与水活度a_w(T,C)重名时容易混淆；在代码或正式论文中应将参考常数另记为A_d、A_w。此处建议最终采用A_d、A_w记号，水活度仍记a_w，避免把能量参考平移当作界面活度。','其中A_d、A_w分别是干物质和水的常数参考焓平移，水活度另记a_w(C,T)。二者物理含义不同；前者只改变能量零点，后者参与界面平衡。保持记号和质量方程一致，才能检查温标与热焓参考态的独立不变性。')
p.write_text(s)
write('时域与论文修改建议.md',r'''
# 时域裁定与可直接使用的论文段落

## 1. 最终裁定：保留3小时文件，但不宣称无歧义的数天全程交付

原PDF第二页要求“建立整个烘干过程”的模型，并明确论文只列3h内每0.5h结果；完整逐秒Excel未直接给结束时间。模板和附录1规定单位与布局，但没有提供一个数天终点，因此题面存在需要披露的输出时域解释。

本轮保留现有1—10800s文件：它覆盖论文明确展示时域的全部逐秒状态，属于可解释的工作口径。不能因此声称已经消除“整个过程”措辞的全部歧义，也不能把现有交付称为已经计算到真实干燥结束。

| 层次 | 当前事实或选择 | 严禁扩大的结论 |
|---|---|---|
| 方程适用意图 | 附录3从初态统一使用，不在人为1800s处分段换式 | 方程形式覆盖两阶段，不等于数天初边值问题已经闭合 |
| 外部输入 | 原始241点，0—14400s | 超过4h继续保持任意环境不是测量事实 |
| 论文表 | 0.5、1、1.5、2、2.5、3h | 不能以“2—3天”背景替代真正结束条件 |
| 已验收Excel | 1—10800s，21个半径，453600个结果 | 不叫数天全过程，不自动采用第三问阈值 |
| 本轮新增 | 前3h机制、物理闭合与管理补丁 | 未进行新的长时PDE、达标定位或收缩计算 |

若队伍决定将“完整结果”解释为直至第三问的达标时刻，可以在后续求解统一第三问轨迹后补充更长result2副本；必须明确该停止条件来自第三问，并保持前3h结果一致。本轮不默认采纳该阈值，也不强行选择48或72h。

## 2. 长时扩展之前必须补齐的契约

首先确定4h后T∞与b的延拓方式，其次明确停止规则和输出频率，再选择所需验证。现有数学形式可以写到更长时刻，但当前汇总、Excel审计及论文表约定3h；只修改某个solve_fv的end参数不构成完整长时交付。

尾段均值保持与末值保持只能作为可比较假设，不是观测延长。建议需要长时行为时先只比较这两种环境延拓；均值必须说明样本平均或分段线性输入的时间平均，不能在代码、正文与记录间混用。

''' + f'''| 4h后候选延拓的数值来源 | 温度/℃ | 等效环境量/(kg/kg) |
|---|---:|---:|
| 原附件最后一条观测 | {v['time_scope']['last_environment_value'][0]:.6f} | {v['time_scope']['last_environment_value'][1]:.8f} |
| 3—4h的61个样本算术平均 | {v['time_scope']['tail_sample_mean_3to4h'][0]:.6f} | {v['time_scope']['tail_sample_mean_3to4h'][1]:.8f} |
| 3—4h分段线性输入的时间平均 | {v['time_scope']['tail_time_mean_linear_3to4h'][0]:.6f} | {v['time_scope']['tail_time_mean_linear_3to4h'][1]:.8f} |

''' + r'''
表中数值由原附件实际重新计算，但三种延拓均未用于本轮新增PDE求解。建议比较关键径向状态、若明确采用的终止时间、总有效失水和边界平衡残差；结论只在所选择的模型与外推假设下成立。

只有确实延伸时，才重新检查低C使D退化、表层分辨率、正值和非线性边界分支，以及长时轴向端部影响。已有3h四位输出、短时边界层和二维对照都不是数天情景的严格误差保证；这不构成当前3h答案的新数值缺陷。

## 3. 可直接插入正文的段落

### 3.1 模型适用范围和变量解释

第二问从初始时刻统一采用附录3，建立固定圆柱中截面的有效热质传输模型。经验ρc_p用于当地有效热容量，含水率按干物质质量归一化；固定参考干密度只用于有效水分守恒解释，不将其同时视为严格真实变密度样品。

外部环境由附件1给定，并以分段线性插值构造输入。题面没有提供空气水分量与药材平衡含水率的关系，故本问采用数值恒等的等效边界映射，并继承交换系数；这是一项闭合假设，而非气固平衡关系已被实测识别。

### 3.2 扩散机制与空间差异

''' + f'''将扩散系数相对初值的对数变化分解为温度项H与含水率项M。3h时轴心、1 cm和表面的D/D₀分别为{loc[0]['D_ratio_3h']:.4f}、{loc[1]['D_ratio_3h']:.4f}和{loc[2]['D_ratio_3h']:.4f}，说明在该轨迹的累积变化中，升温促进大于失水抑制；分解并不表示独立的因果贡献百分比。

表面扩散率在早期短暂降低，随后回升，并在约{loc[2]['global_sample_max_s']/3600:.3f}h取得每秒轨迹的最大值。最后半小时三个位置的温度项仍增加，但含水率抑制更强，D分别净下降{-100*loc[0]['last_half_hour_D_relative_change']:.3f}%、{-100*loc[1]['last_half_hour_D_relative_change']:.3f}%、{-100*loc[2]['last_half_hour_D_relative_change']:.3f}%，体现阶段性竞争。

''' + r'''
### 3.3 参数敏感性和验证范围

现有单因素情景表明，3h含水率对hₘ的相对变化比对hT更敏感，但hT对早期温度仍有可见影响。因此应针对输出量和时刻讨论主导参数，而非仅凭一个扩散时间尺度作全局判断；扰动范围为确定性情景，不作置信区间解释。

数值验证支持当前模型、参数和输入下的离散精度，不说明缺失的界面平衡、潜热或携焓机制足够小。真实蒸发模型需要以相同质量通量联合闭合气固平衡和界面能量，并补齐组成热焓与密度基准，不能只追加一个吸热项。

### 3.4 输出时域

本文模型统一描述预热与恒温阶段的有效传输，按题目明确展示要求给出前3h的结果。result2.xlsx保存1—10800s、径向0—2cm每0.1cm的完整逐秒状态；这是对本问Excel时域的工作性解释，不将其称为数天全过程结果。

附件1仅覆盖前4h，进一步求取干燥终点必须明确环境延拓和停止条件，并重新验证长时低含水率与端部影响。本文没有以背景中的2—3天设定固定终点，也没有在第二问结果中默默引入后续问题的收缩数据或停止阈值。

## 4. 正文与支撑材料如何取舍

正文优先保留扩散率比图、H/M表及上述结论；瞬时导数、全时域灵敏度、缓存测试和每次访问记录放支撑材料。不要把本轮审阅过程写成参赛队已完成的人工作业；最终采纳和人工核对仍应由队伍逐项记录。

题给两张温度／含水率结果表不改。若合并到正式论文，应保留Richardson与独立验证的简洁方法说明，不删掉外推后声称原FV2560独自保证全部四位一致；实现和历史核验细节无需占据正文主要篇幅。
''')
write('程序复用改进与验证.md',f'''
# 有限程序复用改进与实际验证

## 1. 已证实的问题与修改边界

旧run_all.py在resume时主要比较输入哈希，并以状态中的passed或NPZ／JSON是否存在决定复用。修改物性、边界、积分配置或源码后，仍可能复用旧结果；任意非空目录只要没有run_state.json，也并非完整地受到保护。

这不否定本地9月11日的新目录十阶段验收。本轮只在 `runtime/` 新副本修改入口管理，并增加 `source/q2_run_guard.py`；所有原科学算子、时间积分、Richardson、谱解、二维方程、导出和论文生成源文件均按原字节复制。

## 2. 已实现的最小安全约束

| 约束 | 实现 | 实际验证 |
|---|---|---|
| 源码／配置绑定 | 每阶段绑定相关源码、整个入口、管理器、输入及Python/NumPy/SciPy版本；入口哈希包含硬编码网格、容差、时域；额外配置字典亦纳入 | 物性源码、时域入口、额外配置及环境变化均使相关缓存失效 |
| 阶段依赖 | assemble依赖六个计算／测试阶段，export和report依赖assemble，audit依赖结果／Excel／正文 | 依赖缺失拒绝汇总；上游文件损坏使下游失效 |
| 绘图不触发无关计算 | report源码只进入report及audit签名 | 只改q2_report.py，数值阶段仍可复用 |
| 模板局部失效 | 模板哈希与导出引擎纳入excel，传给audit | 模板变化只使excel和audit失效 |
| 完整阶段复用 | 只有完整阶段及所有输出哈希匹配才复用；不凭单个NPZ存在续算 | 中断／失败阶段不复用，篡改文件不能通过 |
| 输出保护 | 拒绝输入、源码、第一问、已验收第二问／审核树、任意未管理非空目录；检查解析后路径及内部符号链接 | 覆盖、符号链接绕过、复制归属标记、legacy缓存均被拒绝 |
| 明确合法续算 | 新目录由schema=2标记绑定绝对输出目录及run ID；必须显式--resume | 同源未变完整阶段可正常跳过 |

配置变化不是靠手写“模型版本字符串”识别，而由执行源码字节、入口中的实际配置和相关依赖共同约束。修改入口会保守地使全部阶段失效；只改独立绘图文件不会。依赖映射是显式的，未来新增模块必须同步登记其依赖，不能宣称对任意未来插件自动安全。

失败阶段从该阶段起点重新运行，不复用原二维阶段的无签名内部checkpoint。这样少了细粒度中断恢复，却避免把未验证的旧状态接到新模型；本轮优先保证可追溯性，不建设复杂的任务调度系统或自动迁移旧缓存。

## 3. 实际执行与范围

`tests/test_run_guard.py`的 **{test['count']}项**测试均通过，包括真实的两次60s、40区间核函数计算。原与副本温度、含水率的未舍入数组最大差均为0，核心源码SHA256一致；该计算只验证管理修改未改算子，不替代3h物理模型验收。

另外真实运行了新入口的tests阶段，返回0；随后同目录合法resume输出REUSE VERIFIED tests而未重新计算。未带resume写非空目录，以及尝试把输出写进已验收结果目录，都实际返回拒绝。记录见 `tests/results/entry_integration.json`。

本轮没有运行新入口的全十阶段流程，没有新增3h主数值解或重新导出正式Excel。原十阶段、独立Radau及全部453600值一致性来自被复用的验收证据；不能把管理测试中的文本夹具说成真实数值参考解。

## 4. 使用方式

在本包根目录可先执行管理与核函数回归：

```bash
python -m pip install -r requirements.txt
python tests/test_run_guard.py
python tests/check_entry.py
```

`check_entry.py`在临时新目录实际运行tests阶段并记录合法／非法续算行为。它会更新本包自己的tests/results，不触碰原已验收目录；输出中COMPLETE: False是只执行一个阶段的正常状态，不是十阶段计算失败。

未来确需新完整计算时，使用本包中的新副本和新的输出目录：

```bash
python runtime/run_all.py --out work_new --excel-engine portable
python runtime/run_all.py --out work_new --excel-engine portable --resume
```

上面完整计算命令是可运行入口说明，**本轮未用它运行all阶段**。程序拒绝把旧交付目录作为合法缓存接管；不要仅将patch直接写入原q2_final_delivery再运行默认输出，保护机制会拒绝该目录树。

`patches/run_all.patch`和`patches/q2_run_guard.py`提供最小源码差异；优先直接使用已给出的runtime完整新副本。Windows不必安装patch命令；数值依赖沿用现有版本，portable不需要artifact_tool。

## 5. 历史证据的更新与保留

portable导出已由2026-09-11本地审核实际验证，不能再笼统写“此分支未测”。但本轮未用Microsoft Excel桌面打开工作簿，也不重复输出完整Excel；此前BDF警告已经有隔离诊断，不修改第三方库或屏蔽全局警告。

原Pro清单另包的26个中间NPZ，不伪造为本包已齐全。为本轮机理需求，只从此前上传的原Pro验证数组压缩包中提取7个既有情景并核对哈希；它们与本地Radau／重跑证据在目录和说明上分别标识。
''')
identityrows='\n'.join(f"| `{x['remote_path']}` | {'相同' if x['matches'] else '不同'} | `{x['actual_upload_git_blob']}` |" for x in src['checked_file_identities'])
write('evidence/source_access.md',f'''
# 本轮来源、版本和实际访问层级

## 1. 采用的版本

读取日期为2026-09-11。通过GitHub连接先读根README.md、readable/README.md，再查询main；实际提交为 `{commit}`，父提交为 `1942d7506632e94853b32772c7c9bb4ba3a3c0be`，提交时间UTC 2026-09-11 06:06:03。

当前远端已纳入q2_final_delivery、review_q2_20260911、q2_refinement_handoff及阶段论文。上传提示词仍写未推送，远端提示词已改成同步完成；本轮继续以上传已验收数值为分析基底，并核对需要的远端对象身份。

## 2. 精确的文件身份对照

以下不是整个仓库的无差别核验。远端身份来自固定提交的连接结果；上传身份用Git blob格式重算。匹配证明所用副本和指定对象字节一致，不声称远端二进制下载成功，也不证明所有未查看的文件相同。

| 远端路径 | 与上传副本 | 上传实际Git blob |
|---|---|---|
{identityrows}

17项检查中15项匹配，交接提示词和MANIFEST不匹配。远端提示词只额外读取第1—18行核对更新说明，未对其全部正文执行文本diff；MANIFEST只从连接读取前三行及完整对象身份，上传清单则实际逐项核验190个文件，全部通过。

远端复审ZIP为21010377字节、对象ae920e88ff8afe3ebaabb24212a2a0b7b2c83b85；上传ZIP为{src['archive_comparison']['uploaded_bytes']}字节、SHA256 `{src['archive_comparison']['uploaded_sha256']}`。二者不字节相同；未下载远端ZIP，不能把压缩大小差直接归因为唯一内容变化。

## 3. 原件和文献阅读层级

| 资料 | 本轮实际读取 | 没有声称的范围 |
|---|---|---|
| 根README、readable/README、QUALITY | GitHub完整文本 | 不把首页替代题目或模型资料 |
| 原题 | 同Git对象的实际上传PDF；渲染并目视第2、4页；readable完整四页文本 | 不声称远端PDF下载成功 |
| 附件1／模板 | 上传真实XLSX、Git对象交叉核对；环境完整241行读取，模板结构及示例格读取 | 没有把表头单位当作题面未写的物理分母 |
| 当前Q2 | 上传提示词和验收全文；必要源代码、正文、NPZ、验证JSON；远端源码对象与审核全文核对 | 未重新执行本地验收中全部十阶段与Radau计算 |
| 第一问 | 远端q1_solver.py第108—155行，历史审核第80—110行 | 没有重跑第一问，没有修改第一问 |
| da Silva2014 | GitHub逐页PDF第4页完整提取文本，重点Meq定义 | 原PDF图像未取得，乱码公式未当作逐式核验 |
| Adrover2020 | GitHub第5、6页文本；出版社HTML方法；上传第6页原页面图目视核验式(8)—(13) | 未下载整篇原PDF，不声称通读所有22页 |
| Foods11 LFS | 本轮不依赖其未知全文或参数 | 没有把LFS指针视为实际PDF，没有宣称获取 |
| COMSOL官方热湿接口 | 官方页面中的表面／域内潜热、扩散携焓说明 | 不当作本药材实验或组成参数来源 |

## 4. 实际访问失败及处理

容器直接请求raw.githubusercontent.com遇到DNS／访问失败；下载工具和网页二进制路径未成功返回PDF。随后使用上传真实原件，并与连接返回的Git对象核对。此为同身份副本使用，不伪称直接从GitHub取得二进制。

表格组件预热／导入没有成功用于本轮读表，实际采用标准库ZIP/XML只读提取。后处理最初一次启动因新输出目录写权限失败，修正该新目录权限后运行成功；保留成功运行stdout/stderr，不宣称整个环境完全无告警。

这些访问和权限问题没有改变模型或已验收输出。后续新目录复现验证显示全部机制NPZ数组及CSV一致；软件管理测试另外记录。文件保护最终检查覆盖上传解压后的191个原文件，哈希均未改变。

## 5. 三种来源的隔离

`reused/q2_final_delivery/`为旧Pro输出及原输入的只读复制；`reused/review_q2_20260911/`为本地审核报告／记录复制。`reused/sensitivity_tracks.npz`是本轮从7个已核验原Pro情景作的精确索引投影，既不称原完整NPZ，也不冒充新PDE求解。

`data/`和`figures/`是本轮轨迹后处理；`runtime/`仅新入口管理，科学源码原样复制；`tests/results/`是本轮管理、短时核函数和单阶段入口验证。完整来源机器记录见source_identity.json、upload_manifest_audit.json及sensitivity_provenance.json。
''')
write('README.md',f'''
# 第二问改进复审交付

**最终决定：有条件保留已验收的result2.xlsx和60个论文表值，不改动原正式答案。**本包补充实算机制分析、表面边界不可识别性、相容的物理扩展骨架、时域裁定和少量已测试的程序管理改进；未新增缺少参数的真实温湿预测。

采用上传复审包中的已验收结果，并实际读取GitHub提交 `{commit}`。远端已更新并包含第二问；检查的原件、验收正文和主要代码与上传相同，交接说明／清单存在版本差异。完整路径与阅读层级见evidence/source_access.md。

## 文件索引

| 文件或目录 | 用途 |
|---|---|
| 深化审查结论.md | 保留决定、阻断与非阻断事项、改进收益和完成程度 |
| 机制与不确定性分析.md | H/M/D、瞬时与累计竞争、确定性灵敏度 |
| 物理闭合与数据需求.md | 三类模型对照、质量与携焓推导、补测优先级 |
| 时域与论文修改建议.md | 3h工作口径、长时扩展契约、可直接使用的论文段落 |
| 程序复用改进与验证.md | 缓存签名、依赖失效、保护规则与实际测试 |
| figures/ | 10对PNG(300dpi)/SVG机制图，全部有源数据 |
| data/ | 每秒三位置机制CSV/NPZ、半小时表、60s贡献、灵敏度和数值记录 |
| source/ | 后处理、来源投影、报告生成和只读交付审计源码 |
| reused/ | 必需的旧结果、原输入和审核记录；不同来源有清单 |
| runtime/ | 完整新源码副本，只有入口管理与新增guard改变 |
| patches/ | 入口unified diff与新guard，供新副本使用 |
| tests/results/ | 本轮26项管理／60s核函数测试、实际tests阶段及resume/拒绝记录 |
| evidence/ | 原件图像、版本比较、原文件哈希、运行与交付审计 |

## 复现本轮主要成果

```bash
python -m pip install -r requirements.txt
python source/q2_mechanism.py --out reproduced_analysis
python tests/test_run_guard.py
python tests/check_entry.py
python source/audit_new_delivery.py
```

第一条后处理命令只读取本包的未舍入状态，不求解PDE，在新目录生成全部图／CSV／NPZ。省略--out则更新本包自己的data和figures；它不会重建旧result2.xlsx。`build_review_docs.py`使用本包已生成记录重建评审文稿，变更输入后应重新人工审阅叙述。

`test_run_guard.py`使用临时软件夹具验证缓存与路径规则，并作两次相同60s/40区间核函数比较；`check_entry.py`实际跑新入口的tests阶段并验证resume与拒绝覆盖。两者不是完整3h生产解或本轮全十阶段验收。

可选地，提供原上传包的解压目录，核验191个原文件仍未改变：

```bash
python source/audit_new_delivery.py --handoff /path/to/extracted_handoff
```

完整原敏感性数组不在本包重复分发；本包内轨迹投影足以重现新增分析。需要从原数组重新提取时，可运行prepare_reused.py并显式传入原交付解压目录、此前单独的q2_validation_arrays.zip和新的投影输出目录。

## 数值与物理结论

H+M与ln(D/D₀)在三位置全部样点的最大差为{v['identity_max_abs_log_error']:.3e}；初始表面先受失水抑制，随后升温占优，晚期又出现净回落。各位置峰值不同，瞬时项可受环境波动而改号，不能据最终D高于初值宣称全过程一直加速。

hₘ和边界平衡映射不可凭外部输入独立识别；没有本药材的气固平衡、真实失重和组成热焓，不能给唯一相变修正值。保留有效容量方程，不直接把它改成无配套携焓的乘积导数，不把相邻扩散引起的C_t当局部蒸发率。

现有Excel仍为1—10800s。方程意图覆盖预热／恒温，并不等于数天输入和停止规则已经闭合；长时研究需单独采用环境延拓、终止规则和相应验证。本轮未求第三问结束时刻或第四问收缩。

## 保护与实际运行状态

原上传191文件哈希保持不变；新后处理独立目录复跑的所有NPZ数组与CSV一致。管理回归26项通过，原／复制核函数未舍入差为0，真实新入口tests阶段、合法resume和非法覆盖拒绝均通过；新入口完整all流程未运行。

本地9月11日已验证portable Excel导出及453600个输出，现作为继承证据；本轮未重新导出旧工作簿，也未用Microsoft Excel桌面打开。已有BDF工作区告警诊断保留，不屏蔽所有警告或改第三方库。

本包是队伍审查材料，不是自动完成的最终参赛论文。图表文字、假设范围及AI参与内容仍需队伍逐项人工核对；仓库身份和哈希记录属于溯源材料，不能直接当成已匿名的竞赛提交压缩包。
''')
# Fix spacing in the explicitly derived temperature equation.
p=B/'物理闭合与数据需求.md';s=p.read_text().replace(r'\rho_d\mathcal H_TT_t',r'\rho_d\,\mathcal H_T\,T_t');p.write_text(s)
