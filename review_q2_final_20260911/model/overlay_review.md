# 缓存与输出保护补丁独立复审

2026-09-11；只读审阅`runtime/overlay/source/q2_run_guard.py`及测试，不修改其文件、不重复长测试。

**结论：补丁可用于当前第二问入口的新运行副本。已确认的问题有对应修复及定向验证，未发现显著新回归；不改变第二问方程或正式数值。**

审阅版本SHA256：`9ff7e2cbec479dcb0246ae5b76d8b3330e60f91eda32da5ec4908c2e72162788`。

| 核查项 | 代码及证据裁定 |
|---|---|
| 上游输出改变后的缓存 | 第139–144行将上游已登记产物哈希纳入签名并取消动态签名缓存；同源码/配置重算得到新字节时，assemble、excel、report、audit失效，独立探针通过。依赖未变且产物字节相同仍可复用，逻辑合适。 |
| Windows路径与别名 | 第97、108行分别规范保护名和所有者路径大小写；第118–128行先拒绝symlink/junction及多硬链接文件，再进入子目录。构造、begin和finish阶段调用检查；真实junction/hardlink及大小写探针通过。 |
| 阶段产物完整性 | 第40–81行补齐当前固定网格、参考解、测试、情景和汇总输出契约，报告检查清单所列PNG/SVG。与driver、finalize、report的实际文件名一致；单个mesh JSON被拒绝，既有完整重跑10阶段的产物全部通过清单检查。 |
| 回归与范围 | 已读`runtime/fixed/runtime_audit.json`、测试源码及入口日志：24项管理/短时核函数检查通过，10个定向探针通过，实际tests阶段、合法resume及拒绝覆盖通过；原11份科学源码字节一致。 |

两项需要Windows符号链接创建权限的原测试因WinError1314明确跳过，未伪称通过；junction和硬链接测试实际执行。核验环境是Python3.13.14，本次没有验证其他Python版本，也没有重新运行完整十阶段。该边界与补丁只修改运行管理的范围一致。

产物契约由当前driver调用`stage_outputs`执行；修改硬编码网格或产物命名时须同步维护契约。安装补丁后guard源码哈希变化会使旧管理签名失效，这是保护行为。保留原Pro目录，并在新副本使用补丁即可，无需因此重算已经验收的正式Excel。
