# 第三问复审：独立参考重跑与复现入口

核查日期：2026-09-11。**新运行支持当前临界时长57.4740 h、正式执行时长57.4741 h及表5。复现入口尚有具体工程改进点；独立逐分钟四位稳定性也应另列，不应由表5通过推及全部Excel数值。** 本轮不修订原件、不重跑完整套件。

## 一、实际执行与独立性

用原 `A题/附件/附件1.xlsx`，BLAS/OMP/MKL线程均为1，实际运行：

```powershell
& .\review_q1_20260910\.venv\Scripts\python.exe -X utf8 -B .\q3_final_delivery\source\q3_reference.py --n 80 --method Radau --rtol 2e-11 --input .\A题\附件\附件1.xlsx --out .\review_q3_20260911\runtime\reference80
```

80阶参考的动态未知量为160，Radau线性系统约480阶；原报告耗时69.04 s，本机本轮实耗 **196.64 s**。与主N2048进程并行时，观察到参考进程峰值工作集约128 MB；未启动第三个长期数值进程。

该算法从0时刻积分，使用全局Chebyshev配点、积分原函数梯度、代数Robin表面以及Radau时间推进，与主有限体积/BDF空间与时间算子不同。它复用了主程序的输入解析器和 `Environment`，因此是空间/时间求解的独立参考，不是完全独立的数据解析或新写的第三套物理模型。本轮运行的是交付内独立参考的原始源码，SHA256与执行命令均记录在机器证据中。

| 检查项 | 本轮实测 |
|---|---:|
| 临界秒数 | 206906.38993234077 s |
| 临界小时数 | 57.47399720342799 h → **57.4740 h** |
| 与原主解临界值差 | -0.004737331561045721 s |
| 与原80阶Radau结果差 | -4.802132025361061e-9 s |
| 根前0.01 s的最大C | 0.1500000029127689 |
| 根后0.01 s的最大C | 0.1499999970872313 |
| 原执行端点206906.76 s的连续谱重构最大C | **0.1499998922080308 < 0.15** |
| 该端点表面C | 0.052646939026722454 |
| 端点与主解21点最大含水率差 | 2.6719551085574444e-9 |
| 表5全部50个含水率值最大差 | 1.1699731405911962e-8 |
| 表5全部50值、执行端点21值四位显示 | 全部一致 |

端点比较不是从舍入表插值：由新参考保存的完整末状态，再实际Radau积分约0.36006766 s至正式206906.76 s，检查原函数插值多项式全部实驻点；最大值位于轴心。保存了 `reference80_official_endpoint.npz`。

参考表面非线性方程的最小导数约2.0874e-6，最大边界残差2.9782e-18，无后备求根触发，最大牛顿次数5，轨迹含水率保持正值。这些是本次数值状态诊断，不能将根两侧0.02 s间隔升级为连续PDE误差界。

## 二、逐分钟输出四位稳定性的剩余差异

在相同60 s输出时刻加统一正式末行，比较全部 **72429个含水率值**，原主解与新80阶参考存在 **8个四位显示差异**。最大未舍入含水率差为3.6031241457123997e-7，位于60 s表面。温度相应比较有1个末位差，最大未舍入差1.4286753113879058e-7 K；温度并不写入第三问result3.xlsx。

| t/s | r/cm | 原主解显示 | 参考显示 | 舍入中点 | 主解减中点 | 参考减中点 |
|---:|---:|---:|---:|---:|---:|---:|
| 3120 | 1.3 | 2.2553 | 2.2552 | 2.25525 | +5.71300014e-8 | -5.05774407e-8 |
| 3840 | 1.1 | 2.2780 | 2.2779 | 2.27795 | +4.7542579e-8 | -2.40605965e-8 |
| 3960 | 1.6 | 1.8806 | 1.8805 | 1.88055 | +5.0229575e-9 | -7.98311515e-8 |
| 12300 | 0.1 | 1.6241 | 1.6240 | 1.62405 | +2.4323115e-9 | -3.27414005e-8 |
| 29160 | 2.0 | 0.3446 | 0.3447 | 0.34465 | -4.95647394e-9 | +4.2190116e-9 |
| 35520 | 1.0 | 0.5087 | 0.5088 | 0.50875 | -1.2179043e-9 | +1.2436834e-9 |
| 94440 | 1.5 | 0.1762 | 0.1763 | 0.17625 | -9.532717e-10 | +1.040116e-10 |
| 180300 | 0.1 | 0.1588 | 0.1587 | 0.15875 | +5.3004157e-10 | -1.02135994e-9 |

所有位置、未舍入双精度值、显示值与十进制中点有符号距离均在 `reference_comparison.json`。**这不推翻时长或表5，也不证明8格中的参考值自动就是真值**；它说明当前逐格四位稳定性没有像第二问那样全部验收。交付原说明主要声称工作簿与自身数组一致、表5与独立参考一致，并未明确承诺全72429格独立舍入一致，因此不应把这件事夸大为全部结果错误。

建议Pro先确定第三问采用的精度承诺。若只要求保留四位格式及受控绝对误差，应清楚区分这两者与独立舍入稳定性；若要求全部四位稳定，则针对这8格确认参考阶数/时间误差与主网格变化，再决定是否需要更高阶后处理或加密。不得仅逐格替换8值、按已知显示答案调参或随意放宽误差阈值。

## 三、已证实的工程问题与验收要求

| 位置 | 触发与证据 | 改进建议及验收 |
|---|---|---|
| `source/make_report.py:50,64,185,213` 等 | 报告只读夹具将summary事件改为60 h后，正文结论仍写57.4740/57.4741；README也有原端点硬编码。原输入下这些数字正确，改参数后会混入旧答案 | 所有结论、尾环境、单元格数、终点、误差数与图横轴范围均由当前summary/config生成。验收用纯报告夹具改事件/未来环境，检查正文、表5、README及图范围同步，无须为测试报告再求解PDE |
| `run_all.py:130–132`；`source/q3_solver.py:242`；主config | 主入口复制写死的execution_time_s=206906.76。求解器只检查该时间在根后且同积分段，main范围不重新计算0.03 s预算/0.36 s量化；full分析仅检验旧值是否碰巧吻合。README已披露换输入后需人工改端点，本轮不把它说成旧答案调参 | 分离原冻结复现与新情景运行。新情景应先求根、生成明确的误差预算和量化端点，必要时续积分；不要复制旧固定端点。验收至少一组提前达标、一组推迟达标的已有情景，结束行均来自新事件而非旧数字；完整长期误差预算另行说明 |
| `run_all.py:95–105`；`source/validate_results.py:69–171` | `--scope verify` 明确使用BASE，随后重写validation诊断、reference端点数组、表5/CSV/end_event，以及正文和README。校验函数xlsx_check本身也写xlsx_readback。本轮未在原目录运行此模式 | 提供只读输入根和独立审核输出根；原MANIFEST对应文件验证前后哈希不变。若保留原地再生成，应改名并显式要求新副本，不能当冻结证据的只读检查 |
| `source/q3_solver.py:211,308–309`；参考同类检查在 `q3_reference.py:124,152` | 原solver只检测.json。孤立夹具已有prefix.npz、无.json时，零时长保存确实覆盖旧npz。该试验没有PDE积分；高层run_all新目录存在即拒绝的保护有效 | 同时拒绝任一目标.json/.npz已存在，采用新目录和成对临时文件的完成标记。验收分别存在一个文件、两个文件、失败后不完整产物，均不静默覆盖；不必照搬复杂缓存系统 |
| `source/validate_results.py:159–164` | 实际复制工作簿后，在W2添加999，或把A1改成错误的min/m单位，xlsx_check仍成功；原72429个所需值保持一致 | 核验精确允许的单元格集合A1:Vn、A1秒/cm语义、半径与行序、时间格式、末行事件。验收加入多余列/行、错误标题单位等孤立坏例，同时保持原副本通过 |
| `run_all.py:69–78` 与 `validate_results.py` | 入口保存source/input SHA但verify没有与清单及实际执行源码版本建立前置校验。重算的配置来自旧结果JSON，新参考则固定几何/传递系数/均值场景 | 正式复现入口核对MANIFEST、input SHA、配置及源版本；尊重 `q3_solver_main_executed.py` 历史实际源码映射，不简单把它误判成篡改。若未来场景改变参数，应显式传给独立参考和diagnostics；不应沿用后处理中写死的hm、未来水分量。用只改输入/配置或源码的坏例验证能明确拒绝旧证据 |

已检查并排除一项疑点：**main范围的绘图并不要求完整几何套件**。`make_report.figures` 第15–26行仅读取主NPZ的full_TC、weights和snapshots，主配置full_samples=true；第27–29行的cylinder80x64_iso.npz有存在性条件，缺失就跳过二维图。run_all的main分支也不调用依赖summary.json的generate。因此不将“缺少cylinder40x64使main绘图必失败”列为问题。

## 四、文件与执行范围

- `reference80.log/json/npz`、`reference80_exit_code.txt`：从原附件真实运行的80阶Radau参考。
- `reference80_official_endpoint.npz`、`reference80_table5.csv`、`reference_comparison.json`：短续算、表5及逐分钟全量比较。
- `audit_runtime.py`、`audit_runtime.log`、`runtime_counterexamples.json`：复制源码后运行校验器及隔离坏例；选定10个原件哈希前后完全一致。
- `counterexample_report_excerpt.txt`、`counterexample_solver.log`：旧结论混入与孤立NPZ覆盖的夹具证据，不是新物理解。
- `source_copy/`：仅供安全运行原验证函数的字节副本；没有编辑原 `q3_final_delivery`。

本轮没有运行全套full，也没有执行原目录verify。复审改进优先消除输出与当前数据不一致、防止冻结证据被改写，并明确8格末位稳定性的口径；不要求为了工程修订重复全部长期计算。
