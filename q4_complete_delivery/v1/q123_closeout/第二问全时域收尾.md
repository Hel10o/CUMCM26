# 第二问完整时域统一导出

本轮将第二问的温度与干基含水率从共同初态联立求解至 **206906.76 s（57.4741 h）**，完整覆盖所选第二、三问模型的烘干过程。旧第二问两张正式工作表共453600个数值格及论文两表60个数值，四位小数均保持一致。

实际读取的仓库提交为 `dbb8845a783fda4eaf754ff4c906d1922cab8020`。旧第三问的 `solution.npz` 虽然同时包含温湿场，但只保存每60秒输出，未保存全部连续插值段。因此不能直接无损恢复逐秒数值，本轮重新求解一条统一轨迹。

物理模型仍是固定半径2 cm、长度25 cm、附录3整套变物性的一维径向有效传热—传质模型。初态为28℃与2.55 kg/kg干基含水率；保留通量散度中的变系数，不显式加入潜热，不用不同模型的温度和含水率拼接输出。

前4小时的环境取附件1分段线性插值；之后采用末小时61个观测点的算术均值平台。平台温度为49.99893442622951℃，等效水分边界量为0.04998754098360656。这个延拓是未来环境假设，第二、三问完全共用。

数值实现复用已审查的参考域Chebyshev配置法和水分原函数通量。正式解采用80阶、Radau时间积分、相对容差2×10⁻¹³；最大步长30秒，4小时前限制为20秒。温湿Robin表面条件联合求解，逐秒值直接评价本次积分的连续解。

此次从初态实算的等号根为206906.38993232802秒，最湿位置在轴心。选定端点206906.76秒的全径向最大含水率为0.14999989220802712，确实小于0.15。以上是一维原有效模型口径；二维全域终点的适用范围另见几何比较报告。

| 对照对象 | 温度最大绝对差 / ℃ | 含水率最大绝对差 / kg·kg⁻¹ | 温度/含水率四位差异数 |
|---|---:|---:|---:|
| 旧Q2前3h未舍入轨迹（含初态） | 1.0744116707428475×10⁻⁹ | 8.41234375492661×10⁻⁸ | 0 / 0 |
| 旧Q2两张正式Excel工作表 | 四位结果相同 | 四位结果相同 | 0 / 0 |
| 旧Q2论文两张结果表 | 四位结果相同 | 四位结果相同 | 0 / 0 |
| 旧Q3全部共同60s点及执行端点 | 1.7135448615590576×10⁻¹⁰ | 6.220135517764902×10⁻¹² | 0 / 0 |
| 已完成的160阶BDF参考：全部60s点及同端点 | 3.271416915140435×10⁻⁹ | 9.331814709856445×10⁻⁹ | 0 / 0 |
| 本轮前1800s逐秒80阶/160阶预检 | 1.6157031268448918×10⁻¹⁰ | 6.348321868188123×10⁻¹⁰ | 0 / 0 |

旧第二问正式数组来自两级有限体积解的Richardson外推；本轮来自同一配置法联合轨迹。最大含水率差出现在第1秒的药材表面，属于两种数值离散对起始边界层的差别，未改变四位输出。本次没有静默改动历史单元格或物性参数。

逐秒未舍入数据保存在 `output/q23_unified.npz`。时间数组包含0至206906秒的每个整数秒，并追加206906.76秒，共206908行；实际径向采样为0至2 cm、间隔0.1 cm。正式工作簿按原模板从第1秒开始，每张表共206907条数据。

| NPZ键 | 维度 | 含义 |
|---|---|---|
| `time_s` | (206908,) | 秒，含初态和小数执行端点 |
| `radius_cm` | (21,) | 当前固定几何的实际半径坐标 |
| `profile_TC` | (206908, 21, 2) | 最后一维0为温度℃，1为干基含水率kg/kg |
| `environment` | (206908, 2) | 每个输出时刻的环境温度和等效水分边界量 |
| `x` | (81,) | 配置节点，x=(r/R)² |
| `snapshot_time_s` | (19,) | 完整配置状态保存时刻 |
| `snapshot_full_TC` | (19, 81, 2) | 对应完整配置节点温湿状态 |

联合轨迹的真实运行日志和统计为 `output/q23_unified.log`、`output/q23_unified.json`。`validation/source_provenance.json`记录复用源码、原始输入及比较基线的字节哈希。向量化输出与标量评价的最大差仅为温度7.11×10⁻¹⁴、含水率2.66×10⁻¹⁵。

冷启动所需源码和附件1均包含在本目录；为复现旧表比较，还附带约7 MB的冻结基线，无需下载整个历史仓库。运行时指定新的输出文件名，求解器拒绝覆盖既有NPZ或JSON。`validation/reproduction_commands.txt`列出正式参数及核验命令。

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 python source/solve_unified_q23.py \
  --n 80 --method Radau --rtol 2e-13 --max-step 30 \
  --out /absolute/new/path/q23_unified
```

本收尾只补齐第二问的完整时间范围及同模型数值一致性。未把一维内部的高精度解释为端部或二维几何误差同样很小，也未将其他潜热模型的二维差异用作本模型误差界；相关判断应结合本轮同物理模型的二维定向检验。

本次空间依据包括新运行的前1800秒逐秒加密，以及已验收160阶BDF在全60秒输出和同一端点的独立参照。后者明确是历史证据的重新比对，不声称本轮又完成了逐秒160阶全程计算；全部结果可由 `validation/unified_audit/audit.json` 追溯。

曾尝试更紧容差的160阶Radau与BDF全程运行，但后期效率明显下降，已停止这两项额外试验。中断日志及原因单独保留，均未计为验证通过。旧端点的二进制表示差2.91×10⁻¹¹秒，比较时按同一十进制端点对齐并记录。
