# 第二问改进复审交接

让 Pro 从 [Hel10o/libai](https://github.com/Hel10o/libai) 读取本目录，并粘贴 `发给Pro的简短提示词.txt`；也可上传同名复审ZIP作为便于读取的副本。主提示词为 `第二问深化分析提示词.md`，要求主动读取GitHub资料并记录实际采用的提交版本。

当前数值已通过验收。本轮优先推进可识别性、物理闭合、机制解释及题意时域，程序改动只按实际收益和影响范围决定。

ZIP保持项目相对目录结构：

| 目录 | 内容与来源 |
|---|---|
| `q2_refinement_handoff/` | 本轮新写的提示词、启动指令、说明和全包SHA256清单 |
| `q2_final_delivery/` | 用户放入项目的Pro原交付119个文件，原样打包 |
| `review_q2_20260911/` | 后续本地验收报告、独立实现、独立完整参考及短时加密、机器证据和日志 |
| `review_q2_20260911/reproduced/` | 本地十阶段重跑的核心结果/两张表/Excel、JSON和日志；不重复打包大型中间验证NPZ、完整CSV及绘图副本 |

本包足以阅读当前答案、分析其未舍入轨迹、查看验证结论并重新求解。为控制重复体积，未包含本地重跑的逐网格、谱参考、敏感性和二维中间NPZ；这些可运行原交付的 `run_all.py` 生成，或从仓库或本地审查目录中实际存在的文件读取。某些历史脚本和报告链接涉及上述省略的大文件；按包内 `omitted_review_files.json` 查询，不把缺少的原Pro中间数组与本地重算副本混为一谈。

原Pro自身拆包情况也保留原说明，其原中间数组不因本次打包而被冒称已补齐。本交接目录随2026-09-11项目同步纳入GitHub；仓库中的 `q2_final_delivery/`、`review_q2_20260911/` 和本目录是最新第二问阅读入口。复审ZIP保留相同相对路径；若后续仓库与上传副本不同，记录实际版本和差异，不将不同来源的结果拼接为同一轮验证。

`MANIFEST.sha256`中的路径相对于ZIP根目录，清单自身不列入哈希。解压后可据此检查传输完整性。本文与历史审查文件都不是新的第二问数值结果；最后裁定由Pro按主提示词完成并交付。
