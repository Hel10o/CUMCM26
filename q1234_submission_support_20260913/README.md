# 支撑材料提交包（2026-09-13）

本轮随正文第四问二维终点讨论精简，新增已有核查JSON、原始NPZ及两份导览/哈希文件，共98个成员、11,495,123字节（11.50 MB / 10.96 MiB）。仅更新两份包内导览，原94个成员中的其余92个逐字节保留；四问结果、全过程数组、12图及AI说明不变。新增记录对应正文第11.3节的限制说明，未运行PDE或导出。详见[核验记录](evidence/q4_endpoint_record_20260913/verification.json)。

本轮交付核对基线为 `13d939f`：现有94成员齐全，12图、代码输入及AI说明与当前论文目录一致。已修正包内导览的旧图数并说明第二问全程NPZ组织；仅一个README变化，其余93成员逐字节保留。压缩包为11,188,361字节（11.19 MB / 10.67 MiB），SHA文件随ZIP提供。实际核验见[本轮记录](evidence/submission_zip_delivery_20260913/verification.json)。

按 2026 年竞赛格式规定，把四问论文所需的支撑材料整理为一个 ZIP：

- 交付物：`out/A题_支撑材料.zip`（10.67 MB，94 个成员，官方上限 20 MB）
- 包内顶层目录：`支撑材料/`

本轮按图 3 的正文显示字号放大 10 张图的文字，并同步图表来源及成员清单。包内仍有 12 张正文图，
全部与当前论文引用的 PDF 字节一致；图 3、图 7、四份结果工作簿、全程轨迹 NPZ 及其余成员均保留刷新前字节。
已整合提交 `285f3d984190b0655e1a46b8cfffe91b656cc7ea` 的独立 AI 说明更新，保留该版 3 页 PDF，
本轮未修改或核验其内容真实性。记录见 `evidence/figure_legibility_refresh_20260913/刷新记录.md`。

此前 AI 说明修订仅替换包内 `AI工具使用详情.pdf` 为按 2026 规定改写的 3 页说明（ChatGPT 桌面端、GPT-6 Astra；求解代码、文献检索、语言润色），其余 93 个成员保持原字节。记录见论文目录 `evidence/ai_details_contest_20260913/`。

此前一轮随问题三流程图去除色块刷新支撑包：用已冻结的白底版本替换原彩色框版本，同步图表来源及成员清单。
包内仍为 12 张正文图，均与当前论文引用的 PDF 字节一致；其余 11 张图、
四份结果工作簿及全程轨迹 NPZ 均保留原 ZIP 中的字节，没有重跑求解、重新导出工作簿或生成数组。
详见 `evidence/q3_white_refresh_20260913/刷新记录.md`。本轮仅重做打包检查，不把历史数值检查记作本轮运行。

问题二流程图替换与机制图新增记录保留在 `evidence/q2_figure_refresh_20260913/刷新记录.md`，
图 5 双热图替换记录保留在 `evidence/q2_heatmap_refresh_20260913/刷新记录.md`；
问题三章首与流程图修订记录保留在 `evidence/q3_workflow_refresh_20260913/刷新记录.md`。
最初的数值核验报告保留在 `evidence/q2_figure_refresh_20260913/baseline_package_report.json`。

## 为什么 result2 拆成两个文件

题目要求第二问逐 1 s、径向每 0.1 cm 保存完整结果。全程工作簿为 206907 行 × 21 个位置 × 2 个物理量，
共 8,690,094 个数值；按现行序列化为 26.87 MB，与官方"支撑材料压缩包不超过 20 MB"冲突。

`xlsx` 本身是 deflate 压缩的容器，因此实测了三种独立序列化，均无法压到 20 MB 以内：

| 方案 | 实测大小 | 脚本 |
|---|---|---|
| 原始工作簿（对照） | 26.87 MB | — |
| 最小 ECMA-376 标记重写 | 28.83 MB | `slim_result2.py` |
| 共享字符串表重写 | 28.90 MB | `sst_probe.py` |

同一份数值若改为紧凑二进制数组只需约 2.4 MB（`int32×10000` 甚至 1.81 MB），说明超限是
xlsx 文本序列化的信息熵下界，不是标记冗余。因此本包按"模板工作簿 + 全程数组"提供，数值不删减：

- `results/result2.xlsx`：题面表 3、表 4 所用的前 3 h 网格（10801 行），模板格式
- `results/result2_全程轨迹_206907x21.npz`：烘干全程每个 1 s 的完整网格

两者数据一致：前 3 h 的 453,600 个温度与水分浓度值逐一相同，仅表头文字不同。

## 构建

```console
python build_package.py
```

脚本会重建全程数组、暂存 `stage/支撑材料/`、扫描身份信息、写 `evidence/package_report.json`
并断言 ZIP 不超过 20 MB。`stage/` 与探针输出不进入版本库。

问题二流程图与机制图修订时使用 `refresh_figures.py` 做仅图表与清单的打包刷新。它以提交
`dfe7b2911672deacdea62096fc409bcf04855fdf` 中原 ZIP 的 SHA-256 为入口检查，直接保留其数值文件，
不调用上述完整构建中的数组生成过程；当前 ZIP 已刷新，再运行该脚本会因基线哈希不同而停止，
防止把后续版本误作本次基线。它没有删除或移动任何暂存目录。

图 5 双热图替换时使用 `refresh_q2_heatmap.py`，以提交 `1b84c827ae72353b37549715ff4da5513cc61ea1`
中 ZIP 的 SHA-256 为基线，并核对已冻结热图的 SHA-256。它只允许将旧图 5 换成热图、同步两个清单，
断言其他 11 张图与数值成员原字节不变。旧脚本、旧轮次证据均保留；本轮刷新后再次运行新脚本也会因
基线哈希不同而停止，不会把后续版本误作本轮输入。

问题三章首修订时使用 `refresh_q3_workflow.py`，以提交 `2206ae259332121e6392f0543fe8e7759595912f`
中 ZIP 的 SHA-256 为入口条件，并核对已冻结的新问题三流程图哈希；只允许置换这一张图及同步两个清单，
其他 11 张图和数值成员必须保持原字节。脚本不会删除暂存目录；当前 ZIP 已刷新，再次运行会因基线哈希
不同而停止。旧脚本和旧证据目录均保持不变。

问题三白底修订时使用 `refresh_q3_white.py`，以提交 `6496064c62f5495d0e4d324905bff0cd782cb2db` 中
ZIP 的 SHA-256 和已冻结白底流程图的 SHA-256 为入口条件，只置换 Q3 图及两个清单。图引用扫描限于
正文主文件及分章文件，不读取本轮无关的 `ai_details.tex`，包内 AI 说明 PDF 保留原字节。再次运行脚本
会因当前 ZIP 已不再等于本轮基线而停止；旧脚本、旧轮次证据均保持不变。

本轮字号修订使用 `refresh_figure_legibility.py --freeze` 冻结最终图形及来源清单哈希，再运行
`refresh_figure_legibility.py` 窄刷新。脚本以已整合 AI 更新的 ZIP 哈希 `8409a3c6358c994d5897cc023de809050e1dc37270d77f2384cde219e78662d8`
为入口条件，只允许置换 10 张指定图及两个清单，保留图 3、图 7 和其余成员字节；写入前再次检查输入和原 ZIP，
防止覆盖并发更新。当前 ZIP 已刷新，再运行会因基线哈希不同而停止。未调用完整构建、求解或数组导出。

`slim_result2.py` 与 `sst_probe.py` 是上表两个压缩探针，只在 `out/` 写临时工作簿用于量体积；
两者都不产出交付文件，运行结果不作为交付证据保留。

## 原始构建已执行的核对

- 论文表 3、表 4 的全部 60 个数值可在 `results/result2.xlsx` 中定位到同一单元格（表头为 0.5 h 步长、半径为 0、0.5、1、1.5、2 cm）。
- 前 3 h 工作簿与全程工作簿前 10801 行的 453,600 个数据值逐一相同。
- 全程数组 8,690,094 个值在写盘后抽样逐值读回一致，末行为 206906.76 s。
- 四份正式结果工作簿无 `docProps`，不含作者元数据。
- 全包二进制身份串扫描 0 命中；图表 PDF 仅有 Matplotlib 元数据。

## 问题二流程图与机制图刷新实际核对

- ZIP CRC、94 个成员读回与预期字节一致，体积 11,249,458 字节，小于原 20 MiB 包体积合同上限。
- 12 张图的路径集合及每个文件的 SHA-256 与当前 TeX 引用一致；旧 Q2 流程图不再入包。
- 四份 `result*.xlsx` 与全程轨迹 NPZ 的字节及 SHA-256 与基线包一致；未重做上节数值逐值核验。
- 对全部解压成员字节做身份关键词扫描，并检查所有 PDF 元数据，均为 0 命中；此项未做 OCR，不能等同于完整匿名性证明。
- 四份结果工作簿均未发现 `docProps/` 成员。

## 图 5 热图替换实际核对

- ZIP CRC 与全部 94 个成员读回通过，体积 11,287,899 字节（10.76 MiB），符合原 20 MiB 包体积检查。
- 12 张图的路径集合和字节与当前 TeX 引用一致；只移除 `figures/font_revision/q2_profiles.pdf`，新增 `figures/q2_heatmap/q2_fields.pdf`，其他 11 张图原字节不变。
- 四份工作簿及全程 NPZ 与前一版 ZIP 逐字节一致；原始数值逐值核验未重跑。
- 全部解压成员身份关键词扫描、全部 PDF 元数据检查均为 0 命中；四份工作簿均无 `docProps/` 成员。扫描不含 OCR，不等同于完整匿名性证明。
- 该轮报告及前一版报告副本保留于 `evidence/q2_heatmap_refresh_20260913/`。

## 问题三章首流程图替换实际核对

- ZIP CRC 与全部 94 个成员读回通过，体积 11,288,228 字节（10.77 MiB），符合原 20 MiB 包体积检查。
- 12 张图的路径集合和字节与当前 TeX 引用一致；只移除 `figures/question_workflows/q3_workflow.pdf`，新增 `figures/q3_intro/q3_workflow.pdf`，其他 11 张图原字节不变。
- 四份结果工作簿和全程 NPZ 与前一版 ZIP 逐字节一致；原始数值逐值核验未重跑。
- 全部解压成员身份关键词扫描、全部 PDF 元数据检查均为 0 命中；四份工作簿均无 `docProps/` 成员。扫描不含 OCR，不等同于完整匿名性证明。
- 该轮报告、前一版报告副本及读回结果保留于 `evidence/q3_workflow_refresh_20260913/`。

## 问题三流程图白底替换历史核对

- ZIP CRC 与全部 94 个成员读回通过，体积 11,288,088 字节（10.77 MiB），符合原 20 MiB 包体积检查。
- 12 张图的路径集合和字节与当前正文引用一致；只移除 `figures/q3_intro/q3_workflow.pdf`，新增 `figures/q3_white/q3_workflow.pdf`，其他 11 张图原字节不变。
- 四份结果工作簿、全程 NPZ 及包内 AI 说明 PDF 与前一版 ZIP 字节一致；未重做原始数值逐值核验。
- 全部解压成员身份关键词扫描、全部 PDF 元数据检查均为 0 命中；四份工作簿均无 `docProps/` 成员。扫描不含 OCR，不等同于完整匿名性证明。
- 当前报告为 `evidence/package_report.json`；本轮报告、前一版报告副本及读回结果位于 `evidence/q3_white_refresh_20260913/`。

## 本轮图内字号刷新实际核对

- ZIP CRC、全部 94 个成员逐字节读回通过，体积 11,188,113 字节（10.67 MiB），小于 20 MiB。
- 12 张图的路径集合及文件字节与正文引用一致；替换 10 张图，图 3、图 7 原字节不变。
- 四份工作簿、全程 NPZ、3 页 AI 说明及其余非图非清单成员原字节不变；只检查保留字节，未重做数值逐值验证。
- 全部解压成员身份关键词扫描、全部 PDF 元数据检查均为 0 命中；四份工作簿均无 `docProps/` 成员。未做 OCR。
- 输入冻结、并发 AI 更新隔离、前一版报告和本轮读回记录均在 `evidence/figure_legibility_refresh_20260913/`；当前报告为 `evidence/package_report.json`。

## 提交前的剩余事项

- 论文附录写作"本文表 3、表 4 及**全程** `result2.xlsx` 采用后续统一轨迹"，与本包实际所放文件不一致；
  提交前应把该句改为实际提交版本，避免"支撑材料与论文内容不相符"。
- `support/launch.py smoke` 只做语法、快照哈希与 `--help` 检查，不等于数值复现；复现命令与限制见
  `支撑材料/support/README.md`。
- 120 MB 的第三方文献 `foods-11-04045-v2.pdf` 因超限未纳入，引用信息见论文参考文献。
