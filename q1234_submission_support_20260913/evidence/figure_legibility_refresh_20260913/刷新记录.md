# 图内字号修订支撑包刷新记录

本轮仅刷新当前正文引用的图形与清单，不重跑 PDE、参数情景、结果工作簿导出或全程数组生成。

## 实际读取与并发整合

- 论文工作起点提交：`4f09fdad4182b6f220e801af54dffd0be37ad04c`；原支撑 ZIP SHA-256：`707f55909541942d055b6b0fecf0f54a655bb12a78fc0d1e42f4252c7aa009aa`。
- 打包等待期间，独立 AI 说明任务更新了支撑包并提交 `285f3d984190b0655e1a46b8cfffe91b656cc7ea`。逐成员读回确认 94 个成员集合不变，仅 `AI工具使用详情.pdf` 改变，其余 93 个成员逐字节一致。
- 本轮保留已整合的新 AI PDF 字节，以更新后的 ZIP `8409a3c6358c994d5897cc023de809050e1dc37270d77f2384cde219e78662d8` 为刷新基线。新旧 AI PDF 均为 3 页；未核验 AI 说明内容真实性。隔离证据见 `concurrent_ai_isolation.json`。

## 实际修改

- 保持 12 张正文图；图 1、2、4、5、6、8、9、10、11、12 改用本轮放大文字的 PDF。图 3 作为字号参照保持原字节，图 7 已为 10 pt 白底流程图，也保持原字节。
- 同步 `support/图表来源清单.md` 与包内 `支撑材料文件列表.md`。全部非图非清单的 80 个成员保留基线字节，包括四份结果工作簿、全程轨迹 NPZ 与 3 页 AI 说明。
- 入口为 `refresh_figure_legibility.py --freeze` 与 `refresh_figure_legibility.py`；冻结清单见 `frozen_inputs.json`。脚本在写入前后核对路径白名单、冻结输入和原 ZIP 哈希，未覆盖历史报告。

## 实际核验

- ZIP CRC 及 94 个成员逐字节读回通过，12 张图路径集合和字节与当前正文引用一致。只移除 10 个旧图路径、加入 10 个新图路径，已有路径中仅两个清单字节改变。
- 图 3、图 7、四份结果工作簿、全程 NPZ、AI PDF 及其余保留成员逐字节一致。逐个数值文件的 SHA-256 见 `refresh_report.json`。
- 对全部解压成员字节进行身份关键词扫描，对全部 PDF 检查元数据，均无命中；四份工作簿均无 `docProps/` 成员。此检查未做 OCR，不等同于完整匿名性证明。
- 成品：94 个成员，11,188,113 字节（10.67 MiB），小于原 20 MiB 体积检查上限。
- 成品 ZIP SHA-256：`031872a5738d2568803722b318c87c7842e51418aca08fdcf7d25cb10c778e2e`。

## 未做或受限

未重跑 PDE、参数情景或数值复现；未重新导出工作簿和数组；未重做原始数值逐值核验。此次字节保持检查不作为新增建模验证。AI 说明只做保留字节、页数和元数据检查，未重新审核内容。

前一版包报告保留为 `prior_report.json`；本轮报告为 `refresh_report.json`，并同步为支撑目录 `evidence/package_report.json`。
