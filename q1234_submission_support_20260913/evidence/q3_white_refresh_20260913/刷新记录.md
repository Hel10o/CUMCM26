# 问题三流程图去除色块后的支撑包刷新

## 实际读取与基线

- 实际读取提交：`6496064c62f5495d0e4d324905bff0cd782cb2db`。
- 原 ZIP：`out/A题_支撑材料.zip`，94 个成员、12 张正文图、11,288,228 字节。
- 原 ZIP SHA-256：`eceb65a8936b3a2b09182e3f37f71c7e61e79a96ed60bf9d880c2ba752f3c642`。
- 已确认旧包含 `figures/q3_intro/q3_workflow.pdf`，不含 `figures/q3_white/q3_workflow.pdf`。
- 收到主任务的图与来源清单冻结确认后才执行最终刷新。已在入口核对新图 SHA-256：`4c5e1ea9a2c3a75cd616c4cc6943a5c79a77390caeacdffc14def05fdb7e9428`。
- 原当前报告以工作区原字节复制为本目录 `prior_report.json`；准备阶段记录旧轮次脚本和证据哈希，旧文件保留不变。原 ZIP 可从上述提交恢复。

## 实际修改

1. 用主任务冻结的白底 Q3 流程图 `figures/q3_white/q3_workflow.pdf` 置换包内原彩色版本路径。
2. 同步已冻结的 `support/图表来源清单.md`，更新包内 `支撑材料文件列表.md`，图数仍为 12。
3. 新增 `refresh_q3_white.py` 与本轮证据，更新当前 ZIP、README 及 `evidence/package_report.json`。
4. 只扫描 `main.tex` 和 `sections/*.tex` 的正文图引用；本轮无关的用户本地 `ai_details.tex` 不参与读取或处理，旧 ZIP 的 AI 说明 PDF 原字节直接保留。
5. 脚本只在明确的 `out/` 目录写临时 ZIP，完成读回检查后替换指定交付文件，未进行递归删除或目录移动。

## 实际核验

- 原、新 ZIP 的 CRC 检查均通过；新 ZIP 全部 94 个成员读回字节及成员集合与打包预期一致。
- 12 个图 PDF 的路径集合与当前正文引用集合一致，各图字节与当前论文源文件一致。
- 其余 11 张图与原 ZIP 对应成员逐字节一致。
- 四份 `result*.xlsx`、全程 NPZ 与原 ZIP 对应成员逐字节一致，五个哈希见 `refresh_report.json`；包内 AI 说明 PDF 同样保持原字节。
- 已存在成员中只有图表来源清单和包内成员清单发生内容变化；除 Q3 图的路径置换外，其他原有成员均保持原字节。
- 对全部解压成员字节做已有身份关键词扫描，检查所有 PDF 元数据，均为 0 命中；四份工作簿均无 `docProps/` 成员。扫描未做 OCR，不构成完整匿名性证明。
- 新 ZIP：11,288,088 字节（10.77 MiB），94 个成员，满足既有 20 MiB 包体积检查；本轮未重新检索竞赛规则。
- 新 ZIP SHA-256：`8f17955fd4622d553a900a3e9d2998a1e26194cead6fafa91c164a24939200d3`。

## 未做或受限

- 本子任务仅打包已冻结图件；绘图内容与版式对比由主论文修订任务完成，本记录不把它列作本打包脚本运行的检查。
- 未运行 PDE、参数情景、`build_package.py`、工作簿导出、数组生成或原始数值逐值复核。
- 未执行 `support/launch.py smoke` 或完整模型复现；未改动、暂存或提交论文目录中的任何文件，包括用户本地 AI 源文件。
