# 问题二图表修订后的支撑包刷新记录

## 基线与范围

- 实际读取原支撑包所在提交：`dfe7b2911672deacdea62096fc409bcf04855fdf`。
- 原 ZIP：`out/A题_支撑材料.zip`，SHA-256 为 `ea93f1b834deb57564685f415082b9c7e9c6e665763875099caf4f159d490b9e`，11,214,227 字节，93 个成员。
- 已确认原包含 `figures/question_workflows/q2_workflow.pdf`，不含 `figures/q2_revision/q2_mechanism.pdf`；原包正文图为 11 张，当前 TeX 引用图为 12 张。
- 原 `evidence/package_report.json` 按原字节复制为本目录 `baseline_package_report.json`。原 ZIP 可从上述 Git 提交恢复。
- 已读回 Git 中原报告：JSON 内容与副本一致；Git 版本为 LF，原工作区及其保留副本为 CRLF，二者仅有检出换行差异，不声称 Git blob 与工作区字节相同。
- 本轮只刷新图表和清单。没有改论文、模型、题面输入、正式数值或其他问的图。

## 实际修改

1. 包内删除旧 Q2 流程图路径，加入当前 `figures/q2_revision/q2_workflow.pdf`。
2. 加入 `figures/q2_revision/q2_mechanism.pdf`，保留其他 10 张正文图的原字节。
3. 更新 `support/图表来源清单.md` 与包内 `支撑材料文件列表.md`。
4. 新增 `refresh_figures.py`，从已验收 ZIP 保留全部数值成员，只替换图表与清单；输出前先在明确的 `out/` 目录内写临时 ZIP，检查后替换指定交付 ZIP。未调用任何递归删除或目录移动。
5. 更新本目录当前构建报告与 README；历史报告单独保存。

## 实际核验

- CRC 检查通过；94 个成员逐个读回，与预期成员集合、字节均相同。
- ZIP 内正文图集合为 12 个 PDF，与当前 TeX 引用路径集合完全一致；每张图与当前论文图源字节一致。
- 四份 `result*.xlsx` 与 `result2_全程轨迹_206907x21.npz` 和原 ZIP 对应成员逐字节一致，五个 SHA-256 见 `refresh_report.json`。
- 除一张旧图路径移除、两张新图路径加入及两个清单修改外，其余成员字节均保持原样。
- 对全部解压成员字节进行已有身份关键词扫描，并检查全部 PDF 元数据，均为 0 命中；该检查不包含 OCR，不据此声称完整匿名性证明。
- 四份结果工作簿均无 `docProps/` 成员。
- 新 ZIP 为 11,249,458 字节（10.73 MiB），94 个成员，满足既有 20 MiB 包体积检查。此处沿用原打包合同，未重新检索竞赛规则。
- 新 ZIP SHA-256：`d703b922b87f88d7f08d1d3dd05cc2b46d4d486a725f2c1a2f444c7aaf5ab5c3`。

## 未做

- 未运行 PDE 求解、参数情景、完整构建入口 `build_package.py` 或工作簿导出。
- 未重建全程数组，未重做原 60 个论文表格数值、453,600 个前 3 h 值及全程数组数值核验；这些历史结果仅保留于 `baseline_package_report.json` 与原始构建记录，本轮证据是数值文件字节完全一致。
- 未运行 `support/launch.py smoke`，未复现模型，未修改独立提交的 AI 使用说明 PDF。

本轮第一次刷新尝试因原文件列表换行格式与预设插入标记不符而停止，未写出或替换 ZIP；调整为按实际清单条目插入后完成上列检查。
