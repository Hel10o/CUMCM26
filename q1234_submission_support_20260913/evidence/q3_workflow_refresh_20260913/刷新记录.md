# 问题三流程图修订后的支撑包刷新

## 实际读取与基线

- 实际读取提交：`2206ae259332121e6392f0543fe8e7759595912f`。
- 原 ZIP：`out/A题_支撑材料.zip`，94 个成员、12 张正文图、11,287,899 字节。
- 原 ZIP SHA-256：`b127ea501b815e0032e396e6757b69668e27e98b0e7106b862c43f3aacd8a5bc`。
- 已确认旧包含 `figures/question_workflows/q3_workflow.pdf`，不含 `figures/q3_intro/q3_workflow.pdf`。
- 新脚本先行准备，收到主任务关于图及图表来源清单冻结的确认后才执行最终刷新。新图 SHA-256 为 `360735eeb04898b7c4bdca66ad3c6409eb4cb4c533b93938915926c653740c1d`，已在执行入口核对。
- 原当前报告以工作区原字节复制为本目录 `prior_report.json`；旧轮次脚本和证据在准备阶段记录哈希，保留不变。原 ZIP 可从上述提交恢复。

## 实际修改

1. 用新 `figures/q3_intro/q3_workflow.pdf` 置换包内旧 Q3 流程图路径。
2. 同步已冻结的 `support/图表来源清单.md`，更新包内 `支撑材料文件列表.md`，图数仍为 12。
3. 新增 `refresh_q3_workflow.py` 和本轮证据，更新当前 ZIP、README 与 `evidence/package_report.json`。
4. 脚本保留原包全部数值字节，只在明确的 `out/` 目录写临时 ZIP，核对后替换指定交付文件；未进行递归删除或目录移动。

## 实际核验

- 原、新 ZIP 的 CRC 均通过；新 ZIP 全部 94 个成员读回字节及成员集合与打包预期一致。
- 12 个图 PDF 的路径集合与当前 TeX 引用集合完全一致，各图字节均与当前论文图源一致。
- 其余 11 张图均与原 ZIP 对应成员逐字节一致。
- 四份 `result*.xlsx` 和 `result2_全程轨迹_206907x21.npz` 与原 ZIP 对应成员逐字节一致，五个哈希见 `refresh_report.json`。
- 已存在成员中只有图表来源清单和包内成员清单发生内容变化；除置换 Q3 图路径外，其他原有成员均保持原字节。
- 对全部解压成员字节做已有身份关键词扫描，检查所有 PDF 元数据，均为 0 命中；四份工作簿均无 `docProps/` 成员。扫描未做 OCR，不构成完整匿名性证明。
- 新 ZIP 为 11,288,228 字节（10.77 MiB）、94 个成员，满足既有 20 MiB 包体积检查；本轮未重新检索竞赛规则。
- 新 ZIP SHA-256：`eceb65a8936b3a2b09182e3f37f71c7e61e79a96ed60bf9d880c2ba752f3c642`。

## 未做或受限

- 未运行 PDE、参数情景、`build_package.py`、工作簿导出或数组生成。
- 未重做原论文表格数值、前 3 h 网格及全程数组数值核验；本轮证据是数值文件与已验收前一版完全相同的字节，历史数值检查保留在旧目录。
- 未执行 `support/launch.py smoke`、完整模型复现或独立 AI 说明 PDF 检查，未编辑论文目录。
