# 问题二图 5 双热图替换后的支撑包刷新

## 实际读取与基线

- 实际读取提交：`1b84c827ae72353b37549715ff4da5513cc61ea1`。
- 原 ZIP：`out/A题_支撑材料.zip`，94 个成员、12 张正文图、11,249,458 字节。
- 原 ZIP SHA-256：`d703b922b87f88d7f08d1d3dd05cc2b46d4d486a725f2c1a2f444c7aaf5ab5c3`。
- 核查旧包含 `figures/font_revision/q2_profiles.pdf`，不含 `figures/q2_heatmap/q2_fields.pdf`。
- 原当前构建报告以原工作区字节复制为本目录 `prior_report.json`；旧轮次 `refresh_figures.py` 与 `evidence/q2_figure_refresh_20260913/` 均未修改。原 ZIP 可从上述提交恢复。
- 收到主任务“新图冻结”确认后才执行最终刷新；热图 PDF 冻结 SHA-256 为 `47fd500235089fddc27d421ead469e8ca59c268ad14d499f8b6dde9a051b4a0e`，执行前已核对。

## 实际修改

1. 移除包内旧 Q2 状态路径与径向剖面图，加入新温度、含水率双热图，图数仍为 12。
2. 同步当前论文的 `support/图表来源清单.md`，更新包内 `支撑材料文件列表.md` 的图源路径。
3. 新增本轮专用脚本 `refresh_q2_heatmap.py`、本轮证据，更新当前 ZIP、README 和 `evidence/package_report.json`。
4. 脚本以原 ZIP 与冻结新图的哈希为入口条件，只在明确的 `out/` 目录写临时 ZIP，完成读回检查后替换指定交付文件；未删除或移动任何暂存目录。

## 实际核验

- 原、新 ZIP 的 CRC 检查均通过；新 ZIP 的全部 94 个成员读回字节及成员集合与打包预期一致。
- 包内正文图集合为 12 个 PDF，与当前 TeX 引用集合一致；每个图 PDF 与当前论文文件字节一致。
- 除被替换的图 5 外，其余 11 张图均与原 ZIP 对应成员逐字节一致。
- 四份 `result*.xlsx` 及 `result2_全程轨迹_206907x21.npz` 与原 ZIP 对应成员逐字节一致，哈希列于 `refresh_report.json`。
- 已存在的成员只有图表来源清单、包内成员清单两项内容变化；其他原有成员字节均保持。
- 对全部解压成员字节做已有身份关键词扫描，并检查所有 PDF 元数据，均为 0 命中；四份工作簿均无 `docProps/` 成员。身份扫描不包含 OCR，不构成完整匿名性证明。
- 新 ZIP：11,287,899 字节（10.76 MiB），94 个成员，满足原 20 MiB 包体积检查；本轮未重新检索竞赛规则。
- 新 ZIP SHA-256：`b127ea501b815e0032e396e6757b69668e27e98b0e7106b862c43f3aacd8a5bc`。

## 未做或受限

- 未运行 PDE、参数情景、`build_package.py`、工作簿导出或数组生成。
- 未重做原 60 个论文表格值、前 3 h 的 453,600 个数值或全程数组数值核验；本轮只确认数值文件与已验收前一版的字节一致，历史检查继续保留在旧证据目录。
- 未运行 `support/launch.py smoke`，未重新复现模型，未改动论文或 AI 使用说明 PDF。
