# 四问论文内容改进与定稿 · 模型指示文档

## 0. 当前状态与任务

**结构修订已经完成。** 提交 `50e7b54`（另 `fca3c12` 调整摘要重点）已把第八、九、十章的标题与组织改到位：第八章为"模型检验：降维有效性与后段环境灵敏度"，第九章为"模型评价、适用范围与推广"并新增 9.1"模型优点"（六条），第十章结论删去重复段，附录加了一行代码导览。执行记录见 `paper/q1234_draft_v1/evidence/structure_revision_20260912/论文结构与表述修订记录.md`。

**本份是第二份指示，做内容层改进。** 不要重复执行结构修订；先读取上表记录与当前 `sections/` 源文，确认基线，再按本份 I1–I5 修改。本份不再要求"先做结构修订"。

任务性质仍是**论文编辑**，不是建模或计算。

---

## 1. 硬边界

**只读不改：** 四份正式 Excel、所有未舍入数组（`.npz`/`.json`）、日志、全部求解器源码。

**明确禁止：**

- 不重跑任何 PDE 求解或参数情景。I3 的作图**只能读已保存数组**，不得为此重新积分。
- 不改写摘要中"针对问题一／二／三／四"四段既有表述与加粗重点（`fca3c12` 刚做过重点调整）；I5 只允许在关键词之前**追加**一句，且不加粗。
- 不改写结论前两段既有内容；I5 只允许追加一句。
- 不新增或删除一级章节（正文仍是 10 个一级标题）。
- 不修改任何已定稿数字。I1 只压缩措辞与去重，**不删证据**：含具体数字的句子、以及"不能声称能耗／不能作为实际保证"这类保护性声明，一律保留。

---

## 2. 改进项 I1：把过度对冲改写为集中化限定

### 2.1 问题

第九章 9.2 现在已把三条限制（气固边界与可达性、经验密度相容性、相变能量）压缩成三个密集段落，这是对的。但第一至七章的正文里仍保留着同样的限定，形成第二次陈述：读者在第四章读到一次、到第九章再读一次。后果是正文每段末尾都在自我否定，只读首句的读者会以为论文什么都没证明。

### 2.2 必须保留在正文的限定（每章一处，不得删）

以下四句决定各问结果的解释范围，必须留在原处：

| 位置 | 保留内容 |
|---|---|
| `q1.tex` 4.4 末段 | 验证不消除等效湿度边界、参考密度、无显式潜热假设的物理不确定性 |
| `q2.tex` 5.6 末段 | 导出一致性不等于全部逐秒状态另作独立求解 |
| `q3.tex` 6.3 末段 | 经验数值余量不含环境、边界映射与物性假设的不确定性 |
| `q4.tex` 7.4 末段 | 局部续算继承前史，不构成连续二维严格界，也不推翻精细主解 |

### 2.3 可压缩为交叉引用的重复（压成一句 + `\ref` 到 9.3）

- `q3.tex` 6.4 中"该节点结果与配对根差支持 57.4741 h 的工程保守方向……也不将该时长解释为精确的二维最短时间"（现约 `q3.tex:164-166`）：三处连续限定压成一句加交叉引用。
- `q4.tex:235-240`："因此，本文保留 51.0921 h……也不等于已经取得连续二维的数学保证"四句压成两句，其余归入 9.3。
- `q2.tex:200-204`：账本免责压缩，保留"不构成潜热、携焓及真实总能量已闭合的证明"一句。
- `q2.tex:184-185`、`q3.tex:172-174`：环境情景"人为设定、非置信区间"在 8.2 已完整说明，正文只留半句。

### 2.4 验收

统计正文（第一至七章）各章末尾的限定句不超过 2 句；被压缩的每一句中的数字，在 9.3 都能找到对应，**全文数字总数不减少**；"能耗""节能""可忽略"仍为零命中。

---

## 3. 改进项 I2：补写"模型推广"

### 3.1 问题

9.1 已经写了"模型优点"（六条），但第九章仍缺正面回答"这个模型还能用在哪"。9.3 末尾那句"推广到真实过程应先满足上述数据与接口需求，不以增加模型复杂度替代验证"是**限制**表述，不构成推广内容。国赛评分含"模型的推广"，这块仍是空的。

### 3.2 位置与组织

在 9.1 之后新增二级小节**「模型推广」**，编号为 9.2，原 9.2、9.3 顺延为 9.3、9.4。加小节前先确认正文没有用"9.2""9.3"这类**数字**引用（现源的交叉引用都用 `\ref` 标签与公式编号，预计不受影响，但必须实际检查）。约三分之一页。

### 3.3 写作要求（据实描述已具备的能力，不夸大）

1. **同类物料的复用**：换成另一种圆柱形农产品，只需替换该料的物性公式与环境记录，控制体收支、离散格式与事件判定逻辑不变；前提是该物料能给出与附录同形制的有效物性与交换系数。
2. **几何的替换**：一维径向与轴对称二维共用同一套控制体收支与原函数改写，换几何只需替换散度算子；8.1 已用同网格配对算例演示一维到二维的切换。
3. **阈值事件判定的移植**：以重构多项式全部实驻点求空间最大值、并把等号临界根与严格执行时刻分开，可直接用于任何"某处不得超过某值"的工艺终点问题，不限于含水率。
4. **收缩历程的处理**：材料坐标加干物质守恒的做法可用于其他已知外半径历程的物料；须同时说明前提——还需该物料的内部运动依据，仅有外半径不能唯一确定内部变形（沿用 `q4.tex` 现有口径）。
5. **工艺余量的情景框架**：8.2 的单因素情景方法可作为取得工况记录后的余量评估模板。

**禁止：** 写成"精度高、通用性强"之类空话；不得声称已用另一物料或其他几何验证过（那需要新计算）；不得把"能够扩展"写成"已经验证"；不得与 9.1 六条优点重复表述。

---

## 4. 改进项 I3：补一张降维证据图（零重算）

### 4.1 问题

"中截面一致但整根失水分歧"是全文最重要的方法论结论，现在只靠表 `tab:wholebody` 与文字（`validation_limits.tex:2-20`）支撑。一张图比表更能让人一眼接受"一维够用但只限于中截面"。

### 4.2 数据来源（已保存，**不要重跑 PDE**）

配对的一维／二维场在以下位置，键名一致：

| 情形 | 二维数组 | 配对一维数组 | 时刻 |
|---|---|---|---|
| 问题一 | `q4_complete_delivery/v1/validation/geometry/q1_80x128.npz` | `q1_80x0.npz` | 1800 s |
| 问题二 | `q4_complete_delivery/v1/validation/geometry/q23_80x256_integral.npz` | `q23_80x0_integral.npz` | 10800 s |
| 问题四 | `q4_complete_delivery/v1/validation/geometry/q4_80x128_integral.npz` | `q4_80x0_integral.npz` | 21600 s |

已在问题一配对文件上核实的键：`snapshots (n_t, n_z, n_r, 2)`、`snapshot_times_s`、`mid (n_t, 21, 2)`、`Cmax`、`weights`、`reference_r_m`、`z_m`。二维中截面即 `z` 取中间一层；一维文件轴向维为 1。同目录已有 `midplane_and_end_effects.png` 与 `dimension_reduction_check.json` 可参考，但需按论文质量重绘。

### 4.3 图的内容

双联纵排：

- **上排**：给定时刻的中截面径向含水率剖面，二维与一维叠画（一维用虚线），显示几乎重合；图注给出最大差（问题一、二用正文已有数字，不新造）。
- **下排**：整根体积加权平均含水率随时间演化，二维与一维分离，并在三点标注失水增加百分比 **7.2533%、4.7118%、2.7379%**（与表 `tab:wholebody` 完全一致，不得出现第二个数）。

### 4.4 实现要求

- 复用 `paper/q1234_draft_v1/build_assets.py` 的绘图风格（字体、字号、配色、线型），输出 `figures/dimension_reduction.pdf` 与 `.png`。
- 在 8.1 插入该图并加一句引导，**不新增小节**；图号沿用自动编号。
- 图注与 evidence 写明来源数组路径；若新增绘图脚本，纳入 evidence 与清单，并按现有 `assets_manifest.json` 的方式记录数据依赖。
- 图注须写明"体积加权平均在空间均匀干密度假设下等于干质量加权平均"（沿用现有口径）。
- 若绘图脚本无法在只含论文源稿的独立目录运行（依赖仓库内冻结数组），如实记录数据依赖，不得把图造成凭空重算。

---

## 5. 改进项 I4：把潜热论述的落点补到受影响最大的位置

### 5.1 问题

潜热的量化现在只在 9.3"相变能量与可报告的指标"一段（`validation_limits.tex:74`）。但**受影响最大的是第一问的预热段**——同一段失水的潜热需求约为同段对流输入的 9.50 倍——而第一问是必须交的规定表。读者读完第四章时并不知道这一点。

### 5.2 做法

1. 在 `q1.tex` 4.4 末尾**追加一句**：本问所处时段是相变能量缺口影响最显著的时段，量级与相关限制见第 9.3 节；该处原有的物理不确定性那句保留不动。
2. 在 `q2.tex` 5.6 末尾同样追加一句交叉引用（前数小时同属早期大通量时段）。
3. 具体数字（45.59 kJ、4.801 kJ、9.50、164.10 W/m²）**只保留在 9.3**，正文不复制，避免两处不一致。

### 5.3 禁止

**不得声称方向。** 不得写"因此本文结果偏乐观""偏保守"或"真实时长一定更长"。耦合相变后表面降温会增加对流输入、同时改变扩散，净效应需重解，现有证据不足以定方向。只能写量级显著、必须重解。

---

## 6. 改进项 I5：补量级一致性声明

### 6.1 依据

57.4741 h ≈ 2.39 天，51.0921 h ≈ 2.13 天，均落在题目所述"2–3 天"区间内；第四问终点位于附件 2 半径观测时域内。

### 6.2 做法

- 摘要：在"关键词"之前**追加一句**（不改动四段原文，也不加粗）：所得干燥时长落在题目所述 2—3 天区间内。
- 结论第二段末尾同样追加一句。
- 措辞写成"量级一致／落在题目所述区间内"，**不得**写成"与题目一致因此模型得到验证"——那是把量级核对冒充验证。

---

## 7. 可选改进项 I6：第四问精细二维正式点

**仅在具备计算条件时执行；不执行则保持现稿条件表述，不做任何改动。**

- 合同见 `q1234_overall_review_delivery/v1/最小补算计划.md` 第 2 节 E1（固定条件、改变因素、前史处理、比较指标、成功标准、失败处置）。
- 若执行：写入新目录，不覆盖历史证据；完成后据实更新第七章、摘要与结论中的"待核验"表述（通过则升级，未通过则按 E1 失败处置另立版本讨论）。
- 若未执行：第七章与摘要维持"二维证据支持保守方向、全域终点待精细二维直接核验"，**不得**因为本份文档提到该项就默认它已完成。
- 不可从复审中约 28 s 的局部续算外推耗时，须先基准测时，并遵守 E1"先作一维退化对照，明显不够即停止"的要求。

---

## 8. 措辞红线

1. 全文零命中："能耗""节能""可忽略"；60.65%／60.6526% 只能写成模型临界时间缩短。
2. 不得写"题面指定中截面"；不得把附录 ρ 同时当真实湿密度与干密度。
3. 不得新增连续二维严格界、"各处达标已充分验证"、"实际执行保证"类主张。
4. "模型推广"不得出现无证据的性能宣称；不得把"可扩展"写成"已验证"。
5. 潜热方向不得断言。
6. 量级一致性不得写成验证。
7. I1 压缩措辞时，保护性声明与含数字的句子不得删除。

---

## 9. 一致性检查

1. 一级标题仍为 10 个；摘要四段原文与加粗重点未被改写（只追加一句未加粗的句子）。
2. 正文（一至七章）每章末尾限定句不超过 2 句；被压缩句中的数字在 9.3 可查，全文数字总数不减少。
3. 新增图中三个百分比与表 `tab:wholebody` 完全一致，无第三个数。
4. 全文"能耗""节能""可忽略""题面指定中截面"零命中；60.65%／60.6526% 语境正确。
5. "各处达标"在摘要、第六章、第七章、第十章四处强度同级。
6. 第九章含"模型优点"（已有）与"模型推广"（本轮新增）两个正面小节；小节编号顺延后无重复、无跳号。
7. 所有两处新增 `\ref` 有效；编译日志无 undefined reference，无 `??`。
8. 正文页数 ≤ 30，并记录新页数（当前正文 27 页，预计新增约 1 页，须实测）。
9. AI 声明仍位于参考文献之前。

---

## 10. 交付物

1. 修改后的 `paper/q1234_draft_v1/sections/*.tex`、`main.tex`（摘要与结论仅追加句）、`figures/` 新图。
2. 若新增绘图脚本，纳入 `paper/q1234_draft_v1/` 与 evidence，并更新 `assets_manifest.json` 类清单。
3. 本轮修订记录：`paper/q1234_draft_v1/evidence/<修订名>_<日期>/`，写明改了什么、依据什么、验收了什么，附页面检查 JSON、图的数据来源与哈希。
4. 更新 `paper/q1234_draft_v1/README.md` 与仓库根 `README.md` 中受影响的句子（图数、页数、本轮改进项）。
5. 若修改了源稿 ZIP 内包含的文件，同步更新该 ZIP 与其清单（`source_package.json`），保持目录与压缩包一致（`AGENTS.md` 约定）；旧清单按现有做法备份，不覆盖历史证据。
6. 按 `AGENTS.md` 提交并推送到 `origin/main`；不强制推送，不覆盖历史证据；报告时区分"已本地提交"与"已远程同步"。

---

## 11. 验收标准

- I1：正文限定句数量下降，证据数字一个不少，9.3 承接全部量化内容。
- I2：第九章有"模型推广"小节，据实、无空话、无未验证宣称、不与 9.1 重复。
- I3：新增降维图由已保存数组生成（附来源路径与哈希），与表格数字一致，未为此重跑 PDE。
- I4：第一、二问各有一句指向 9.3 潜热论述的交叉引用；全文无方向性断言。
- I5：摘要与结论各有一句量级一致性声明，措辞不越界、未加粗。
- I6（若执行）：新目录、无覆盖、按 E1 验收并据实更新表述；若未执行，正文保持原条件表述。
- 编译通过；正文页数 ≤ 30；已定稿数字逐字未变（抽样核对三处时长与六处反例数字）。

---

## 12. 报告格式

分四块，未做的不写成已完成：

1. **实际读取**：提交 SHA（应为 `fca3c12` 或其后）、读过的文件与结构修订记录。
2. **实际修改**：逐文件列出（压缩的限定句、新增小节、新增图与脚本、追加的句子）。
3. **实际核验**：第 9 节九项检查逐项结果、编译与页数、新图与表格数字比对、抽样数字比对。
4. **未做/受限**：例如 I6 未执行、未重跑求解、未做人工审核。

遇到阻碍时保留具体证据与已完成部分，不得编造结果，不得把未运行的检查写成已通过。
